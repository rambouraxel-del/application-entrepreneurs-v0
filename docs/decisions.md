# Décisions importantes

## Emplacement du projet
Le dossier `Application-Entrepreneurs-V0/` a été créé dans `Dossier Développement Claude Code`, le répertoire de travail dédié au développement, distinct des dossiers `Chartes` et `V0` qui contiennent les documents de référence.

## Technologie
Le projet reste en HTML/CSS/JavaScript natif, sans framework ni dépendance externe, conformément à la règle de simplicité de la V0.

## Arborescence
L'arborescence proposée initialement a été validée sans adaptation.

## Page de connexion (étape 2)
- `index.html` reste le point d'entrée technique du prototype et redirige automatiquement vers `pages/login.html`, qui est la vraie page de connexion fictive. Cela évite de dupliquer le contenu de connexion à deux endroits.
- Nom provisoire de l'application retenu : "Cockpit Entrepreneur".
- Le lien "Mot de passe oublié ?" est intégré à titre visuel uniquement, sans action réelle.
- "Créer un compte" / "Demander un accès" ne sont pas intégrés à cette étape.

## Structure générale et navigation par onglets (étape 3)
- Navigation par simples liens HTML entre les pages de `pages/`, sans JavaScript : solution la plus simple et la plus fiable pour une V0 statique en HTML/CSS/JS natif.
- La barre d'onglets est dupliquée dans les six fichiers HTML des pages principales. **Ceci est une décision temporaire propre à la V0**, acceptée uniquement parce que le projet reste une V0 statique simple sans système de composants ni de templates. Ce choix devra être réétudié si le projet évolue vers une architecture avec composants réutilisables (par exemple lors du passage à la V1), afin d'éviter la duplication durable d'un même élément d'interface, conformément au principe de non-duplication de la charte technique.
- Pas de zone supérieure secondaire (bandeau de titre/contexte) à cette étape : le titre `<h1>` de chaque page suffit à indiquer la position de l'utilisateur.

## Squelette du tableau de bord (étape 4)
- Les emplacements d'indicateurs favoris sont nommés de façon neutre ("Indicateur 1" à "Indicateur 4") plutôt qu'avec des exemples concrets, afin de ne pas anticiper le contenu détaillé prévu à l'étape 5.
- Agenda du jour / To-do list et Alertes / Notifications sont disposés en grille à deux colonnes égales ; cette disposition reste simple (CSS Grid natif, sans media query) et pourra être affinée pour le responsive plus tard si nécessaire.

## Enrichissement visuel du tableau de bord (étape 5)
- La to-do list ne reprend pas "Relancer le devis Martin" parmi les tâches, puisque cette action est déjà affichée comme priorité du jour ; deux autres tâches ("Vérifier les stocks", "Envoyer la relance client Bernard") sont affichées comme terminées à la place, pour éviter toute incohérence entre une action présentée comme priorité et comme tâche déjà faite.
- Les tâches terminées sont simulées par un style visuel (texte barré + coche décorative en CSS `::before`), sans case à cocher HTML interactive, afin de ne donner aucune impression de liste modifiable.
- Les cartes d'indicateurs favoris utilisent une bordure fine sans ombre (au lieu de l'ombre des cartes KPI) pour rester visuellement moins prioritaires, conformément à la hiérarchie demandée, sans changer la structure de grille validée à l'étape 4.
- Les couleurs de statut (vert, rouge, orange, gris/bleu) ne sont utilisées que sur les éléments concernés (variations KPI, alertes) et nulle part ailleurs dans l'interface.

## Pages secondaires fictives (étape 6)
- Toutes les coordonnées fictives (e-mails) utilisent le domaine `example.com`, réservé aux usages documentaires, afin d'éviter toute ressemblance avec de vraies coordonnées. Les numéros de téléphone suivent une numérotation séquentielle clairement fictive (01 23 45 67 89, 02 34 56 78 90, etc.).
- Tous les boutons fictifs ("Ajouter un client", "Ajouter un rendez-vous", "Créer une facture", "Créer un devis", "Modifier" sur chaque section Paramètres) portent la classe commune `btn-wip`, sans action réelle, afin d'être reliés au pop-up "Work in progress" à l'étape 7 sans réécriture du HTML à ce moment-là.
- La page Base de données ne comporte aucun bouton d'action, conformément à sa vocation de simple aperçu des données centrales, non d'un espace de gestion.
- Réutilisation des classes déjà existantes (`.dashboard-block`, `.dashboard-card`, `.agenda-list`, `.favorite-value`) sur les pages secondaires plutôt que la création de nouvelles classes équivalentes, conformément au principe de non-duplication de la charte technique.

## Pop-up "Work in progress" (étape 7)
- Contrairement à la duplication acceptée pour la barre d'onglets (étape 3), la structure HTML du pop-up n'est **pas** dupliquée dans les pages : elle est générée dynamiquement par `js/app.js` et injectée une seule fois dans la page au premier clic sur un élément `btn-wip`. Le texte, la structure et le comportement du pop-up n'existent donc qu'à un seul endroit dans le projet.
- Le lien "Mot de passe oublié ?" (page de connexion), jusque-là purement visuel sans aucune réaction au clic, est désormais relié au même système `btn-wip` : il ouvre le pop-up au lieu de faire remonter la page en haut. Cela ne change pas sa nature non fonctionnelle, mais donne un retour clair à l'utilisateur.
- La balise `<script src="../js/app.js">` n'a été ajoutée qu'aux pages contenant réellement un élément `btn-wip` (Clients, Agenda, Facturation, Paramètres, Connexion) ; le Tableau de bord et la Base de données n'en ont pas besoin puisqu'ils ne contiennent aucun bouton fictif.

## Harmonisation visuelle générale (étape 8)
- `pages/dashboard.html` porte désormais `class="page-content dashboard wide"` : la classe `dashboard` reste comme repère de contexte réservé à d'éventuels styles futurs propres au tableau de bord, mais elle ne porte plus la largeur (fournie uniquement par `.wide`), pour supprimer la duplication de règle entre `.page-content.dashboard` et `.page-content.wide`.
- Les boutons `.btn-primary` et `.btn-secondary` partagent désormais la même taille et le même gabarit, mais restent distinguables grâce à la couleur de fond (plein vs contour) et au poids de police (600 vs 500), conformément à la demande de conserver une hiérarchie visuelle claire entre action principale et secondaire.
- `.login-card` et `.wip-modal`, tous deux des panneaux centrés proéminents, partagent désormais la même valeur d'ombre plutôt que deux intensités différentes.
- Le correctif de spécificité sur `.page-subtitle` ne change pas le contenu affiché, seulement sa taille réelle (14px au lieu de 15px par erreur) : aucun impact sur les données ou la structure des pages.

## Refonte graphique globale — V0.1
- **Sauvegarde préalable obligatoire** : le projet ne dispose pas de dépôt Git, donc une copie complète du dossier (`backup-v0-before-v0-1-refonte-graphique/`) a été créée avant toute modification, pour permettre un retour arrière manuel si nécessaire.
- **Abandon de la navigation par onglets supérieurs au profit d'une sidebar** : ce changement structurel majeur remplace entièrement `.tabs`/`.tab`/`.tab-active` (supprimées du CSS) par `.sidebar`/`.sidebar-link`/`.sidebar-link-active`. Le choix reste conforme à la charte technique (composants standardisés, un seul système de navigation actif à la fois, pas de double logique).
- **`js/app.js` est désormais chargé sur les 7 pages** (y compris Tableau de bord et Base de données), car chacune contient maintenant une cloche de notifications `btn-wip` dans sa topbar. Cela annule la décision de l'étape 7 qui limitait le chargement du script aux pages contenant un bouton fictif — la situation a changé, la règle reste la même (charger le script uniquement là où `btn-wip` est présent), simplement `btn-wip` est désormais partout.
- **Paramètres retiré de la navigation principale** : conformément à la mission V0.1, l'accès à `settings.html` passe désormais uniquement par l'icône "roue crantée" de la topbar (lien réel, fonctionnel), présente sur toutes les pages. Aucun lien de la sidebar n'est actif sur `settings.html` ; c'est l'icône roue crantée elle-même qui est mise en surbrillance sur cette page.
- **Nouveau lien réel ajouté** : "Voir l'agenda" (bloc Agenda du jour du tableau de bord) pointe vers `agenda.html` — ce n'est pas une nouvelle fonctionnalité mais un raccourci vers une page déjà existante et déjà accessible depuis la sidebar.
- **Graphique "Évolution du CA"** : SVG statique dessiné à la main (polyline + zone de dégradé), sans aucun calcul ni donnée réelle, ajouté uniquement pour se rapprocher visuellement de la maquette. Il ne constitue pas un système de statistiques.
- **Icônes** : toutes les icônes (sidebar, topbar, cartes KPI) sont du SVG inline écrit à la main, sans bibliothèque ni police d'icônes, conformément à la règle de simplicité et d'absence de dépendance externe.
- **Aucune entrée de sidebar supplémentaire** (Trésorerie, Produits/Services, Finance, Statistiques) n'a été créée : ces modules sont explicitement listés comme hors périmètre par la mission V0.1, la maquette servant uniquement de référence graphique et non de spécification fonctionnelle complète.
- **Variables CSS introduites** (`:root` avec couleurs, ombres, rayons) pour centraliser la charte graphique et faciliter d'éventuels ajustements futurs, conformément au principe de factorisation demandé et à la charte technique (styles centralisés, dissociés de la logique).

## Corrections ciblées du tableau de bord — V0.1.1
- **Sélecteur de date limité au tableau de bord** : décision explicitement validée par le chef de projet pour rester dans un périmètre correctif ciblé ; son extension aux autres pages (harmonisation complète de la topbar) est reportée à une itération future.
- **Fusion Priorité du jour / To-do list** : plutôt que de garder deux sources d'information séparées pour une action qui est fondamentalement une tâche, "Relancer le devis Martin" devient le premier élément (mis en avant visuellement) de la to-do list. Une seule source de vérité pour cette information, conformément au principe de non-duplication de la charte technique.
- **Disposition à 3 colonnes** (`dashboard-row-3`) ajoutée comme modificateur de `.dashboard-row` plutôt que comme système parallèle : la classe de base (`.dashboard-row`, 2 colonnes) reste disponible pour un usage futur, la variante à 3 colonnes s'obtient en cumulant les deux classes.
- **Couleurs décoratives des blocs Agenda** : uniquement des teintes indigo/violet/bleu (famille "accent" de la marque), jamais vert/rouge/orange, pour ne pas empiéter sur les couleurs réservées aux statuts (règle établie depuis l'étape 5 et respectée depuis).
- **Cohérence inter-pages renforcée** : le montant de l'alerte "facture en retard" du tableau de bord (1 250 €) reprend désormais exactement la valeur de la facture FAC-001 affichée sur la page Facturation, plutôt que de laisser deux informations non recoupées sur le même sujet.
- **Test visuel réel bloqué par la politique de navigation de l'extension Chrome** : `file://` refusé (accès aux fichiers locaux non autorisé pour l'extension) et `http://localhost:8834/` également refusé (domaine hors liste blanche de navigation). Un serveur HTTP local minimal (PowerShell, sans dépendance) a été laissé actif sur `http://localhost:8834/` pour permettre une vérification manuelle rapide par le chef de projet ; il ne modifie aucun fichier du projet, il ne fait que le servir.

## Structuration technique et Git — V0.1.2
- **Racine du dépôt Git = `Application-Entrepreneurs-V0/`**, et non le dossier parent `Dossier Développement Claude Code/` : ce choix exclut naturellement, sans règle `.gitignore` dédiée, le dossier de sauvegarde (`backup-v0-before-v0-1-refonte-graphique/`) et le dossier de configuration `.claude/`, tous deux situés en dossiers frères hors du dépôt.
- **Identité Git configurée localement au dépôt** (`git config user.name`/`user.email`, sans `--global`) : Axel Rambour / rambour.axel@gmail.com, à la demande explicite du chef de projet, pour ne pas affecter d'autres dépôts éventuels sur la même machine.
- **Deux commits séparés** plutôt qu'un seul : le premier (`v0.1.1 - refonte graphique stabilisée`) capture exactement l'état applicatif déjà validé, sans aucun ajout ; le second (`v0.1.2 - structuration technique et documentation projet`) regroupe uniquement les nouveaux documents et mises à jour de cette étape. Cela garde un historique Git lisible où chaque commit correspond à une intention claire.
- **Correction du premier commit par `amend`** : la première tentative de commit contenait une faute d'encodage ("stabilisee" au lieu de "stabilisée"). Comme ce commit venait d'être créé à l'instant, sans aucune autre opération entre-temps et sans avoir été partagé, il a été corrigé par `git commit --amend` plutôt que par un nouveau commit correctif — cas où l'amend reste sûr, contrairement à la règle générale de préférer toujours un nouveau commit.
- **Tag `v0.1.1`** créé sur le premier commit uniquement (pas sur le second), pour que le tag corresponde exactement à l'état visuel validé, sans les ajouts documentaires de la V0.1.2.
- **Pas de trailer `Co-Authored-By`** dans les messages de commit de ce projet, à la demande explicite du chef de projet — messages de commit strictement conformes au texte fourni.
- **Dépôt GitHub distant non créé** : `gh` (GitHub CLI) n'est pas installé sur cette machine ; aucune tentative de créer un dépôt distant, d'ajouter un remote ou de pousser n'a été faite. Une procédure manuelle a été documentée à la place (voir rapport de l'étape V0.1.2).

## Navigation et architecture générale — V0.2
- **`pages/database.html` conservée mais orpheline**, plutôt que supprimée : option explicitement autorisée par le chef de projet, pour ne pas casser un accès direct par URL et laisser la décision de suppression/refonte à une phase ultérieure (voir `docs/backlog.md`).
- **Sidebar/topbar de `database.html` non simplifiées** : la page garde exactement la même chrome (sidebar à 7 liens, aucun actif ; topbar avec recherche/cloche/roue) que les autres pages, pour rester visuellement cohérente si elle est ouverte directement, plutôt que de créer un traitement spécial à part.
- **Icônes des 3 nouveaux modules** : SVG inline dessinés à la main (portefeuille pour Trésorerie — réutilisation de l'icône déjà utilisée pour le KPI "Trésorerie disponible" du tableau de bord —, boîte/paquet pour Produits/Services, bâtiment à colonnes pour Finance), sans bibliothèque ni dépendance externe, conformément à la règle de simplicité déjà appliquée depuis la V0.1.
- **Nouveau composant `.construction-banner`** plutôt que de réutiliser un bloc existant : les pages placeholder ont un besoin visuel spécifique (signaler clairement "module non terminé") que ne couvrait aucun composant déjà en place ; ce composant est générique et réutilisable pour de futures pages placeholder.
- **`.placeholder-grid` créée en complément de `.database-grid`** plutôt que de réutiliser cette dernière : les deux règles sont identiques aujourd'hui (3 colonnes), mais nommer la nouvelle classe d'après son usage réel (zones de la page Finance) évite la confusion qu'aurait créée l'usage d'une classe nommée d'après une page en voie de suppression.
- **Pas de bouton d'action sur Trésorerie et Finance** : ces deux pages présentent des indicateurs (lecture), pas des listes d'éléments à créer, contrairement à Produits/Services qui reçoit un bouton fictif "Ajouter un produit" cohérent avec le pattern déjà utilisé sur Clients/Facturation.
- **Sélecteur de date non étendu** aux nouvelles pages ni aux pages existantes, conformément à la décision déjà prise en V0.1.1 et rappelée explicitement dans la mission V0.2.
- **Clarification roadmap** : ajout d'une section dans `docs/roadmap-v0bis.md` précisant que la V0.2 pose uniquement la navigation, tandis que le développement fonctionnel réel de Trésorerie/Produits-Services/Finance reste prévu en V0.7/V0.8/V0.9 — décision validée explicitement par le chef de projet avant exécution.
- **Travail réalisé sur la branche `v0.2-navigation-architecture`**, sans merge dans `main`, sans tag, sans push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Tableau de bord hub de navigation — V0.3
- **Transformation `<div>`/`<li>` → `<a>` sans changement de classes de base** : les cartes KPI et les lignes d'alerte conservent exactement les mêmes classes CSS qu'avant (`dashboard-card kpi-card`, `alert-item alert-critical`/`warning`/`info`), seule la balise change et des classes additives (`kpi-card-link`) sont ajoutées. Choix fait pour garantir un rendu visuel strictement identique au design V0.1.1 validé.
- **Chevron de survol en CSS pur (`::after`)** plutôt qu'un SVG ajouté dans le HTML de chaque carte KPI : évite d'alourdir le balisage des 7 cartes concernées (4 KPI + 3 favoris) pour un simple indicateur discret, et centralise ce détail visuel dans une seule règle CSS.
- **Cartes du résumé mensuel partiellement cliquables** : "Clients ce mois-ci", "Devis envoyés" et "Dépenses du mois" pointent vers leur module respectif (demande explicite du chef de projet, ajoutée après la proposition initiale) ; "Tâches terminées" reste non cliquable faute de module Tâches dédié — cohérent avec la décision de garder la to-do list intégrée au tableau de bord plutôt que d'en faire un module séparé.
- **"Appeler le fournisseur Dupont" non cliquable** : aucun module "Fournisseurs" n'existe dans la navigation V0.2 ; plutôt que de rediriger vers un module approximant (Clients ou Produits/Services), la tâche reste sans lien pour ne pas induire l'utilisateur en erreur sur la destination.
- **Tâches déjà cochées non cliquables** : une tâche terminée n'a pas besoin de porte d'entrée vers un module, elle documente une action passée.
- **"Voir toutes" (alertes) reste en Work in progress** : contrairement aux alertes individuelles qui ont chacune une destination claire, "voir toutes les alertes" n'a pas de page unique qui agrège l'ensemble des modules concernés — pas de vraie destination, donc pas de vrai lien.
- **Bouton "Personnaliser les indicateurs"** ajouté sous forme d'un simple lien texte discret au-dessus de la grille KPI plutôt qu'un bloc visuel imposant (icône dédiée, bandeau, etc.), pour respecter la consigne de ne pas alourdir une zone déjà dense en informations.
- **Aucune page cible modifiée** (`clients.html`, `agenda.html`, `facturation.html`, `tresorerie.html`, `produits-services.html`, `finance.html`) : cette V0.3 relie le tableau de bord vers l'extérieur, elle ne touche pas aux pages de destination elles-mêmes.
- **Travail réalisé sur la branche `v0.3-dashboard-hub`**, sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Liste clients CRM & accès fiche client — V0.4.1
- **Paramètre d'URL `?client=<slug>` dès la V0.4.1**, à la demande explicite du chef de projet, alors que le contenu de la fiche reste générique : le nom affiché et les initiales de l'avatar sont dérivés du slug (ex. `atelier-leroy` → "Atelier Leroy" / "AL"), mais aucune autre donnée (téléphone, e-mail, historique) ne varie encore selon le client. Choix qui prépare la V0.4.2 sans construire de vraie logique de données par client.
- **Statuts centralisés dans `js/app.js` (`CLIENT_STATUSES`)** plutôt que codés en dur à la fois dans le `<select>` et dans chaque `<td>` : le filtre est peuplé dynamiquement depuis ce tableau, seule source à modifier pour faire évoluer la liste en V0.4.3. Les badges des lignes du tableau restent écrits individuellement en HTML (les données clients ne sont pas encore pilotées par un modèle JS), limite assumée et documentée plutôt que de construire un rendu de tableau entièrement dynamique, hors périmètre de cette version.
- **Mapping badge validé par le chef de projet** : Prospect/Inactif → `badge-neutral`, Client actif → `badge-success`, À relancer → `badge-warning`, Fidèle → `badge-info` (indigo, pour distinguer un client fidèle d'un client simplement actif sans mobiliser une couleur réservée aux statuts d'alerte), Litige → `badge-danger`.
- **Jeu de données porté de 4 à 6 clients fictifs** pour représenter chacun des 6 statuts : nécessaire pour que le filtre par statut soit réellement testable sur chaque valeur, pas seulement sur les 3 statuts déjà présents en V0.3.
- **Recherche et filtre implémentés en lecture directe du DOM** (attributs `data-search`/`data-status` sur chaque `<tr>`), sans modèle de données JS séparé : cohérent avec le hors périmètre explicite de la V0.4.1 (pas de vraie base de données ni de persistance), tout en rendant la fonctionnalité réellement utilisable.
- **Un seul bouton d'action par ligne ("Voir la fiche")** plutôt que plusieurs (Modifier/Détails/Historique), conformément à la demande explicite : ces actions détaillées seront proposées depuis la fiche client elle-même, pas depuis la liste.
- **Lien "Clients" de la sidebar reste actif sur `fiche-client.html`** : choix fait pour signaler que cette page appartient au même ensemble fonctionnel que la liste, plutôt que de la traiter comme une section à part sans lien actif.
- **Vérification visuelle réalisée par test fonctionnel réel** (recherche, filtre, reset, navigation aller-retour) via l'outil de prévisualisation intégré, malgré l'échec systématique de la capture d'écran (timeout) : l'inspection DOM/CSS et l'arborescence d'accessibilité ont suffi à confirmer le bon rendu, sans recourir uniquement à des vérifications de code comme lors des phases précédentes où le navigateur n'était pas accessible.
- **Travail réalisé sur la branche `v0.4.1-clients-crm-entry`**, sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Fiche client CRM complète — V0.4.2
- **`CLIENT_DETAILS` remplace entièrement l'ancienne logique de nom déduit du slug (V0.4.1)** : décision explicitement validée par le chef de projet. La fiche affiche désormais des données réellement différenciées par client plutôt qu'un simple nom recalculé, tout en restant une source statique en mémoire (pas de vraie base de données), conformément au hors périmètre de la V0.4.2.
- **Cohérence stricte avec `clients.html`** : nom, entreprise, téléphone, e-mail, statut et dernier contact de chaque entrée `CLIENT_DETAILS` recopient exactement les valeurs déjà affichées dans le tableau de la page Clients, pour éviter toute incohérence visible entre les deux pages (exigence explicite du chef de projet).
- **`CLIENT_STATUSES` exposé via `window.COCKPIT_CLIENT_STATUSES`** : le mapping badge (label + classe CSS) déjà centralisé en V0.4.1 pour le filtre de `clients.html` est réutilisé tel quel pour le badge de statut de l'en-tête client, plutôt que de dupliquer une seconde liste de statuts sur cette page — poursuit l'effort de centralisation en vue de la V0.4.3.
- **État "Client introuvable" plutôt qu'un contenu par défaut silencieux** : un slug absent ou inconnu masque le contenu de la fiche et affiche un message explicite avec un retour vers la liste, conformément à la demande explicite, pour ne jamais laisser deviner une fausse identité de client.
- **Listes plafonnées strictement aux volumes demandés** (3 notes, 5 événements d'historique, 3 rendez-vous, 3 documents) avec un message d'état vide dédié quand une liste est vide (ex. aucun rendez-vous à venir pour `atelier-leroy`), plutôt que de masquer le bloc entier : la structure de la fiche reste identique quel que soit le client, seul le contenu varie.
- **Réutilisation maximale des composants existants** (`.dashboard-block`, `.dashboard-block-header`, `.dashboard-block-link`, `.dashboard-row`, `.data-table`, `.badge`, `.kpi-card`) plutôt que la création d'un système de blocs dédié à la fiche client : cohérence visuelle immédiate avec le reste de l'application et volume de CSS additif réduit. Seuls les éléments sans équivalent (en-tête étendu, cartes de notes, items d'historique/rendez-vous, état vide) reçoivent des classes nouvelles, nommées distinctement pour ne pas interférer avec `.agenda-item`/`.todo-item` du tableau de bord.
- **Grille `.client-kpis` à 5 colonnes héritant du responsive de `.dashboard-kpis`** : plutôt que de dupliquer les règles `@media` existantes pour un cinquième cas, la nouvelle classe ne définit que la valeur par défaut (5 colonnes) ; les règles responsive déjà en place pour `.dashboard-kpis` (2 colonnes sous 1100px, 1 colonne sous 760px) s'appliquent automatiquement puisque l'élément porte aussi cette classe.
- **"Voir dans l'agenda" et "Voir dans Facturation" pointent vers les pages génériques**, sans filtrage par client : décision explicitement validée, car ni `agenda.html` ni `facturation.html` ne disposent aujourd'hui d'une logique de filtrage par client — construire ce filtrage aurait dépassé le périmètre de la V0.4.2.
- **Bouton "…" en simple déclencheur du pop-up Work in progress**, sans menu déroulant réel : décision explicitement validée, le projet ne dispose d'aucun composant de menu contextuel à ce jour et sa création aurait dépassé le périmètre.
- **KPI "Gestes commerciaux" en un intitulé court** plutôt qu'un intitulé détaillé mentionnant remises/concessions, conformément à la préférence explicite du chef de projet.
- **Maquette utilisée comme référence de structure uniquement** : le bloc "Plan Professionnel" visible sur la maquette n'a pas été repris (absent du projet), la sidebar et la topbar n'ont pas été modifiées, et les valeurs d'exemple de la maquette n'ont pas été copiées telles quelles quand elles entraient en contradiction avec les données déjà existantes sur `clients.html` (ex. "Entreprise" reste "—" pour Martin Dupont, comme dans la liste, plutôt que "Dupont Construction" vu sur la maquette).
- **Travail réalisé sur la branche `v0.4.2-client-profile-crm`**, sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Ajustements UX fiche client — V0.4.2.1
- **Pas de nouvelle branche** : la V0.4.2 n'étant pas encore validée/mergée dans `main`, cette correction reste un commit supplémentaire sur `v0.4.2-client-profile-crm`, décision explicitement validée par le chef de projet. Un seul tag `v0.4.2` sera créé lors de la finalisation, couvrant la fiche CRM complète et ces ajustements — pas de tag `v0.4.2.1` séparé.
- **KPI restructurés avec valeur + légende par indicateur** (`{ value, caption }`) plutôt qu'une légende générique par position de carte : nécessaire car "Prochaine action" n'a pas la même légende selon le client (ex. "Rendez-vous prévu" vs "Aucune action prévue"), contrairement à l'ancien modèle où la légende était fixe et écrite directement dans le HTML.
- **"Prochaine action" affiche une date courte en valeur et le type d'action en légende** (plutôt qu'une phrase longue combinée) : cohérent avec le format visuel des autres KPI (valeur courte et proéminente, légende explicative en dessous).
- **"Montant à encaisser" et "Devis en cours" restent des chiffres indépendants des listes Documents/Rendez-vous affichées** (pas un simple recomptage des lignes visibles) : ce sont des indicateurs de pilotage à part entière, comme "Chiffre d'affaires généré" n'est pas recalculé depuis les documents listés.
- **`.fiche-back-btn` renommée `.fiche-back-link` et redéfinie en petit lien discret** plutôt que de créer une classe entièrement nouvelle : la classe n'était utilisée que sur cette page, son usage change (positionnement + taille) mais son rôle (lien de retour) reste identique.
- **Icônes en SVG inline simples, cohérentes avec le reste du projet** (sliders, crayon, liste, horloge, calendrier, document, téléchargement) : aucune librairie d'icônes ajoutée, conformément à la demande explicite ; mêmes conventions que les icônes déjà présentes dans la sidebar et les KPI (`viewBox="0 0 24 24"`, `stroke="currentColor"`, `stroke-width="2"`).
- **`.block-icon-btn` créée pour les actions secondaires en icône** (30px, style proche de `.topbar-icon-btn` mais plus petit et adapté à un en-tête de bloc) plutôt que de réutiliser `.topbar-icon-btn` tel quel, jugé trop grand dans ce contexte.
- **Chaque icône d'action porte un `title` et un `aria-label`**, conformément à la consigne d'accessibilité explicite — vérifié un par un lors des tests.
- **"Voir toutes les notes" convertie en icône, "Nouvelle note" reste en texte** : décision explicite du chef de projet, la seconde étant jugée une action principale à garder lisible.
- **Rendez-vous liés cliquables via un identifiant `id` ajouté à chaque rendez-vous dans `CLIENT_DETAILS`**, sans construire de vrai ciblage dans `agenda.html` : le lien `agenda.html?rdv=<id>` prépare la navigation sans dépasser le périmètre (l'agenda ne sait pas encore lire ce paramètre).
- **Travail réalisé sur la branche `v0.4.2-client-profile-crm`** (poursuite), sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Interactions CRM préparatoires & modales placeholder — V0.4.3
- **Moteur de modale générique indépendant du pop-up "Work in progress"** plutôt que de réutiliser/étendre `.wip-overlay`/`.wip-modal` : ces derniers sont un composant à message unique déjà validé sur toutes les pages ; les modales V0.4.3 doivent porter des formulaires (sélecteur, textarea, listes), ce qui aurait exigé de le remanier en profondeur avec un risque de régression. La duplication CSS reste limitée (un seul composant `.modal-*` supplémentaire).
- **"Enregistrer"/"Ajouter"/"Supprimer" cumulent `data-modal-close` et `btn-wip`** sur le même bouton plutôt que d'écrire une logique dédiée : le clic ferme la modale (moteur générique) et déclenche le pop-up Work in progress existant (délégation déjà en place), sans code supplémentaire à maintenir pour ce comportement.
- **La modale "Personnaliser les statuts" remplace le contenu de la modale de statut** (même overlay réutilisé) plutôt que d'empiler une seconde fenêtre, décision explicitement validée par le chef de projet — plus simple qu'une vraie pile de modales pour un besoin encore placeholder. Un lien "Retour au changement de statut" permet de revenir en arrière en rouvrant la modale de statut avec le client courant.
- **Modale "Toutes les notes commerciales" en lecture seule**, sans icônes modifier/supprimer par ligne, décision explicitement validée : les actions modifier/supprimer restent uniquement sur les 3 notes visibles dans la fiche, pour ne pas complexifier une vue qui n'est qu'un aperçu élargi.
- **`notesArchive` ajouté à `CLIENT_DETAILS`** (2 notes fictives par client, 1 seule pour `julien-petit` dont l'historique est trop récent pour en justifier davantage) : source statique dédiée à la modale "Toutes les notes", distincte de `notes` (les 3 dernières, affichées sur la fiche) pour ne pas mélanger les deux volumes.
- **Focus géré simplement** : le premier champ pertinent de chaque modale reçoit le focus à l'ouverture (`data-modal-autofocus`), et le focus revient au bouton déclencheur à la fermeture (capturé via `document.activeElement` au moment de l'ouverture) — suffisant pour un prototype, sans construire un vrai piège de focus (tab trapping), jugé hors périmètre pour des modales placeholder.
- **Icônes 100 % réutilisées ou en SVG simple inline** (crayon et réglages déjà utilisés en V0.4.2.1, poubelle et "+" nouveaux mais mêmes conventions `viewBox`/`stroke`) : aucune librairie d'icônes ajoutée, conformément à la demande explicite.
- **Travail réalisé sur la branche `v0.4.3-crm-placeholder-interactions`**, créée depuis `main` après validation et fusion de la V0.4.2 ; sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Ajustements de parcours (retour contextuel + statut cliquable) — V0.4.3.1
- **Badge de statut converti en vrai `<button>` plutôt qu'un `<span role="button">`**, à la demande explicite du chef de projet : un élément natif interactif gère gratuitement le focus clavier et l'activation Entrée/Espace, sans code JavaScript supplémentaire à maintenir pour la gestion clavier (contrairement à un `<span>` qui aurait exigé un `tabindex`, un `role` et une gestion manuelle des touches). Le reset des styles par défaut du navigateur (`border: none`, `font: inherit`) reste minimal car `.badge`/`.badge-*` définissaient déjà `background-color`, qui prime sur le style natif du bouton.
- **Icône crayon du statut supprimée sans remplacement** : le badge lui-même porte désormais l'affordance (curseur, léger assombrissement au survol, chevron discret en `::after` réutilisant la technique déjà utilisée sur `.kpi-card-link` en V0.3), pour éviter l'ambiguïté relevée par le chef de projet (l'icône séparée laissait penser à une modification de toute la fiche).
- **Chevron du badge en `currentColor`** plutôt qu'une couleur fixe : s'adapte automatiquement à la couleur de chaque statut (vert, orange, rouge, etc.) sans règle CSS dédiée par statut.
- **Lien de retour contextuel injecté par JavaScript plutôt qu'écrit en dur dans `agenda.html`/`facturation.html`** : n'apparaît que si les paramètres `from`/`client` sont présents et valides, ce qui garantit qu'un accès direct à ces pages (sidebar) reste strictement inchangé, sans avoir à dupliquer une condition d'affichage dans chaque page HTML.
- **`CLIENT_DETAILS` exposé via `window.COCKPIT_CLIENT_DETAILS`** (même pattern que `window.COCKPIT_CLIENT_STATUSES` en V0.4.1) : évite de dupliquer les noms de clients pour construire le libellé "Retour à la fiche de [Nom]" sur des pages qui n'ont normalement pas accès à cette donnée.
- **Repli sur un libellé générique** ("Retour à la fiche client") si le slug est présent mais inconnu de `CLIENT_DETAILS` : le lien reste fonctionnel (il pointe quand même vers `fiche-client.html?client=<slug>`, qui affichera son propre état "Client introuvable" le cas échéant) plutôt que de masquer le lien de retour dans ce cas limite.
- **Pas de vrai filtrage Agenda/Facturation par client** : les paramètres `from`/`client` ne servent qu'à afficher le lien de retour, conformément au hors périmètre explicite.
- **Travail réalisé sur la branche `v0.4.3-crm-placeholder-interactions`** (poursuite), sans merge, tag ni push : sera couvert par le même tag `v0.4.3` que le reste de la version, sans tag séparé (même logique que la V0.4.2.1).

## Catalogue Produits / Services & accès fiche — V0.5.1
- **Pas de colonne Catégorie/Famille dans le tableau**, décision explicite du chef de projet : évite d'alourdir un catalogue encore jeune ; la question pourra être réétudiée plus tard si le nombre de produits/services le justifie.
- **Seulement deux types (Produit, Service)**, sans "Forfait" ni "Abonnement" : ces notions relèvent de modalités de vente/paiement (comptant, plusieurs fois, récurrent), qui seront traitées plus tard dans la fiche produit/service ou dans la Facturation — pas comme des types au sens du référentiel catalogue.
- **Mapping couleur des badges Type/Statut arbitré par le chef de projet** : Service → `badge-info`, Produit → `badge-neutral` ; Actif → `badge-success`, Brouillon → `badge-warning`, Inactif → `badge-neutral`, Archivé → `badge-info` (plutôt que `badge-danger`, réservé à des statuts réellement critiques dans d'autres modules — l'archivage n'est pas une alerte).
- **TVA affichée en texte simple, jamais en badge** : une TVA n'est pas un état/statut au sens visuel des badges existants, et rester en texte évite de créer une fausse impression de logique fiscale déjà construite. Elle reste strictement indicative en V0.5.1 (pas de calcul réel, pas de dépendance au régime fiscal/type de société, pas de prix TTC).
- **`.clients-filters`/`.clients-status-select`/`.clients-counter` généralisées en `.table-filters`/`.table-select`/`.table-counter`**, décision explicite du chef de projet sous réserve de non-régression vérifiée : ce pattern (recherche + filtres + compteur + réinitialisation) est appelé à être réutilisé dans d'autres modules (Agenda, Facturation...), autant lui donner un nom neutre dès maintenant plutôt que de dupliquer une classe nommée "clients" pour chaque nouveau module. Seuls les noms de classes CSS changent ; les `id` utilisés par le JavaScript (`clients-search`, `clients-counter`, etc.) restent inchangés, donc aucune logique existante n'a été touchée.
- **`PRODUCT_TYPES`/`PRODUCT_STATUSES`/`PRODUCT_DETAILS` centralisés dans `js/app.js` et exposés globalement** (`window.COCKPIT_PRODUCT_*`), même pattern que `CLIENT_STATUSES`/`CLIENT_DETAILS` en V0.4.1/V0.4.2 : prépare la fiche détaillée de la V0.5.2 (qui pourra réutiliser ces sources) sans construire de vraie base de données.
- **Fiche produit/service V0.5.1 volontairement minimale** (nom, type, prix HT, TVA, statut + bannière "à venir"), sur le modèle de la toute première version de `fiche-client.html` en V0.4.1 : établit le lien catalogue → fiche et le pattern d'URL (`?item=<slug>`) sans anticiper le contenu détaillé de la V0.5.2, qui reste à cadrer avec le chef de projet.
- **Travail réalisé sur la branche `v0.5.1-products-services-catalog`**, créée depuis `main` après validation et fusion de la V0.4.3 (+ V0.4.3.1) ; sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Fiche Produit / Service complète — V0.5.2
- **Grille 5 colonnes du résumé commercial dupliquée (`.kpis-5`) plutôt que réutilisation/renommage de `.client-kpis`**, décision explicite du chef de projet : évite de toucher `fiche-client.html` et donc tout risque de non-régression sur une page déjà validée, pour un gain de nommage jugé secondaire à ce stade.
- **"Statut d'utilisation" et "sélectionnable en devis/facture" dérivés automatiquement du statut** (table `STATUS_AVAILABILITY` dans `js/app.js`), plutôt que des champs indépendants par produit, décision explicite du chef de projet : élimine tout risque d'incohérence entre le statut affiché et sa disponibilité. Explicitement cadré comme une logique préparatoire d'affichage, pas une règle métier active de facturation — reste une simple table de correspondance statut → libellé, à l'image de `HISTORY_TYPES`/`CLIENT_STATUSES`.
- **Bénéfices client / Limites-conditions en listes à puces courtes (1 à 2 éléments)**, décision explicite du chef de projet, via une nouvelle classe minimale `.simple-list` : garde la fiche lisible sans sombrer dans des paragraphes longs qui alourdiraient une page déjà riche en blocs.
- **Modalités de paiement en badges neutres uniformes** (pas de couleur sémantique par modalité), décision explicite du chef de projet : ce sont des tags descriptifs, pas des statuts, une hiérarchie de couleur créerait une fausse impression de priorité entre eux. Liste centralisée `PRODUCT_PAYMENT_MODALITIES` (locale à l'IIFE de la fiche, non exposée globalement, à la différence de `PRODUCT_TYPES`/`PRODUCT_STATUSES` qui servent aussi au catalogue) : Comptant, Forfait, Abonnement, Plusieurs fois, Récurrent — explicitement distinctes du champ Type (Produit/Service) pour éviter la confusion signalée par le chef de projet.
- **Coût estimé et Marge estimée en champs texte statiques et totalement indépendants** (ex. "280 € HT" et "≈ 63 %"), sans aucune formule reliant les deux : évite de laisser croire à un moteur de marge déjà fonctionnel, conformément au hors périmètre explicite. Prépare un futur outil de calcul réel sans en construire la logique maintenant.
- **`.info-grid-wide` (variante une colonne de `.info-grid`, poids de texte normal)** pour la Description commerciale : les champs longs (description détaillée, listes de bénéfices/limites) rendaient la grille 2 colonnes habituelle illisible ; une nouvelle variante minimale évite de complexifier `.info-grid` pour tous ses autres usages (Informations client, Paramètres de vente).
- **`.history-item-compact` (variante 3 colonnes de `.history-item`, sans colonne auteur)** : l'historique de l'offre n'a pas de notion d'auteur (contrairement à l'historique des échanges client), une variante dédiée évite une colonne vide disgracieuse plutôt que de réutiliser tel quel `.history-item`.
- **Bouton "Modifier" unique et secondaire dans l'en-tête**, en `btn-wip`, sans menu d'actions ni icône supplémentaire : le chef de projet a explicitement limité l'en-tête à une seule action, les vraies interactions (édition, modales) restant hors périmètre jusqu'à la V0.5.3.
- **`PRODUCT_DETAILS` enrichi in place** (mêmes clés de slug qu'en V0.5.1) plutôt qu'une structure de données séparée pour la fiche détaillée : une seule source par produit/service, cohérente entre catalogue et fiche, à l'image de `CLIENT_DETAILS` pour les clients.
- **Travail réalisé sur la branche `v0.5.2-product-service-detail`**, créée depuis `main` après validation et fusion de la V0.5.1 ; sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Interactions préparatoires Produits / Services — V0.5.3
- **Détail de marge accessible via un lien texte discret plutôt qu'une carte KPI entièrement cliquable**, décision explicite du chef de projet : évite de mélanger deux sémantiques différentes du pattern `.kpi-card-link` déjà utilisé ailleurs (navigation vers un module), qui aurait pu laisser croire à une navigation plutôt qu'à l'ouverture d'une modale.
- **Aucune action "Utiliser dans un devis" ajoutée au bloc "Utilisation future en facturation"**, décision explicite du chef de projet : le message textuel déjà présent suffit à annoncer la V0.6 ; un bouton sans vraie destination (la Facturation n'existe pas encore) risquerait de donner une impression de fonctionnalité cassée plutôt que de préparation.
- **Historique de l'offre plafonné à 3 événements inline, historique complet réservé à la modale**, décision explicite du chef de projet (revenant sur le choix initial de la V0.5.2 de tout afficher inline) : donne une vraie utilité à la modale "Historique complet" plutôt qu'un simple doublon du bloc de la fiche, et garde le bloc inline compact. Deux offres (`audit-strategique`, `creation-site-vitrine`) ont reçu un 4e événement fictif pour rendre ce plafond observable.
- **Nouveau type d'événement "Modification des modalités de paiement" ajouté à `OFFER_HISTORY_TYPES`**, demandé explicitement pour la modale d'historique complet : cohérent avec l'ajout de la modale "Modalités de paiement acceptées" dans cette même version.
- **`PRODUCT_PAYMENT_MODALITIES` promue source globale** (`window.COCKPIT_PRODUCT_PAYMENT_MODALITIES`, définie dans l'IIFE catalogue aux côtés de `PRODUCT_TYPES`/`PRODUCT_STATUSES`), alors qu'elle était locale à l'IIFE de la fiche en V0.5.2 : nécessaire pour que la modale "Ajouter un produit / service" (catalogue) et les modales de la fiche partagent exactement la même liste, sans duplication de données.
- **Ajout de l'option "Autre" aux modalités de paiement**, non cochée par défaut et absente des données existantes : reste purement indicative dans les modales, sans impact sur les badges déjà affichés sur les 8 offres du catalogue.
- **Modale "Changer le statut de l'offre" avec un bouton "Valider" plutôt que "Enregistrer"**, précision explicite du chef de projet : distingue visuellement un changement d'état (validation) d'une sauvegarde de formulaire classique, tout en gardant le même comportement technique (ferme la modale, déclenche Work in progress).
- **Badge de statut de la fiche converti en vrai `<button>` (`badge-clickable`)**, exactement le pattern déjà validé sur la fiche client en V0.4.3.1 : cohérence intentionnelle entre les deux modules plutôt qu'une variante inédite.
- **Formulaires "Ajouter un produit / service" et "Modifier l'offre" dupliqués entre l'IIFE catalogue et l'IIFE fiche** plutôt que factorisés dans une fonction partagée : les deux IIFE restent volontairement indépendantes et autonomes (une page peut être ouverte sans l'autre), à l'image du choix déjà fait pour `.kpis-5` en V0.5.2. Le contenu des formulaires reste proche mais les valeurs par défaut diffèrent (statut initial Brouillon vs statut actuel de l'offre). *(Le formulaire "Modifier l'offre" de la fiche a depuis été retiré lors de la correction V0.5.3.1, remplacé par des modales dédiées bloc par bloc — voir plus bas. Le formulaire "Ajouter un produit / service" du catalogue reste inchangé.)*
- **Nouvelles classes CSS minimales** : `.modal-input` (champs texte/nombre en modale, sur le modèle de `.modal-textarea`), `.modal-checkbox-list`/`.modal-checkbox-item` (cases à cocher des modalités de paiement), `.kpi-detail-link` (lien discret sous une carte KPI) — aucune n'existait déjà sous une forme réutilisable.
- **Travail réalisé sur la branche `v0.5.3-products-services-interactions`**, créée depuis `main` après validation et fusion de la V0.5.2 ; sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Correction V0.5.3.1 (avant clôture de la V0.5.3)
- **Correctif de contraste scopé à `#item-profile-content`** plutôt qu'un changement de `--color-text-secondary` (variable globale utilisée sur tout le site) : corrige la lisibilité signalée sur la fiche produit/service sans risquer de régression visuelle sur les autres pages (Dashboard, Clients, Agenda...), conformément à la consigne explicite de ne pas faire de refonte graphique globale.
- **Bloc "Utilisation future en facturation" supprimé sans remplacement fonctionnel équivalent**, remplacé par "Coûts & marge" : le premier était jugé trop déclaratif (uniquement oui/non + texte), le second apporte une vraie valeur d'usage (tableau de coûts, total calculé) tout en restant préparatoire.
- **"Coût estimé" devient un total calculé à l'affichage** (`quantite × coutUnitaire` sommé sur les lignes de `couts`) plutôt qu'un texte statique : demande explicite du chef de projet ("un calcul local simple côté front"), tout en respectant strictement le hors périmètre (aucune formule de marge, aucune persistance). Le champ `coutEstime` (V0.5.1/V0.5.2) est retiré au profit de `couts`, une seule source alimentant à la fois le KPI, le bloc et la modale de marge — élimine le risque d'incohérence entre ces trois affichages.
- **Libellé "Quantité par vente / prestation"** plutôt que "Nombre d'unités par produit", précision explicite du chef de projet : le bloc doit fonctionner aussi bien pour un produit physique que pour un service, un vocabulaire orienté "produit" aurait été trompeur pour les offres de type Service.
- **"Ajouter un coût" ouvre une vraie modale placeholder** (nom, quantité, coût unitaire) plutôt qu'un simple déclenchement direct du pop-up Work in progress, précision explicite du chef de projet revenant sur la proposition initiale plus minimaliste : cohérent avec le niveau de finition des autres actions de la fiche (toutes ouvrent une modale avant le Work in progress).
- **Historique : auteur fictif unique "Administrateur principal"** plutôt que "Axel Martin" (nom propre) ou plusieurs identités différentes, précision explicite du chef de projet : reste sobre et prépare la logique d'audit trail (qui a fait quoi, quand) sans introduire de personnage superflu dans les données de démonstration.
- **Retour à l'affichage standard `.history-item` (4 colonnes avec auteur)**, suppression de `.history-item-compact` (introduite en V0.5.3 uniquement parce que l'historique n'avait pas encore d'auteur) : évite de maintenir deux variantes de la même liste pour une différence qui n'a plus lieu d'être.
- **Badge Type cliquable, même pattern que le badge Statut** (`badge-clickable`), pour une cohérence stricte entre les deux badges de l'en-tête plutôt qu'une variante inédite pour le type.
- **Icône crayon à côté du nom plutôt qu'un bouton texte** pour "Modifier le nom de l'offre" : le nom est désormais la seule information de l'en-tête sans action dédiée après le retrait du bouton global "Modifier" ; une icône discrète reste cohérente avec le traitement des autres actions bloc par bloc (Description commerciale, Note interne, Historique).
- **`openEditOfferModal`/`buildOfferFormFields` supprimés** (V0.5.3) : devenus du code mort après la suppression du bouton global "Modifier", leurs responsabilités étant désormais couvertes par les modales dédiées (nom, type, statut, description, paramètres de vente).
- **Pagination des listes (Produits/Services et Clients) volontairement non traitée** dans cette correction, décision explicite du chef de projet : sujet transversal à plusieurs listes, mérite une version dédiée plutôt qu'un traitement partiel greffé sur une correction de fiche.
- **Travail réalisé sur la même branche `v0.5.3-products-services-interactions`** (poursuite), sans nouvelle branche : correction avant validation finale de la version, pas de merge, tag ni push.

## Pagination & listes génériques — V0.5.4
- **Helper commun `window.COCKPIT_LIST_PAGINATION.init(config)` (Option A, factorisation légère)** plutôt qu'une duplication complète entre Clients et Produits/Services, décision explicite du chef de projet : le helper reste volontairement agnostique des filtres métier (il reçoit une fonction `matchRow` fournie par chaque page), ce qui évite de le complexifier ou de le rendre fragile face à des besoins de filtrage différents (1 filtre pour Clients, 2 pour Produits/Services, potentiellement plus pour Devis/Factures en V0.6).
- **Le helper est défini dans sa propre IIFE, en dehors de toute logique métier existante**, et n'est appelé que par les deux IIFE de liste (Clients, Produits/Services) qui, elles, sont réécrites uniquement sur leur section recherche/filtre/compteur : aucune autre IIFE (modales, fiches, catalogue de données) n'est modifiée, conformément à la consigne de ne pas refondre `app.js`.
- **Réinitialiser remet aussi la taille de page à 5**, décision explicite du chef de projet : un reset qui laisserait la taille de page inchangée aurait été incohérent avec l'idée d'une remise à zéro complète de la liste.
- **Pagination calculée sur le résultat déjà filtré** (recherche + filtres), pas sur le total brut : la page courante est automatiquement recalée (`Math.min`/`Math.max`) si elle n'existe plus après un changement de filtre, recherche ou taille de page — évite d'afficher une page vide ou un compteur incohérent.
- **Compteur enrichi paramétrable par libellés** (`labelSingular`/`labelPlural`/`labelEmpty`) plutôt qu'un texte figé dans le helper : permet des formulations différentes par page ("clients" vs "éléments", "Aucun client trouvé" vs "Aucun produit ou service trouvé") sans dupliquer la logique de pagination elle-même.
- **Options de taille de page (5/10/25) écrites en dur dans le HTML** plutôt que centralisées dans une liste JS comme `PRODUCT_TYPES`/`CLIENT_STATUSES` : ce sont des valeurs universelles, non spécifiques à un domaine métier, qui n'ont pas vocation à évoluer par page — inutile de les faire transiter par `js/app.js`.
- **`.btn-secondary:disabled` ajouté de façon générique** (pas une classe dédiée à la pagination) : `.btn-secondary` est utilisé dans de nombreux contextes de l'application ; un état `:disabled` cohérent lui profite globalement sans risque, puisqu'aucun bouton existant n'utilisait l'attribut `disabled` avant cette version.
- **Travail réalisé sur la branche `v0.5.4-list-pagination`**, créée depuis `main` après validation et fusion de la V0.5.3 (+ correction V0.5.3.1) ; sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Paramètres entreprise & Module Devis — V0.6.1
- **Aucune persistance réelle malgré la nature "document" du module**, décision explicite du chef de projet confirmant l'analyse initiale : cohérent avec l'intégralité des versions précédentes (aucun `localStorage`, aucune sauvegarde définitive). "Enregistrer", "Créer une nouvelle version" restent en `btn-wip` direct ; "Supprimer" ouvre une modale de confirmation avant de déclencher le même comportement — tout le reste (sélection client, lignes, calculs, verrouillage, navigation entre versions) est en revanche pleinement fonctionnel en direct, démontré sur un jeu de données fictif pré-écrit plutôt que reconstruit dynamiquement.
- **`pages/facturation.html` réutilisée comme vraie liste de devis** plutôt qu'une page `devis.html` séparée, décision explicite du chef de projet : même principe que `clients.html`/`produits-services.html`, sidebar inchangée. Onglets `.page-tabs` ("Devis" actif, "Factures" en `btn-wip`) ajoutés pour rendre la structure explicitement compatible avec l'arrivée des factures en V0.6.2, sans construire cette page à l'avance.
- **`clientSlug` (référence vivante) ET `clientSnapshot` (copie figée par version)**, précision explicite du chef de projet qui amende ma proposition initiale (référence vivante seule) : un devis ancien, surtout envoyé ou accepté, ne doit jamais changer rétroactivement si la fiche client évolue plus tard. Le `<select>` client reste une vraie sélection en direct dans `CLIENT_DETAILS` (un nouveau choix reprend un snapshot à jour), mais une fois "figé" (version existante consultée), c'est le snapshot stocké qui s'affiche, jamais une relecture live de `CLIENT_DETAILS`. Même logique pour `companySnapshot` vis-à-vis de `COMPANY_SETTINGS`. Démontré concrètement : `DEV-2026-00015` v1 affiche l'ancienne adresse de Martin Dupont (différente de sa fiche client actuelle), v2/v3 affichent l'adresse actuelle.
- **Modalités de paiement du catalogue filtrées au statut Actif dans "Ajouter depuis le catalogue"**, en utilisant directement `STATUS_AVAILABILITY.selectionnable` (posé en V0.5.2, resté inutilisé jusqu'ici, noté au backlog comme "à réévaluer si un jour ces deux notions doivent pouvoir diverger") : cette version referme ce point du backlog en lui donnant enfin un usage réel, sans dupliquer la logique de disponibilité.
- **Remise en pourcentage uniquement**, décision explicite du chef de projet : le prix unitaire restant librement modifiable, une remise en montant fixe peut déjà être simulée en ajustant directement le prix ; une remise en montant fixe distincte reste possible plus tard si nécessaire.
- **Arrondi à 2 décimales appliqué ligne par ligne, puis sommé** (pas un arrondi global recalculé après sommation des valeurs brutes) : garantit que le total affiché correspond exactement à la somme des lignes affichées, évitant l'écart classique de double arrondi entre le détail et le total. `formatMoney` utilise systématiquement la virgule décimale et l'espace comme séparateur de milliers ("1 800,00 €"), plus précis que l'affichage catalogue existant ("1 800 € HT") car un devis est un document financier engageant.
- **Numérotation `DEV-AAAA-00001` à compteur annuel purement indicatif** (`computeNextDevisNumero`, balaie les numéros existants et propose le suivant) : aucune allocation réelle, cohérent avec l'absence de persistance ; le numéro affiché peut différer d'une session à l'autre selon les devis déjà présents dans les données fictives.
- **"Dupliquer" est une vraie action (navigation + pré-remplissage), contrairement à "Créer une nouvelle version" qui reste un simple `btn-wip`** : distinction voulue explicitement par le chef de projet (qui liste "Enregistrer / Supprimer / Créer une nouvelle version" comme actions Work in progress, mais pas "Dupliquer"). Techniquement peu coûteux : la duplication réutilise directement le même éditeur, avec un nouveau numéro/version 1/statut Brouillon et un nouveau snapshot client pris en direct.
- **Verrouillage strictement au sens du statut Accepté** (pas aux statuts Envoyé/Refusé) : une version non active reste elle aussi en lecture seule (l'historique ne se modifie jamais), mais avec un message distinct ("ancienne version, non active") plutôt que la bannière de verrouillage, pour ne pas laisser croire à tort qu'un devis Envoyé serait bloqué par une règle métier alors que c'est simplement une version historique.
- **Sélecteur de client en simple `<select>`** plutôt qu'une modale de sélection, décision explicite du chef de projet compte tenu du faible nombre de clients fictifs (6) — à revoir si le volume de clients augmente significativement dans une future version.
- **Travail réalisé sur la branche `v0.6.1-devis-company-settings`**, créée depuis `main` après validation et fusion de la V0.5.4 ; sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Module Factures & Paiements — V0.6.2
- **Statut affiché séparé en deux couches** : `statutEmission` explicite (`brouillon`/`emise`/`annulee`, stocké) et statut de paiement calculé (`non-payee`/`partiellement-payee`/`payee`/`en-retard`, jamais stocké) combinés par `COCKPIT_FACTURE_CALC.computeStatutAffiche`. Décision explicite du chef de projet reprenant la proposition de l'analyse : une facture Brouillon ne peut ainsi jamais afficher un statut de paiement incohérent, et le calcul de retard/paiement a un seul point d'entrée. Priorité assumée dans `computeStatutAffiche` : le retard (échéance dépassée avec un reste à payer positif) prime sur "partiellement payée" — une facture en retard reste "En retard" même si un acompte a déjà été versé.
- **Réutilisation directe de `COCKPIT_DEVIS_CALC`** (`roundMoney`/`formatMoney`/`computeLine`/`computeDevisTotals`/`snapshotClient`/`snapshotCompany`) plutôt que duplication : les lignes de facture ont exactement la même forme que les lignes de devis, aucune raison de réécrire les mêmes calculs et arrondis. `COCKPIT_FACTURE_CALC` n'ajoute que le spécifique aux factures (paiements, statut affiché, numérotation).
- **Numéro officiel réservé à l'émission**, décision explicite du chef de projet : un devis reçoit un numéro indicatif dès le brouillon, une facture n'en reçoit un réel (`FAC-AAAA-00001`) qu'au moment d'"Émettre la facture" — plus fidèle à la pratique réelle où un brouillon de facture n'a pas de numéro définitif. Affiché "Brouillon sans numéro" tant que non émise.
- **Échéance dérivée d'une correspondance exacte sur le libellé "Délai de paiement"** (`Paiement comptant`/`Paiement à réception` → 0 jour, `Paiement à 15 jours` → 15, `Paiement à 30 jours` → 30, tout le reste → 30 par défaut), plutôt qu'un parsing générique du texte libre : évite un parsing fragile tout en gardant une cohérence visible entre les conditions de paiement reprises du devis et la date d'échéance calculée à l'émission.
- **"Émettre la facture" et "Ajouter un paiement" sont des actions réelles en mémoire de page**, validé explicitement par le chef de projet (contrairement à "Enregistrer"/"Supprimer" qui restent en `btn-wip`) : l'émission attribue le numéro, la date d'émission, l'échéance, et verrouille lignes/client/snapshots/conditions de paiement ; un paiement recalcule immédiatement total payé/reste à payer/pourcentage payé/statut affiché. Rien ne persiste après rechargement, conformément au reste du projet.
- **Anti-surpaiement bloquant** : un paiement dont le montant dépasse le reste à payer (avec une tolérance de 0,001 € pour l'arrondi flottant) est refusé avec un message inline dans la modale, sans fermer la modale — décision explicite du chef de projet ("éviter le surpaiement... afficher une alerte ou bloquer l'ajout"). Les paiements existants ne sont pas modifiables/supprimables dans cette version (aucune demande en ce sens).
- **Annulation d'une facture émise volontairement non proposée** : décision explicite et prudente du chef de projet, les avoirs étant hors périmètre. Un statut `annulee` existe dans l'architecture et dans les données fictives (`FAC-2026-00005`, pour démonstration), mais aucune action ne permet d'y arriver depuis l'éditeur ; seul un brouillon dispose d'une action "Supprimer le brouillon" en `btn-wip`. Une note (`#facture-avoir-hint`) rappelle que ce sera traité par un futur module Avoirs, affichée uniquement sur les factures Émises (pas sur les Annulées, pour ne pas dupliquer l'information).
- **Conversion devis → facture strictement limitée aux devis Accepté** : `facture-edition.html?fromDevis=NUMERO&version=N` vérifie que la version existe et que son statut est `accepte`, sinon affiche l'état "introuvable" (même page que pour un lien cassé). La facture générée reprend telles quelles les snapshots déjà figés de cette version de devis (pas une nouvelle capture live) : ce qui a été accepté par le client ne doit pas changer au moment de la conversion. Un second devis Accepté sans facture (`DEV-2026-00016`, ajouté en V0.6.2) a été introduit dans les données fictives pour pouvoir démontrer une conversion réelle en plus de `DEV-2026-00015`, déjà lié à `FAC-2026-00003`.
- **Référence croisée devis ↔ facture bidirectionnelle**, calculée à l'affichage plutôt que stockée en double : la facture connaît son `devisRef` (numéro + version) et l'affiche en lien vers le devis source ; le devis recherche à l'inverse, parmi `FACTURE_DETAILS`, une facture dont le `devisRef` le désigne, pour afficher "Facture associée" et masquer le bouton "Convertir en facture" si c'est déjà fait — évite qu'un même devis Accepté génère plusieurs factures fictives par erreur de manipulation.
- **Liste des factures générée dynamiquement depuis `FACTURE_DETAILS`** (`js/app.js`), contrairement à la liste des devis dont les lignes sont écrites en dur dans `facturation.html` : le statut affiché d'une facture dépend d'un calcul (paiements + échéance), pas d'une simple valeur statique, donc le construire en JS à partir de la même source que l'éditeur évite tout risque d'incohérence entre le HTML et les données.
- **Deux onglets, deux panneaux, une seule page** : `pages/facturation.html` garde ses deux `<div>` (`#devis-panel`/`#factures-panel`) affichés/masqués par un simple toggle de classe/style, chacun avec sa propre recherche/filtre/pagination (`COCKPIT_LIST_PAGINATION` instancié deux fois, avec des ids distincts) — pas de rechargement de page ni de logique de routage.
- **Duplication assumée du code d'édition entre devis et facture** (recherche client, recherche catalogue, modale de description agrandie, rendu des conditions de paiement) : décision explicite du chef de projet pour limiter le risque de régression sur les devis déjà validés et avancer plus vite. La factorisation (sur le principe de `COCKPIT_LIST_PAGINATION`) est notée au backlog plutôt que faite maintenant.
- **Travail réalisé sur la branche `v0.6.2-factures-payments`**, créée depuis `main` après validation, merge, tag (`v0.6.1`) et push de la V0.6.1 ; sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.

## Refonte du tableau de bord — cockpit quotidien (V0.10.2)
- **"Montant décaissé" et "Reste à encaisser" exclus des séries du graphique de performance** : `TRESORERIE_OPERATIONS` (6 opérations fictives, quasi toutes datées de juin/juillet 2026) n'offre aucune profondeur historique fiable sur une fenêtre de 6 ou 12 mois — les proposer aurait signifié afficher un historique très majoritairement inventé. Seules trois séries réellement calculables mois par mois depuis `FACTURE_DETAILS` (dates d'émission et de paiement réelles) sont proposées : CA facturé, Encaissé, Solde net (facturé − encaissé).
- **Agrégations par période nouvelles mais non dupliquées** : aucun calculateur existant (`COCKPIT_FACTURATION_STATS.computeStats()`, `COCKPIT_ANALYSES_CALC.computeSnapshot()`) n'acceptait de filtre de date arbitraire (mois civil / 30 jours glissants / trimestre / période personnalisée). Plutôt que d'adapter ces fonctions partagées (risque de régression sur Facturation/Analyses), une agrégation dédiée au Dashboard a été écrite, qui réutilise `computeDevisTotals`/`roundMoney`/`formatMoney` pour chaque montant — la formule monétaire n'est jamais réécrite, seul le filtre de date est nouveau.
- **Objectif mensuel proratisé pour les périodes non mensuelles** : `DEMO_CONFIG.objectifMensuel` est une valeur mensuelle unique ; pour "30 derniers jours", "trimestre" ou une période personnalisée, l'objectif affiché est proratisé au nombre de jours réels de la période (`objectifMensuel × jours / 30,44`). Approximation assumée, documentée dans le code et le changelog, en l'absence d'une notion d'objectif par période dans le projet.
- **Horizon de trésorerie converti en nombre de jours avant l'appel à `computeSnapshot`** : cette fonction n'accepte qu'un nombre de jours depuis aujourd'hui, pas une date de fin explicite. Plutôt que de modifier sa signature (risque de régression sur `tresorerie.html`, qui l'appelle aussi), le Dashboard calcule en amont le nombre de jours jusqu'à l'horizon choisi (fin de mois, fin du mois suivant, 30/60/90 jours) et le lui transmet tel quel.
- **"Rendez-vous" du tunnel commercial = tous les rendez-vous de `RDV_DETAILS`**, tous statuts confondus, et non les seuls "réalisés" : ce chiffre représente le volume d'entrée du tunnel (l'activité commerciale générée), pas une étape de qualité. Conséquence assumée et documentée : certaines factures n'étant pas issues d'un devis (créées directement), le taux "Factures émises / Devis acceptés" peut dépasser 100 % — chiffre réel et volontairement non masqué, plutôt qu'un tunnel artificiellement forcé à décroître.
- **Alertes "Devis à relancer" (envoyé depuis ≥ 7 jours) et "Rendez-vous non confirmé imminent" (dans les 3 prochains jours)** : deux règles nouvelles, absentes du reste du projet, posées par nécessité pour le bloc Alertes du Dashboard. Seuils raisonnables mais arbitraires, documentés comme hypothèses assumées plutôt que présentés comme une règle métier validée par ailleurs.
- **Alertes de trésorerie reprises telles quelles depuis `COCKPIT_TRESORERIE_CALC.computeSnapshot(...).alertes`**, sans recalcul ni reformulation : évite de dupliquer la logique déjà validée en V0.8 (facture en retard, charge importante sous 7 jours, risque de tension de trésorerie, encaissement important attendu).
- **`js/pilotage-config.js` sans propagation réelle entre Paramètres et Dashboard** : ce sont deux pages chargées séparément, sans backend ni `localStorage` (explicitement exclu du périmètre V0.10.2). Plutôt que de simuler un enregistrement trompeur sur `settings.html`, la carte "Pilotage et activité" affiche honnêtement les valeurs par défaut réellement utilisées par le Dashboard, avec un bouton "Modifier" en Work in progress ; les vrais contrôles fonctionnels (sélecteur de période, changement d'horizon) vivent uniquement sur le Dashboard lui-même, en mémoire de page.
- **To-do list réellement fonctionnelle mais non persistante**, sur le même principe que les autres actions "réelles en mémoire" du projet (ajout d'opération de trésorerie, paiement de facture) : ajout/modification/suppression/complétion passent par un tableau JavaScript local et le moteur de modale générique `COCKPIT_MODAL`, sans nouveau module ni nouvelle bibliothèque.
- **Suppression complète de l'ancienne structure du tableau de bord** (CA du jour statique, ancienne to-do statique, anciennes alertes statiques dont "Stock faible", graphique CA statique, indicateurs favoris, blocs "Aperçu Facturation/Trésorerie/Analyses") plutôt qu'un ajout en parallèle : ces blocs dupliquaient déjà en grande partie ce que Facturation/Trésorerie/Analyses affichent nativement, objectif explicite de la V0.10.2.

## Centre de paramètres — V0.11 (validée, fusionnée sur `main`)
- **Architecture hybride avec un schéma central et des adaptateurs**, plutôt qu'une réécriture complète de `app.js`.
- **`localStorage` limité à la configuration** ; aucune donnée métier ni donnée sensible.
- **Import strict** : propriétés inconnues refusées, valeurs manquantes complétées par les valeurs par défaut.
- **Profil utilisateur, identité d'entreprise et profil de démonstration** restent trois concepts distincts.
- **Désactivation privilégiée à la suppression** pour les référentiels déjà utilisés.
- **Snapshots documentaires historiques immuables** ; application des réglages aux nouveaux documents uniquement.
- **Numérotation configurable explicitement indicative** tant qu'aucun backend n'alloue une séquence persistante.
- **Aucune simulation de mot de passe, 2FA, sessions, permissions, e-mail, push ou SMS.**
- Détail complet des correctifs de finalisation et des tests exécutés : voir `docs/archive/v0.11/delivery-report.md` (archivé après fusion).

## Assainissement global — V0.12
- **Régression pré-existante découverte et corrigée** : `js/settings-consumers.js` était tronqué en plein milieu d'une instruction depuis le commit `v0.11.8` (fichier syntaxiquement invalide, `SyntaxError: Unexpected end of input`), ce qui empêchait tout le socle de paramètres (`enhanceTreasury`, `applyAgenda`, `applyClients`, etc.) de s'exécuter et faisait déjà échouer 2 tests existants. Contenu restauré depuis le dernier commit valide (`580b6ff`), avec la correction de sanitisation d'URL de logo (`applyBrand`) qui avait été perdue dans la troncature.
- **`pages/database.html` supprimée** : orpheline confirmée (aucune page ne pointe vers elle), décision explicite du chef de projet qui annule le choix de conservation de la V0.2 (`docs/decisions.md`, V0.2).
- **`assets/` et `components/`** supprimés : dossiers vides (uniquement des `README.md` placeholders), jamais référencés par aucune page ni aucun style.
- **`docs/v0.11-*.md` et `V0.11-REVIEW.md`** archivés dans `docs/archive/v0.11/` : contenu soit déjà repris dans `changelog.md`/`backlog.md`/`roadmap-v0bis.md`, soit fusionné dans ce fichier (section V0.11 ci-dessus).
- **`js/settings-finalize.js` supprimé** : sa partie catalogue (16 entrées de statut) fusionnée dans le littéral `fields` de `settings-catalog.js` (3 clés en doublon résolues au profit du statut V1 le plus récent) ; sa partie DOM (`setPageSize`/`applyAnalyticsVisibility`) fusionnée dans `settings-consumers.js`, en réutilisant les helpers déjà existants (`emit`, `setSelect`) plutôt que de dupliquer des fonctions équivalentes.
- **Moteur d'alertes Dashboard unifié** : l'ancien bloc `computeDevisARelancer`/`computeRdvNonConfirmesImminents`/`renderAlerts` de `app.js` (V0.10.2) était systématiquement écrasé par `js/settings-alerts.js` (V0.11) car les deux écrivent dans les mêmes éléments DOM ; le bloc historique, mort en pratique, a été retiré. Le moteur trésorerie (`computeAlertes` dans `COCKPIT_TRESORERIE_CALC`) est conservé : il reste la source utilisée par `tresorerie.html`/`analyses.html`, et sert de repli légitime enrichi par `treasuryAlerts()` (`settings-consumers.js`) une fois ce fichier réparé.
- **Pagination sur reset** : `js/app.js` forçait `pageSizeSelect.value = '5'` lors d'un clic sur "Réinitialiser", ignorant la taille configurée. Le module générique `COCKPIT_LIST_PAGINATION.init` capture désormais la valeur affichée au chargement (déjà positionnée par `settings-consumers.js` selon la configuration) et la restaure au reset, au lieu d'une valeur en dur.
- **Filtre Agenda incomplet** : l'option `annule` manquait dans le `<select>` de filtre de `pages/agenda.html` (le statut existe et est géré correctement partout ailleurs — badges, compteurs, dashboard). Option ajoutée.
- **Date du tableau de bord rendue dynamique** : texte figé ("Jeudi 2 juillet 2026") remplacé par un calcul `new Date()` au chargement, réutilisant les tableaux `DAY_NAMES`/`MONTH_NAMES` déjà présents dans `app.js`.
- **Compteur de notifications factice retiré** : le badge `<span class="topbar-badge">3</span>` (bouton cloche, 12 pages) affichait une valeur fixe jamais calculée, à côté d'un bouton déjà `btn-wip`. Retiré plutôt que signalé, faute de source de données réelle à afficher à sa place (aucun module Notifications n'existe en V0).
- **Commande de tests ajoutée** (`npm test` → `tests/run-tests.js`) : exécute tous les `tests/*.test.js` séquentiellement via `child_process.spawnSync`, sans dépendance npm.
- **`app.js` non redécoupé** : conformément au périmètre V0.12, seuls les blocs identifiés comme morts ont été retirés (alertes Dashboard historiques). Une cartographie et une piste de découpage pour la V1 sont documentées dans `docs/architecture-app-js.md`.
- **Travail réalisé sur la branche `v0.12-audit-global`**, créée depuis `main` (V0.11.1) ; sans merge, tag ni push, conformément à la consigne de laisser ces décisions à une revue humaine ultérieure.
