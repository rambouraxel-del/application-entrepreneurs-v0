# Changelog

## Étape 0 — Imprégnation du projet
Lecture et compréhension des documents de référence (charte de conception, charte technique, cahier des charges V0, étapes de développement V0). Aucun fichier créé.

## Étape 1 — Socle technique
Création de l'arborescence du projet `Application-Entrepreneurs-V0/` : dossiers `css/`, `js/`, `pages/`, `components/`, `assets/`, `docs/`, fichiers de base (pages placeholder, `styles.css`, `app.js` vides), et documentation (README de chaque dossier, `PROJECT_INDEX.md`, `README.md` principal).

## Étape 2 — Page de connexion fictive
Création de la page de connexion fictive (`pages/login.html`) : nom provisoire "Cockpit Entrepreneur", phrase d'accueil, champs e-mail et mot de passe fictifs, bouton "Se connecter" et lien fictif "Mot de passe oublié ?". `index.html` redirige désormais automatiquement vers `pages/login.html`. Le bouton "Se connecter" redirige vers `pages/dashboard.html` (toujours un placeholder) via `js/app.js`. Ajout des styles correspondants dans `css/styles.css`.

## Étape 3 — Structure générale et navigation par onglets
Ajout d'une barre d'onglets supérieure commune (Tableau de bord, Clients, Agenda, Facturation, Base de données, Paramètres) sur les six pages principales (`pages/dashboard.html`, `pages/clients.html`, `pages/agenda.html`, `pages/facturation.html`, `pages/database.html`, `pages/settings.html`). Navigation par simples liens HTML, sans JavaScript. Onglet actif identifié par une classe CSS (`tab-active`) propre à chaque page. Ajout des styles correspondants (`.tabs`, `.tab`, `.tab-active`, `.page-content`) dans `css/styles.css`. Pages toujours sous forme de placeholders simples.

## Étape 4 — Squelette du tableau de bord
Structuration de `pages/dashboard.html` en sept zones : KPI principaux (4 emplacements), priorité du jour, agenda du jour, to-do list, alertes importantes, notifications, indicateurs favoris (4 emplacements). Disposition : KPI en haut, priorité en pleine largeur, agenda/to-do côte à côte, alertes/notifications côte à côte, indicateurs favoris en bas. Chaque zone ne contient qu'un titre et, pour les blocs non-KPI, un texte temporaire "Contenu à définir à l'étape suivante." Ajout des styles correspondants (`.dashboard-kpis`, `.dashboard-card`, `.dashboard-block`, `.dashboard-row`, `.dashboard-favorites`, `.dashboard-priority`) dans `css/styles.css`. Aucune donnée fictive détaillée, aucune couleur d'état.

## Étape 5 — Enrichissement visuel du tableau de bord
Ajout de données fictives réalistes dans les sept zones de `pages/dashboard.html` : valeurs et variations pour les 4 KPI, priorité du jour ("Relancer le devis Martin"), 3 rendez-vous fictifs pour l'agenda, 4 tâches fictives pour la to-do list (2 marquées terminées visuellement), 4 alertes fictives avec niveaux de gravité, un bloc notifications fictif, et des valeurs pour les 4 indicateurs favoris. Ajout des classes de statut (`status-positive`, `status-negative`, `status-neutral`, `alert-critical`, `alert-warning`, `alert-info`) et des styles associés (valeurs KPI, listes, badges) dans `css/styles.css`. Structure et disposition de l'étape 4 conservées sans modification.

## Étape 6 — Pages secondaires fictives
Enrichissement des cinq pages secondaires avec du contenu fictif crédible :
- `pages/clients.html` : en-tête avec bouton fictif "Ajouter un client", barre de recherche fictive, tableau de 4 clients fictifs (nom, entreprise, téléphone, e-mail en `@example.com`, dernier contact, statut en badge).
- `pages/agenda.html` : en-tête avec bouton fictif "Ajouter un rendez-vous", liste de 4 rendez-vous fictifs de la journée (réutilise le style `.agenda-list` du tableau de bord).
- `pages/facturation.html` : en-tête avec boutons fictifs "Créer une facture" et "Créer un devis", tableau de 5 documents fictifs (devis/factures) avec statuts en badges colorés.
- `pages/database.html` : 6 cartes représentant les bases centrales (Clients, Produits/Services, Factures, Devis, Rendez-vous, Tâches) avec un nombre fictif d'éléments chacune.
- `pages/settings.html` : 6 sections fictives (Informations de l'entreprise, Préférences d'affichage, Notifications, Compte utilisateur, Objectifs mensuels, Sécurité), chacune avec un bouton fictif "Modifier".

Tous les boutons fictifs de cette étape portent la classe commune `btn-wip`, sans aucune action réelle (ni lien, ni JavaScript), afin d'être reliés au pop-up "Work in progress" à l'étape 7. Ajout des styles correspondants (`.page-header`, `.btn-secondary`, `.search-bar`, `.data-table`, `.badge` et ses variantes, `.database-grid`, `.settings-grid`) dans `css/styles.css`. Aucune fonctionnalité réelle développée.

## Étape 7 — Pop-up "Work in progress"
Ajout d'un pop-up centralisé "Work in progress" (titre + message "Cette fonctionnalité sera développée dans une prochaine version." + bouton "Fermer"), généré dynamiquement en JavaScript dans `js/app.js` (aucune duplication de structure HTML dans les pages). Un seul écouteur d'événement global détecte les clics sur tout élément portant la classe `btn-wip` et ouvre le pop-up ; fermeture possible via le bouton "Fermer", un clic sur le fond assombri, ou la touche Échap. Le lien "Mot de passe oublié ?" de `pages/login.html` a reçu la classe `btn-wip` et est désormais relié au même comportement (son `href="#"` est neutralisé par `preventDefault()`). Ajout de la balise `<script src="../js/app.js">` sur `pages/clients.html`, `pages/agenda.html`, `pages/facturation.html` et `pages/settings.html` (pages contenant des éléments `btn-wip`) ; `pages/dashboard.html` et `pages/database.html` non modifiées car elles ne contiennent aucun élément `btn-wip`. Ajout des styles du pop-up (`.wip-overlay`, `.wip-modal`, `.wip-title`, `.wip-message`, `.wip-close`) dans `css/styles.css`. Le bouton "Se connecter" et les onglets de navigation restent inchangés et fonctionnent normalement.

## Étape 8 — Harmonisation visuelle générale
Audit complet de `css/styles.css` et des 7 pages, puis corrections ciblées sans changement de structure ni de données :
- Correction d'un bug de spécificité CSS : `.page-subtitle` était silencieusement écrasé par la règle `.page-content p` (15px au lieu des 14px prévus) ; la sélection a été renforcée (`.page-content .page-subtitle`) pour que la taille voulue s'applique réellement.
- Harmonisation de `.btn-primary` et `.btn-secondary` : même taille de police (14px) et même gabarit de padding (`10px 20px`), tout en conservant une distinction visuelle claire (fond bleu plein + `font-weight: 600` pour le bouton principal, contour bleu + `font-weight: 500` pour le secondaire).
- Ajout de `font-weight: 600` explicite à `.dashboard-block h2`, pour que les titres de blocs (Agenda, To-do list, Alertes, Notifications, Priorité du jour, sections Paramètres) aient le même poids visuel que les titres de cartes KPI/favoris/base de données.
- Suppression de la duplication de la règle de largeur du tableau de bord : `.page-content.dashboard` et `.page-content.wide` définissaient la même valeur (1100px) ; seule `.wide` porte désormais cette règle. `pages/dashboard.html` utilise maintenant `class="page-content dashboard wide"` — la classe `dashboard` est conservée comme repère de contexte pour d'éventuels styles futurs, sans porter elle-même de largeur.
- Ajout d'un état `:focus` cohérent sur le champ de recherche fictif (`.search-bar input`), identique à celui des champs de connexion.
- Harmonisation de l'espacement de la barre de recherche (`margin-bottom` 20px → 24px) pour respecter le rythme vertical de 24px utilisé partout ailleurs.
- Unification de l'ombre des deux "panneaux proéminents" de l'application (`.login-card` et `.wip-modal`) sur une seule valeur partagée.

Aucune fonctionnalité, donnée fictive ou structure de page modifiée. Bouton "Se connecter", onglets de navigation, disposition du tableau de bord et comportement du pop-up "Work in progress" vérifiés après coup et toujours fonctionnels.

## Étape 9 — Tests de navigation et corrections
Vérification systématique de l'ensemble du prototype (`index.html` et les 7 pages) par relecture de code et scripts de contrôle automatisés, en l'absence d'accès à un navigateur réel dans cet environnement :
- Tous les liens `href`/`src` (feuille de style, script, onglets, redirection) résolvent vers un fichier existant — aucun lien cassé.
- Les 6 pages principales partagent exactement les mêmes 6 onglets, dans le même ordre, avec la classe `tab-active` correctement positionnée sur l'onglet de la page courante.
- Les 11 éléments `btn-wip` attendus sont bien présents et répartis comme prévu (Connexion : 1, Clients : 1, Agenda : 1, Facturation : 2, Paramètres : 6 ; Tableau de bord et Base de données : 0, cohérent avec l'absence de bouton fictif sur ces pages).
- `js/app.js` est chargé exactement sur les 5 pages qui en ont besoin (Connexion, Clients, Agenda, Facturation, Paramètres) et absent des 2 pages qui n'en ont pas besoin (Tableau de bord, Base de données).
- Relecture de la logique JavaScript du pop-up : pas de conflit entre le bouton "Se connecter", les onglets et le système `btn-wip` ; le bouton "Fermer" du pop-up ne porte pas la classe `btn-wip` (pas de réouverture accidentelle) ; la fermeture par clic extérieur ne déclenche pas non plus le système `btn-wip`.
- Aucun identifiant HTML (`id`) dupliqué au sein d'une même page.
- Aucun texte temporaire ("Page en préparation", "Contenu à définir...") oublié sur une page censée être enrichie.

**Aucune correction n'a été nécessaire** : aucun problème réel n'a été détecté sur les points vérifiés. Quelques classes CSS "de contexte" sans style propre (`dashboard-agenda`, `dashboard-alerts`, `dashboard-notifications`, `dashboard-todo`, `database-card`) ont été relevées ; elles n'ont aucun impact visuel (le style vient de `.dashboard-block`/`.dashboard-card`) et n'ont donc pas été modifiées.

Points nécessitant une confirmation visuelle par le chef de projet dans un navigateur réel : rendu effectif du parcours complet, ouverture/fermeture du pop-up (bouton, clic extérieur, touche Échap), absence de débordement visuel dans les tableaux avec les e-mails fictifs les plus longs.

## Étape 10 — Livraison propre de la V0
Finalisation de la documentation du projet, sans aucune modification du prototype (HTML/CSS/JS) :
- `README.md` (racine) entièrement réécrit pour refléter l'état final livré : résumé complet de ce qui a été développé, instructions d'ouverture et de test, limites de la V0, suggestions d'amélioration non appliquées et points à prévoir pour la V1, ces deux dernières sections étant clairement séparées du contenu réellement livré.
- `PROJECT_INDEX.md` mis à jour avec l'arborescence finale annotée et une note explicative sur les dossiers `components/` et `assets/`, restés vides dans cette V0 statique.
- `pages/README.md` corrigé : il décrivait encore les pages comme de simples placeholders "à développer" alors qu'elles sont enrichies depuis les étapes 2 à 6 — incohérence manifeste, corrigée.
- `js/README.md` corrigé : il présentait la navigation et le pop-up comme des logiques "à venir" alors qu'elles sont implémentées depuis les étapes 2 et 7 — incohérence manifeste, corrigée.
- `docs/decisions.md` non modifié : aucune nouvelle décision de conception n'a été prise à cette étape, uniquement de la documentation.

La V0 est considérée comme livrée à l'issue de cette étape.

## V0.1 — Refonte graphique globale
Refonte esthétique et ergonomique complète de l'interface, sans nouvelle fonctionnalité métier, sans suppression de donnée fictive, à partir d'une maquette de référence (sidebar sombre + tableau de bord moderne façon SaaS) :

- **Sauvegarde préalable** : copie intégrale du projet dans `backup-v0-before-v0-1-refonte-graphique/` (dossier frère), avant toute modification.
- **`css/styles.css`** entièrement réécrit : introduction de variables CSS (`:root`) pour centraliser couleurs/ombres/rayons, remplacement du bleu unique par une palette indigo/violet à plusieurs teintes pour les accents décoratifs, nouveau système de mise en page `.app-shell`/`.sidebar`/`.main`/`.topbar` remplaçant l'ancienne barre d'onglets supérieure (`.tabs`/`.tab`/`.tab-active`, supprimées), cartes KPI avec icônes, alertes/agenda restylés en blocs colorés, ajout de media queries pour tablette et mobile.
- **`pages/dashboard.html`** (priorité de la refonte) : nouvelle sidebar (5 liens + icônes SVG), nouvelle topbar (message de bienvenue fictif "Bonjour, Julien !", recherche, cloche de notifications, roue crantée), cartes KPI avec icônes et valeurs plus visibles, blocs Agenda/To-do/Alertes redessinés, ajout d'un graphique fictif statique "Évolution du CA" (SVG dessiné à la main, aucune donnée réelle ni calcul), résumé mensuel (anciens indicateurs favoris) restylé avec icônes.
- **`pages/clients.html`, `agenda.html`, `facturation.html`, `database.html`, `settings.html`** : même sidebar et topbar appliquées, contenu fonctionnel et données fictives strictement conservés, boutons et tableaux restylés selon la nouvelle charte.
- **Paramètres** retiré des liens principaux de la sidebar et déplacé en icône transversale (roue crantée) dans la topbar de toutes les pages ; la page `settings.html` reste pleinement accessible et met la roue crantée en surbrillance pour indiquer la position actuelle.
- **`pages/login.html`** : non restructurée (hors périmètre de la maquette, qui concerne l'espace post-connexion) ; bénéficie uniquement de la nouvelle palette indigo via les variables CSS partagées.
- **`js/app.js`** : **aucune modification nécessaire** (vérifié par comparaison avec la sauvegarde) — la délégation d'événements sur `.btn-wip` fonctionne à l'identique avec la nouvelle structure HTML.
- Aucune entrée de sidebar supplémentaire (Trésorerie, Produits/Services, Finance, Statistiques) n'a été ajoutée : ces modules sont explicitement hors périmètre de la V0.1.

Vérifications effectuées après la refonte (par relecture de code, sans navigateur disponible dans cet environnement) : tous les liens/chemins résolvent, la sidebar affiche le bon lien actif sur chacune des 5 pages principales, aucun lien actif sur `settings.html` (conforme à la décision de la retirer de la navigation principale), 19 éléments `btn-wip` répartis de façon cohérente sur les 7 pages, aucun identifiant HTML dupliqué, CSS syntaxiquement valide.

## V0.1.1 — Corrections ciblées du tableau de bord
Passe corrective sur `pages/dashboard.html` et `css/styles.css` uniquement, pour rapprocher le rendu de la maquette de référence, sans nouvelle fonctionnalité ni donnée fictive supprimée :

- **Suppression du widget "Notifications"** du tableau de bord ; l'information reste portée uniquement par la cloche + badge rouge de la topbar (déjà en place depuis la V0.1).
- **Fusion de "Priorité du jour" dans la To-do list** : "Relancer le devis Martin" devient le premier élément de la to-do list, mis en avant par un fond teinté et un badge "Priorité du jour", au lieu d'un bloc plein-large séparé qui déséquilibrait la grille.
- **Nouvelle disposition à 3 colonnes** (`dashboard-row-3`) : Agenda du jour / To-do list / Alertes importantes sur une seule rangée, remplaçant les deux anciennes rangées de 2. Dégradation responsive en 2 colonnes (≤1100px) puis 1 colonne (≤900px, règle déjà existante).
- **Agenda du jour** : chaque rendez-vous devient un bloc teinté avec bordure colorée à gauche (3 teintes tournantes indigo/violet/bleu, aucune couleur réservée aux statuts).
- **To-do list** : vraies cases à cocher visuelles (non interactives), badges de priorité réutilisant les badges existants (`badge-danger`/`badge-warning`/`badge-neutral`/`badge-success`), séparation par espacement/fond au survol plutôt que par lignes.
- **Alertes importantes** : chaque alerte devient une carte avec icône ronde colorée selon le type (horloge/triangle/calendrier), titre, sous-texte (le sous-texte de l'alerte "facture en retard" reprend désormais le montant réel de FAC-001 déjà affiché sur la page Facturation, pour plus de cohérence inter-pages) et chevron.
- **Sélecteur de date fictif** ajouté dans la topbar du tableau de bord uniquement (relié au pop-up "Work in progress", comme les autres éléments non fonctionnels) — non étendu aux autres pages à ce stade, décision explicitement validée pour rester dans un périmètre correctif ciblé.
- **Sidebar** : léger affinage (logo légèrement agrandi, séparateur discret sous le bloc marque, espacement des liens un peu plus généreux), sans changement d'identité.
- **Nettoyage** : suppression des règles CSS devenues orphelines (`.dashboard-notifications`, `.dashboard-priority`, `.priority-text`, `.notifications-count`).
- **`js/app.js`** : aucune modification.

Vérifications effectuées : liens/chemins valides, aucun identifiant dupliqué, CSS syntaxiquement valide (155 accolades ouvrantes/fermantes équilibrées), 19 éléments `btn-wip` correctement répartis (dashboard passe de 3 à 4 avec l'ajout du sélecteur de date). Test visuel réel dans Chrome tenté mais bloqué par la politique de navigation de l'extension (accès `file://` et `localhost` tous deux refusés) — un serveur HTTP local de secours (PowerShell, `http://localhost:8834/`) a été mis en place pour permettre au chef de projet de vérifier lui-même le rendu sans workaround supplémentaire côté agent.

**V0.1.1 validée par le chef de projet.** Points restants non bloquants, reportés au backlog (`docs/backlog.md`) : suppression/refonte de la page Base de données, harmonisation du sélecteur de date sur toutes les pages, amélioration plus poussée du widget Agenda, évolution fonctionnelle des modules, travail futur sur l'identité/logo.

## V0.1.2 — Structuration technique et documentation projet
Étape purement technique et documentaire, sans aucune modification du prototype (HTML/CSS/JS inchangés) :

- **Initialisation d'un dépôt Git local** à la racine de `Application-Entrepreneurs-V0/` (le projet n'était pas versionné jusque-là). Identité configurée localement au dépôt : Axel Rambour / rambour.axel@gmail.com.
- **Premier commit** (`v0.1.1 - refonte graphique stabilisée`) figeant l'état validé de la V0.1.1, puis **tag `v0.1.1`** sur ce commit.
- **`.gitignore`** créé : exclut les dossiers de sauvegarde (`backup-*/`), les fichiers système, les fichiers temporaires, les futurs fichiers d'environnement et dépendances.
- **`docs/roadmap-v0bis.md`** créé : phases V0.1 à V1, avec leur statut (validée / en cours / à venir).
- **`docs/backlog.md`** créé : idées et pistes non traitées, classées par thème, reprenant notamment les points laissés en suspens à la validation de la V0.1.1.
- **`docs/versioning.md`** créé : convention de nommage des versions (V0.x / V0.x.x), logique de commit/tag/branche, distinction version validée / en chantier.
- **Second commit** (`v0.1.2 - structuration technique et documentation projet`) regroupant ces nouveaux documents et les mises à jour de `README.md`, `docs/changelog.md` et `docs/decisions.md`.
- **Dépôt GitHub distant** : non créé à ce stade (`gh` non installé sur la machine, et aucune tentative de création automatique n'a été faite). Procédure manuelle documentée pour le chef de projet.

Git/GitHub deviennent à partir de cette étape la méthode de suivi recommandée pour le projet, en remplacement des sauvegardes manuelles de dossier utilisées jusqu'ici.

**V0.1.2 validée par le chef de projet.** Dépôt GitHub privé créé et `main` poussé avec le tag `v0.1.1` par le chef de projet lui-même.

## V0.2 — Navigation et architecture générale
Réalisée sur la branche dédiée `v0.2-navigation-architecture` (créée depuis `main`, non fusionnée à ce stade). Objectif : clarifier l'organisation de la navigation principale, sans développement fonctionnel :

- **"Base de données" retirée de la sidebar principale** sur les 6 pages qui la contenaient (`dashboard.html`, `clients.html`, `agenda.html`, `facturation.html`, `database.html`, `settings.html`). Le fichier `pages/database.html` n'est pas supprimé : il conserve sa sidebar/topbar pour rester cohérent si on y accède directement, mais devient orpheline (plus aucune page ne pointe vers elle), à l'image du traitement déjà appliqué à `settings.html` depuis la V0.1.
- **Trois nouveaux modules ajoutés à la sidebar**, dans l'ordre : Tableau de bord, Clients, Agenda, Facturation, **Trésorerie**, **Produits / Services**, **Finance** (7 liens au total).
- **`pages/tresorerie.html`** créée : 4 cartes KPI (Solde disponible, Dépenses du mois, Recettes du mois, Échéances à venir) + bannière "Module en construction".
- **`pages/produits-services.html`** créée : bouton fictif "Ajouter un produit" (`btn-wip`), tableau fictif de 4 produits/services (nom, type, prix, marge estimée, ventes) + bannière "Module en construction".
- **`pages/finance.html`** créée : 4 cartes KPI (Chiffre d'affaires, Marge, Objectif mensuel, Résultat estimé), 3 zones fictives (Bilan prévisionnel, Compte de résultat, Objectifs financiers) + bannière "Module en construction".
- **Nouveau composant CSS `.construction-banner`** (bordure en pointillés, icône, texte) réutilisé sur les 3 nouvelles pages ; **`.placeholder-grid`** ajouté pour la grille à 3 zones de la page Finance.
- **Paramètres et notifications inchangés** : roue crantée et cloche toujours présentes en topbar sur toutes les pages, y compris les 3 nouvelles.
- **Sélecteur de date** non étendu : reste réservé au tableau de bord, conformément à la décision de la V0.1.1.
- **Statistiques / Pilotage** volontairement non ajouté à la sidebar — reste au backlog.
- **`js/app.js`** : aucune modification (la délégation `.btn-wip` fonctionne automatiquement sur les nouvelles pages).
- **Roadmap clarifiée** : une note dans `docs/roadmap-v0bis.md` précise que Trésorerie/Produits-Services/Finance ne sont que des portes d'entrée en V0.2 ; leur développement fonctionnel réel reste prévu en V0.7/V0.8/V0.9.

Vérifications effectuées : tous les liens/chemins résolvent (aucun lien mort), les 9 pages avec sidebar (dashboard, clients, agenda, facturation, database, settings, tresorerie, produits-services, finance) affichent exactement 7 liens chacune, l'onglet actif est correct sur les 7 pages concernées et absent sur `database.html`/`settings.html` (cohérent avec leur statut hors navigation principale), aucun identifiant HTML dupliqué, CSS syntaxiquement valide (162 accolades ouvrantes/fermantes équilibrées), aucun ancien lien vers `database.html` restant.

Commit créé sur la branche `v0.2-navigation-architecture` : `v0.2 - navigation et architecture générale`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

**V0.2 validée par le chef de projet.** Fusionnée dans `main` (fast-forward), taguée `v0.2`, poussée sur GitHub avec le tag.

## V0.3 — Tableau de bord : hub de navigation et interactions de base
Réalisée sur la branche dédiée `v0.3-dashboard-hub` (créée depuis `main`, non fusionnée à ce stade). Objectif : connecter le tableau de bord déjà existant aux modules posés en V0.2, sans nouvelle fonctionnalité métier. Seul `pages/dashboard.html` (contenu) et `css/styles.css` (styles additifs) ont été modifiés :

- **4 KPI principaux rendus cliquables** : CA du jour → `finance.html`, CA du mois → `finance.html`, Objectif mensuel → `finance.html`, Trésorerie disponible → `tresorerie.html`.
- **4 alertes importantes rendues cliquables** : Facture en retard → `facturation.html`, Stock faible → `produits-services.html`, Objectif mensuel en retard → `finance.html`, Rendez-vous important → `agenda.html`.
- **3 cartes du résumé mensuel rendues cliquables** : Clients ce mois-ci → `clients.html`, Devis envoyés → `facturation.html`, Dépenses du mois → `tresorerie.html`. "Tâches terminées" reste non cliquable (aucun module Tâches dédié).
- **2 tâches de la to-do list rendues cliquables** : "Relancer le devis Martin" (priorité du jour) → `clients.html`, "Préparer la facture n°001" → `facturation.html`. "Appeler le fournisseur Dupont" reste non cliquable (aucun module Fournisseurs), les 2 tâches déjà cochées restent non cliquables (actions passées).
- **"Voir l'agenda"** vérifié : pointait déjà vers `agenda.html` depuis la V0.1.1, aucun changement nécessaire.
- **Nouveau bouton "Personnaliser les indicateurs"** ajouté au-dessus de la grille des 4 KPI principaux, en Work in progress (`btn-wip`) — prépare l'idée sans développer la fonctionnalité.
- **Indications visuelles de clic ajoutées**, additives uniquement (aucune modification des règles de base `.dashboard-card`, `.kpi-card`, `.alert-item`) : `.kpi-card-link` (chevron discret en survol via `::after`, légère ombre), `.alert-item` (assombrissement léger au survol), `.todo-item-link` (soulignement du titre au survol). Curseur pointer sur tous les éléments cliquables.
- **Design V0.1.1 non modifié** : aucune structure, couleur, sidebar, topbar ou espacement changé — uniquement des classes additives et des balises `<div>`/`<li>` transformées en `<a>` sans changement de mise en page.
- **"Base de données" non réintroduite** : vérifié, aucune mention ni lien vers `database.html` sur le tableau de bord.
- **`js/app.js`** : aucune modification (les nouveaux liens réels ne nécessitent aucun JavaScript ; la délégation `.btn-wip` reste inchangée pour les éléments encore en Work in progress).

Vérifications effectuées : tous les liens/chemins du projet résolvent (aucun lien mort, toutes pages confondues), CSS syntaxiquement valide (170 accolades équilibrées), balises HTML équilibrées sur `dashboard.html` (div, a, li, ul, section), aucun identifiant dupliqué, exactement 3 éléments `href="#"` restants sur le tableau de bord — tous portant `btn-wip` ("Personnaliser les indicateurs", "Ajouter une tâche", "Voir toutes") —, aucun des nouveaux liens réels ne porte la classe `btn-wip`.

Commit créé sur la branche `v0.3-dashboard-hub` : `v0.3 - tableau de bord hub de navigation`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

**V0.3 validée par le chef de projet.** Fusionnée dans `main` (fast-forward), taguée `v0.3`, poussée sur GitHub avec le tag.

## V0.4.1 — Liste clients CRM & accès fiche client
Réalisée sur la branche dédiée `v0.4.1-clients-crm-entry` (créée depuis `main`, non fusionnée à ce stade). Première sous-version de la V0.4 (Clients / fiche client / socle CRM), centrée sur la page `pages/clients.html` et la création de `pages/fiche-client.html`.

- **Recherche fonctionnelle** ajoutée à `clients.html` : filtre en direct sur nom, entreprise, téléphone et e-mail (JavaScript, sur le DOM déjà rendu — aucune base de données).
- **Filtre par statut fonctionnel** : liste déroulante peuplée dynamiquement depuis une source unique (`CLIENT_STATUSES` dans `js/app.js`), combinable avec la recherche (logique ET). Bouton "Réinitialiser" qui vide les deux filtres.
- **Compteur dynamique** ("X clients affichés sur Y") mis à jour à chaque changement de filtre.
- **Colonne Actions** ajoutée en fin de tableau, avec un unique bouton "Voir la fiche" par ligne (plutôt que plusieurs boutons Modifier/Détails/Historique), pointant vers `fiche-client.html?client=<slug>`.
- **6 statuts par défaut introduits** : Prospect, Client actif, À relancer, Inactif, Fidèle, Litige — mappés aux badges existants (`badge-neutral`, `badge-success`, `badge-warning`, `badge-neutral`, `badge-info`, `badge-danger`). Liste explicitement documentée comme non définitive, en préparation de la personnalisation prévue en V0.4.3.
- **Jeu de données élargi à 6 clients fictifs** (au lieu de 4) pour couvrir chacun des 6 statuts et permettre de tester réellement le filtre.
- **Nouvelle page `pages/fiche-client.html`** : fil d'Ariane "Clients › Fiche client", bouton retour vers la liste, carte d'identité (nom, entreprise, téléphone, e-mail, date factices, badge de statut), bandeau `.construction-banner` annonçant l'enrichissement prévu en V0.4.2. Le nom affiché et les initiales de l'avatar sont dérivés du paramètre d'URL `?client=` (ex. `atelier-leroy` → "Atelier Leroy" / "AL") ; le reste du contenu demeure générique et identique quel que soit le client.
- **`css/styles.css`** : ajouts additifs uniquement (`.clients-filters`, `.clients-status-select`, `.clients-counter`, `.btn-table-action`, `.breadcrumb`, `.fiche-back-btn`, `.client-identity-card` et ses sous-éléments) — aucune règle existante modifiée.
- **`js/app.js`** : deux nouveaux blocs IIFE, scopés par vérification de présence des éléments cibles (même pattern que la logique existante), sans impact sur les autres pages.

Vérifications effectuées : équilibre des balises HTML (div/tr/td) sur les deux pages concernées, 185 accolades CSS ouvrantes/fermantes équilibrées, tous les liens/chemins résolvent (y compris ceux avec paramètre d'URL, vérifiés après retrait du query string), aucun identifiant HTML dupliqué. **Test fonctionnel réel effectué dans un navigateur** (serveur statique local PowerShell servi via l'outil de prévisualisation) : recherche testée (filtrage correct), filtre par statut testé seul et combiné à la recherche (logique ET vérifiée, y compris le cas 0 résultat), bouton "Réinitialiser" testé (recherche et filtre vidés, compteur revenu à 6/6), navigation "Voir la fiche" → `fiche-client.html?client=atelier-leroy` testée (nom "Atelier Leroy" et initiales "AL" correctement affichés), bouton "Retour à la liste" testé (retour effectif vers `clients.html`), page `fiche-client.html` testée sans paramètre (repli correct sur "Client sélectionné" / "?"), aucune régression constatée sur le tableau de bord (pop-up "Work in progress" toujours fonctionnel), aucune requête réseau en échec. La capture d'écran de l'outil de prévisualisation a systématiquement expiré (timeout technique de l'outil, sans lien avec le code) ; la vérification visuelle s'est appuyée sur l'inspection DOM/CSS (`preview_inspect`) et l'arborescence d'accessibilité (`preview_snapshot`), plus fiables et suffisantes pour confirmer la mise en page.

Commit créé sur la branche `v0.4.1-clients-crm-entry` : `v0.4.1 - liste clients crm et acces fiche client`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

**V0.4.1 validée par le chef de projet.** Fusionnée dans `main` (fast-forward), taguée `v0.4.1`, poussée sur GitHub avec le tag.

## V0.4.2 — Fiche client CRM complète
Réalisée sur la branche dédiée `v0.4.2-client-profile-crm` (créée depuis `main`, non fusionnée à ce stade). Deuxième sous-version de la V0.4, elle transforme `pages/fiche-client.html` (générique en V0.4.1) en une vraie fiche CRM, avec des données cohérentes par client.

- **`CLIENT_DETAILS`** : nouvelle source statique dans `js/app.js`, une entrée par slug déjà présent dans `clients.html` (`martin-dupont`, `sophie-bernard`, `atelier-leroy`, `boucherie-morel`, `julien-petit`, `techni-bois-sarl`). Remplace entièrement l'ancienne logique V0.4.1 qui déduisait un nom générique à partir du slug.
- **Cohérence garantie avec `clients.html`** : nom, entreprise, téléphone, e-mail, statut et dernier contact identiques entre la liste et la fiche pour chacun des 6 clients.
- **État "Client introuvable"** propre et non bloquant si le paramètre `?client=` est absent ou ne correspond à aucune entrée de `CLIENT_DETAILS`.
- **En-tête client complet** : avatar (initiales), nom, badge de statut (réutilise le mapping `CLIENT_STATUSES`, désormais exposé via `window.COCKPIT_CLIENT_STATUSES`), entreprise, téléphone, e-mail, adresse, date "Client depuis", date de dernier contact, boutons "Retour à la liste" (réel), "Modifier"/"Ajouter une note"/"…" (Work in progress).
- **5 KPI client** : chiffre d'affaires généré, relances en cours, rendez-vous à venir, documents récents, gestes commerciaux (réutilisent `.kpi-card`/`.dashboard-kpis`, nouvelle classe `.client-kpis` pour la grille à 5 colonnes).
- **Bloc Informations client** : 10 champs (statut juridique, SIRET, secteur, site web, commercial référent, conditions de paiement, mode de règlement, catégorie client, source d'acquisition, TVA) + lien "Personnaliser les champs" (Work in progress).
- **Bloc-notes commercial** : 3 dernières notes par client, actions "Voir toutes les notes"/"Nouvelle note" (Work in progress).
- **Historique des échanges** : jusqu'à 5 derniers événements par client (appel sortant, e-mail envoyé/reçu, rendez-vous réalisé, relance, commentaire interne), action "Voir tout l'historique" (Work in progress).
- **Rendez-vous liés** : jusqu'à 3 rendez-vous par client, message dédié si aucun rendez-vous à venir, lien réel "Voir dans l'agenda" → `agenda.html` (générique, non filtré par client).
- **Documents & facturation** : jusqu'à 3 documents par client (devis, facture, contrat) dans un tableau réutilisant `.data-table`/`.badge`, bouton "Télécharger PDF" par ligne (Work in progress), lien réel "Voir dans Facturation" → `facturation.html` (générique, non filtré par client).
- **Réutilisation maximale des composants existants** : `.dashboard-block`/`.dashboard-block-header`/`.dashboard-block-link`/`.dashboard-row` (déjà utilisés sur le tableau de bord) pour tous les blocs à carte, `.data-table`/`.badge` pour le tableau de documents — aucune refonte de composant, uniquement des classes additives pour les éléments sans équivalent (en-tête étendu, cartes de notes, items d'historique/rendez-vous, état "introuvable").
- **`css/styles.css`** : ajouts additifs uniquement, aucune règle existante modifiée en profondeur. La grille `.client-kpis` (5 colonnes) hérite du comportement responsive déjà en place sur `.dashboard-kpis` (2 colonnes sous 1100px, 1 colonne sous 760px).
- **Sidebar et topbar inchangées**, conformément au périmètre : aucune refonte, aucun nouvel élément hors périmètre (pas de bloc "Plan Professionnel", pas de vrai menu déroulant pour "…").

Vérifications effectuées : équilibre des balises HTML (div/section/table/tr/thead/tbody) et des accolades CSS (218 équilibrées), aucun identifiant dupliqué, tous les liens résolvent. **Test fonctionnel réel dans un navigateur** pour l'ensemble des 6 clients : identité/KPI/informations/notes/historique cohérents et différenciés par client vérifiés en détail sur `martin-dupont` (JSON complet comparé aux données de `clients.html`) et `techni-bois-sarl` (snapshot d'accessibilité) ; cas particuliers testés — `atelier-leroy` (aucun rendez-vous à venir → message dédié affiché), slug inconnu et absence de paramètre (état "Client introuvable" affiché, contenu masqué, page non cassée dans les deux cas). Liens réels "Voir dans l'agenda" et "Voir dans Facturation" vérifiés par déclenchement direct (navigation confirmée vers les bonnes pages). Grille des 5 KPI vérifiée à largeur de bureau (1400px, 5 colonnes) et confirmée cohérente avec le comportement responsive hérité en dessous de 1100px. Non-régression vérifiée sur `clients.html` (recherche, filtre, compteur, statuts inchangés) et `dashboard.html` (pop-up Work in progress toujours fonctionnel, aucune erreur console, aucune requête réseau en échec liée aux pages modifiées).

Commit créé sur la branche `v0.4.2-client-profile-crm` : `v0.4.2 - fiche client crm complete`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.4.2.1 — Ajustements UX fiche client
Correction UX ciblée, réalisée sur la même branche `v0.4.2-client-profile-crm` (pas de nouvelle branche), en commit supplémentaire au-dessus de `v0.4.2 - fiche client crm complete`. La V0.4.2 n'étant pas encore validée/mergée, cette correction sera couverte par le même tag `v0.4.2` lors de la finalisation, sans tag séparé.

- **Bouton "Retour à la liste" repositionné** : sorti de la carte d'identité client, déplacé en petit lien discret aligné à droite du fil d'Ariane (nouvelle ligne `.fiche-top-row`).
- **KPI revus** : remplacement de "Relances en cours", "Rendez-vous à venir" et "Documents récents" par "Montant à encaisser", "Devis en cours" et "Prochaine action" (ex. date + libellé d'action, ou "Aucune action prévue" si rien n'est planifié). "Chiffre d'affaires généré" et "Gestes commerciaux" conservés. Chaque KPI a désormais sa propre légende dans `CLIENT_DETAILS.kpis` (valeur + légende par indicateur, pour les 6 clients).
- **Notes commerciales restylées** : cartes avec liseré coloré à gauche, séparation nette entre contenu et méta (pastille d'initiales de l'auteur, nom, date), sans changer la limite de 3 notes affichées.
- **Rendez-vous liés cliquables** : chaque rendez-vous devient un lien vers `agenda.html?rdv=<id>` (identifiant ajouté à chaque rendez-vous dans `CLIENT_DETAILS`), avec effet de survol cohérent avec `.alert-item` (V0.3).
- **Actions secondaires allégées en icônes** (SVG simples déjà dans le style du projet, aucune librairie ajoutée) : "Personnaliser les champs" et nouvelle icône "Modifier les informations" (bloc Informations client), "Voir toutes les notes" (bloc-notes), "Voir tout l'historique" (historique), "Voir dans l'agenda" et "Voir dans Facturation" (liens réels), "Télécharger PDF" (icône + petit badge "PDF"). Chaque icône porte un `title` et un `aria-label`. "Ajouter une note" reste un bouton texte (action jugée principale).
- **`css/styles.css`** : nouvelles classes additives (`.fiche-top-row`, `.fiche-back-link` remplaçant `.fiche-back-btn`, `.block-icon-btn`, `.pdf-download`/`.pdf-badge`, restyle de `.note-card`/`.note-meta`, `.appointment-item-link`).

Vérifications effectuées : équilibre HTML/CSS/JS, aucun identifiant dupliqué, tous les liens résolvent. Test fonctionnel réel dans le navigateur : nouveaux KPI et légendes vérifiés sur plusieurs clients (dont le cas "Aucune action prévue" pour un client inactif), liens de rendez-vous vérifiés (`?rdv=` correctement généré), boutons icône vérifiés un à un (title/aria-label présents), pop-up Work in progress toujours fonctionnel depuis les nouvelles icônes, non-régression confirmée sur `clients.html` (recherche/filtre/compteur) et absence d'erreur console.

Commit créé sur la branche `v0.4.2-client-profile-crm` : `v0.4.2.1 - ajustements ux fiche client`. Pas de merge, tag ni push à ce stade — sera intégré au tag `v0.4.2` lors de la validation finale de la version.

**V0.4.2 (fiche CRM complète + ajustements UX) validée par le chef de projet.** Fusionnée dans `main` (fast-forward), taguée `v0.4.2` (couvrant les deux commits), poussée sur GitHub avec le tag.

## V0.4.3 — Interactions CRM préparatoires & modales placeholder
Réalisée sur la branche dédiée `v0.4.3-crm-placeholder-interactions` (créée depuis `main`, non fusionnée à ce stade). Ajoute un vrai système de modales sur `pages/fiche-client.html`, sans aucune persistance des données.

- **Moteur de modale générique** ajouté dans `js/app.js` (fonctions `COCKPIT_MODAL.open`/`.close`), indépendant du pop-up "Work in progress" existant : fermeture par la croix, par clic en dehors, par la touche Échap, focus déplacé sur le premier champ pertinent à l'ouverture et restitué au bouton déclencheur à la fermeture.
- **Modification du statut client** : icône crayon à côté du badge de statut ouvre une modale avec sélecteur de statut (peuplé depuis `COCKPIT_CLIENT_STATUSES`), statut actuel rappelé, texte précisant l'absence de persistance. "Enregistrer" ferme la modale et déclenche le pop-up Work in progress.
- **Personnalisation des statuts** : icône réglages dans la modale de statut remplace son contenu par la liste des statuts existants (aperçu badge, icônes modifier/supprimer par ligne, bouton "Ajouter un statut"), avec un lien "Retour au changement de statut" pour revenir en arrière. Tout reste Work in progress.
- **Notes commerciales** : "Nouvelle note" et chaque note visible (icônes crayon/poubelle) ouvrent des modales de formulaire (textarea, rappel auteur/date pour l'édition, extrait de la note pour la suppression) ; toutes les validations déclenchent le pop-up Work in progress sans modifier les données. "Voir toutes les notes" ouvre une modale en lecture seule listant les 3 notes visibles + les notes archivées factices (`notesArchive`, nouveau champ ajouté aux 6 clients).
- **Aucune donnée modifiée durablement, aucun `localStorage` ajouté.**

Vérifications effectuées : équilibre HTML/CSS/JS (accolades et parenthèses), aucun identifiant dupliqué. **Test fonctionnel réel dans le navigateur** : ouverture/fermeture de chaque modale (croix, Échap, clic extérieur), focus déplacé à l'ouverture et restitué au déclencheur à la fermeture (vérifié avec un focus initial simulant un vrai clic souris), aller-retour entre la modale de statut et celle de personnalisation, "Enregistrer"/"Ajouter"/"Supprimer" fermant systématiquement la modale et ouvrant le pop-up Work in progress, modale "Toutes les notes" vérifiée sur deux clients (5 notes pour `martin-dupont`, 3 pour `julien-petit` qui n'a qu'une seule note archivée), présence de `title`/`aria-label` vérifiée sur tous les boutons icône de la fiche. Non-régression vérifiée sur `clients.html` et `dashboard.html` (aucune erreur console, aucune requête en échec liée aux pages modifiées, pop-up Work in progress toujours fonctionnel).

Commit créé sur la branche `v0.4.3-crm-placeholder-interactions` : `v0.4.3 - interactions crm preparatoires et modales placeholder`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.4.3.1 — Ajustements de parcours (retour contextuel + statut cliquable)
Correction UX ciblée, réalisée sur la même branche `v0.4.3-crm-placeholder-interactions` (pas de nouvelle branche, pas de V0.5), avant validation finale de la V0.4.3.

- **Icône crayon du statut supprimée** : remplace l'icône introduite en V0.4.3. Le badge de statut lui-même (`#client-identity-status`) devient un vrai `<button>` stylisé comme un badge (`badge-clickable`), cliquable à la souris et activable au clavier (comportement natif Entrée/Espace d'un `<button>`), avec `title`/`aria-label` "Modifier le statut client" et un léger chevron discret en `::after`. Ouvre exactement la même modale "Modifier le statut client" qu'en V0.4.3.
- **Retour contextuel depuis Agenda et Facturation** : les liens réels "Voir dans l'agenda", "Voir dans Facturation" et les liens individuels de rendez-vous embarquent désormais `&from=fiche-client&client=<slug>`. Sur `agenda.html`/`facturation.html`, si ces paramètres sont présents et que le slug est connu, un petit lien discret "← Retour à la fiche de [Nom]" apparaît en haut de la page (repli sur "← Retour à la fiche client" si le nom n'est pas résolu) ; sinon, aucun changement (accès direct via la sidebar inchangé).
- **`CLIENT_DETAILS` exposé globalement** (`window.COCKPIT_CLIENT_DETAILS`) pour permettre à Agenda/Facturation de retrouver le nom du client sans dupliquer les données.
- **Aucun vrai filtrage Agenda/Facturation par client, aucune persistance du statut, aucun `localStorage`** : purement un ajustement de parcours.

Vérifications effectuées : équilibre HTML/CSS/JS, aucun identifiant dupliqué. Test fonctionnel réel dans le navigateur : accès direct à `agenda.html`/`facturation.html` sans paramètre (aucun bandeau, confirmé), parcours fiche → agenda → retour et fiche → facturation → retour (nom du client correctement affiché et lien de retour fonctionnel), cas slug inconnu (repli générique "Retour à la fiche client"), badge de statut testé à la souris (ouverture modale) et focus/Échap (fermeture + focus restitué au badge), non-régression confirmée sur les modales V0.4.3 (aller-retour statut/personnalisation, modale "Nouvelle note") et sur `clients.html`.

Commit créé sur la branche `v0.4.3-crm-placeholder-interactions` : `v0.4.3.1 - ajustements de parcours retour contextuel et statut cliquable`. Pas de merge dans `main`, pas de tag, pas de push — sera intégré au tag `v0.4.3` lors de la validation finale de la version.

## V0.5.1 — Catalogue Produits / Services & accès fiche
Réalisée sur la branche dédiée `v0.5.1-products-services-catalog` (créée depuis `main`, non fusionnée à ce stade). Transforme la page placeholder `pages/produits-services.html` en un vrai catalogue consultable, et prépare la fiche produit/service détaillée (V0.5.2) avec une première page placeholder.

- **`pages/produits-services.html` réécrite en catalogue** : barre de recherche (`#products-search`), filtre par type (`#products-type-filter`), filtre par statut (`#products-status-filter`), bouton "Réinitialiser", compteur dynamique ("X éléments affichés sur Y"), bouton "Ajouter un produit / service" (`btn-wip`, libellé couvrant les deux types). Tableau `#products-table` à 6 colonnes : Nom, Type, Prix HT, TVA par défaut, Statut, Actions. Colonnes "Marge estimée" et "Ventes" de l'ancien placeholder supprimées (hors périmètre V0.5.1). 8 éléments fictifs (6 Services, 2 Produits ; statuts Actif/Brouillon/Inactif/Archivé tous représentés). Chaque ligne porte un seul lien "Voir la fiche" vers `fiche-produit-service.html?item=<slug>`.
- **Statuts et types centralisés dans `js/app.js`** (nouvelle IIFE "Page Produits / Services", miroir du pattern Clients de la V0.4.1) : `PRODUCT_TYPES` (Produit → `badge-neutral`, Service → `badge-info`) et `PRODUCT_STATUSES` (Actif → `badge-success`, Brouillon → `badge-warning`, Inactif → `badge-neutral`, Archivé → `badge-info` — `badge-danger` volontairement réservé à des statuts réellement critiques dans d'autres modules), exposés globalement (`window.COCKPIT_PRODUCT_TYPES`, `window.COCKPIT_PRODUCT_STATUSES`). `PRODUCT_DETAILS` (une entrée par slug déjà présent dans le catalogue) exposé via `window.COCKPIT_PRODUCT_DETAILS`, prépare la V0.5.2 sans construire de vraie source de données. Recherche/filtre croisé type+statut/compteur/réinitialisation sur le même principe que la page Clients.
- **`pages/fiche-produit-service.html` créée** (première page placeholder, sur le modèle allégé de `fiche-client.html` en V0.4.1) : fil d'Ariane + lien "Retour au catalogue", état propre "Produit ou service introuvable" si le slug est absent ou inconnu, identité minimale (nom, badge type, prix HT formaté avec séparateur de milliers, TVA par défaut, badge statut), bannière indiquant que la fiche complète sera enrichie en V0.5.2. Aucune donnée détaillée (description, historique d'utilisation, options) construite à ce stade.
- **Affichage TVA en texte simple** ("20 %"), pas sous forme de badge : la TVA reste indicative en V0.5.1, aucun calcul, aucun prix TTC.
- **Classes CSS de filtres généralisées** : `.clients-filters` / `.clients-status-select` / `.clients-counter` renommées en `.table-filters` / `.table-select` / `.table-counter` dans `css/styles.css`, réutilisées telles quelles par le catalogue Produits/Services et par `pages/clients.html` (mis à jour en conséquence, y compris le `<select>` généré dynamiquement dans la modale de changement de statut client). Aucun changement visuel sur la page Clients.
- **Aucune donnée modifiée durablement, aucun `localStorage` ajouté, aucune vraie création/modification/suppression, aucune catégorie/famille, aucune unité, aucune gestion de stock, aucun calcul TVA/marge réel.**

Vérifications effectuées : test fonctionnel réel dans le navigateur. Catalogue : recherche par nom (ex. "audit" → 1 résultat), filtre par type seul, filtre type+statut combinés (ex. Produit + Inactif → "Accessoire premium" uniquement), recherche sans résultat (compteur "0 élément affiché sur 8"), bouton Réinitialiser (retour à l'état initial, 8/8, champs vidés), 8 liens "Voir la fiche" vérifiés un par un. Fiche produit/service : slug valide (toutes les informations affichées, prix correctement formaté après correction — voir ci-dessous), slug inconnu (état "introuvable" affiché), accès direct sans paramètre (même état "introuvable", aucune erreur). Non-régression Clients : recherche, filtre par statut, compteur et réinitialisation toujours fonctionnels après le renommage des classes CSS ; modale "Modifier le statut client" (V0.4.3) toujours fonctionnelle, `<select>` interne conservant son style (`table-select modal-select`). Non-régression Dashboard : aucune erreur console, page inchangée.

Un ajustement mineur a été corrigé pendant la vérification : la fiche produit/service affichait initialement le prix sans séparateur de milliers ("1800 € HT" au lieu de "1 800 € HT" comme dans le catalogue) ; une fonction de formatage a été ajoutée dans `js/app.js` pour aligner les deux affichages.

Commit créé sur la branche `v0.5.1-products-services-catalog`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.5.2 — Fiche Produit / Service complète
Réalisée sur la branche dédiée `v0.5.2-product-service-detail` (créée depuis `main`, non fusionnée à ce stade). Transforme `pages/fiche-produit-service.html` (placeholder minimal de la V0.5.1) en une vraie fiche produit/service complète, préparatoire à la Facturation (V0.6).

- **En-tête** : reprend la carte d'identité de la fiche client (`.client-identity-card`, sans avatar) — nom, badge Type, badge Statut (statiques, non cliquables, contrairement à la fiche client V0.4.3 : aucune interaction en V0.5.2), courte description, Prix HT et TVA par défaut en meta compacte, retour catalogue, un seul bouton "Modifier" (`btn-wip`, secondaire).
- **Résumé commercial** : 5 cartes KPI (Prix HT, TVA par défaut, Coût estimé, Marge estimée, Statut d'utilisation), via une nouvelle grille dédiée `.kpis-5` dans `css/styles.css` — volontairement séparée de `.client-kpis` pour ne pas toucher à la fiche client. Coût estimé et Marge estimée sont deux champs texte statiques et indépendants, sans aucun calcul entre eux.
- **Description commerciale** : description courte, description détaillée, bénéfices client et limites/conditions (listes à puces courtes, 1 à 2 éléments). Nouvelle variante `.info-grid-wide` (une seule colonne, texte en poids normal) pour accueillir ce contenu plus long sans le comprimer dans la grille 2 colonnes habituelle.
- **Paramètres de vente** : prix HT par défaut, TVA par défaut, prix modifiable en devis/facture (Oui/Non), conditions particulières, puis modalités de paiement acceptées sous forme de badges neutres groupés (nouvelle classe `.badge-group`), à partir d'une liste centralisée `PRODUCT_PAYMENT_MODALITIES` (Comptant, Forfait, Abonnement, Plusieurs fois, Récurrent) — clairement distincte du Type (Produit/Service).
- **Utilisation future en facturation** : sélectionnable en devis / sélectionnable en facture (Oui/Non), rappel des données reprises (nom, description courte, prix HT, TVA par défaut), message annonçant la V0.6. Reste un simple résumé, sans aperçu ni tableau de devis/facture.
- **Statut d'utilisation et sélection devis/facture dérivés automatiquement du statut** (table `STATUS_AVAILABILITY` dans `js/app.js`, logique préparatoire uniquement) : Actif → disponible / sélectionnable ; Brouillon, Inactif, Archivé → non disponible / non sélectionnable.
- **Note interne** : un bloc unique (réutilise `.note-card`, sans les actions modifier/supprimer de la fiche client), avec une ligne de métadonnées (référence interne, date de création, dernière mise à jour) et un texte statique.
- **Historique de l'offre** : bloc compact réutilisant `.history-list`/`.history-item` (nouvelle variante `.history-item-compact`, 3 colonnes sans auteur), à partir d'une nouvelle table `OFFER_HISTORY_TYPES` (création, passage en actif, modification du prix, modification de la TVA indicative, archivage).
- **`PRODUCT_DETAILS` enrichi pour les 8 éléments existants** avec tous les champs prévus (descriptions, bénéfices, limites, coût/marge estimés, modalités de paiement, prix modifiable, conditions particulières, référence interne, dates, note interne, historique).
- **Aucune donnée modifiée durablement, aucun `localStorage` ajouté, aucune vraie édition, aucune modale, aucune règle métier active de facturation.**

Vérifications effectuées : test fonctionnel réel dans le navigateur sur les 8 fiches (contrôle de la présence de tous les champs via `window.COCKPIT_PRODUCT_DETAILS`), vérification ciblée du cas Brouillon (`pack-accompagnement-dirigeant` : statut d'utilisation "Non disponible", sélectionnable devis/facture "Non") et du cas Archivé (`ancienne-offre-decouverte` : historique à 3 événements avec le badge "Archivage"), cas slug inconnu et slug absent (état "Produit ou service introuvable" affiché dans les deux cas). Grille `.kpis-5` vérifiée en 5 colonnes réelles à largeur desktop (1280px), groupe de badges de modalités de paiement vérifié visuellement. Non-régression confirmée sur le catalogue `produits-services.html` (recherche, liens "Voir la fiche"), sur `clients.html` (compteur clients) et sur `dashboard.html` (aucune erreur console).

Commit créé sur la branche `v0.5.2-product-service-detail`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.5.3 — Interactions préparatoires Produits / Services
Réalisée sur la branche dédiée `v0.5.3-products-services-interactions` (créée depuis `main`, non fusionnée à ce stade). Ajoute 7 modales d'interactions préparatoires sur `produits-services.html` et `fiche-produit-service.html`, sur le même principe que la V0.4.3 côté Clients/CRM : aucune donnée n'est réellement créée, modifiée ou supprimée.

- **"Ajouter un produit / service"** (catalogue) : le bouton devient un vrai déclencheur (retrait de `btn-wip`). Formulaire simulé : nom, type, prix HT, TVA par défaut, statut initial (Brouillon présélectionné), description courte, modalités de paiement (cases à cocher), note interne. "Enregistrer" ferme la modale et déclenche le pop-up Work in progress, sans toucher à `PRODUCT_DETAILS`.
- **"Modifier l'offre"** (fiche) : le bouton "Modifier" de l'en-tête devient un vrai déclencheur, formulaire identique pré-rempli avec les valeurs actuelles de l'offre affichée.
- **"Changer le statut de l'offre"** : le badge de statut de la fiche devient un vrai `<button>` cliquable (`badge-clickable`), exactement sur le modèle du badge de statut client (V0.4.3.1). Sélecteur des 4 statuts, bouton "Valider" (et non "Enregistrer", pour rester distinct d'une simple sauvegarde de formulaire). L'archivage reste un changement de statut parmi d'autres, pas une suppression.
- **"Modalités de paiement acceptées"** : action discrète (icône crayon) dans l'en-tête du bloc Paramètres de vente. Cases à cocher pré-cochées selon l'offre (Comptant, Forfait, Abonnement, Plusieurs fois, Récurrent, et une nouvelle option **Autre**, non cochée par défaut). `PRODUCT_PAYMENT_MODALITIES` promue source globale (`window.COCKPIT_PRODUCT_PAYMENT_MODALITIES`), partagée par le catalogue et la fiche.
- **"Détail de marge à venir"** : lien texte discret "Voir le détail →" sous la carte KPI Marge estimée (nouvelle classe `.kpi-detail-link`), plutôt que toute la carte cliquable — évite la confusion avec les cartes KPI utilisées ailleurs pour la navigation. Modale purement informative (un seul bouton "Fermer") : rappel du Coût estimé/Marge estimée actuels, puis liste structurée des futures familles de coûts (production, mise en service, commercial, sous-traitance, frais variables, marge cible), toutes à "À définir". Aucun calcul, aucune formule, aucune saisie enregistrée.
- **"Modifier la note interne"** : action discrète (icône crayon) dans l'en-tête du bloc Note interne. Textarea pré-rempli avec la note actuelle. La note interne reste un pavé unique, sans historique de notes (contrairement aux notes commerciales de la fiche client).
- **"Historique complet de l'offre"** : action discrète (icône horloge) dans l'en-tête du bloc Historique. Le bloc inline de la fiche est désormais **plafonné aux 3 événements les plus récents** (avec une mention "+N événement(s) supplémentaire(s)" si l'historique est plus long), tandis que la modale affiche l'historique complet, sans plafond. Nouveau type d'événement **"Modification des modalités de paiement"** ajouté à `OFFER_HISTORY_TYPES` et illustré sur deux offres (`audit-strategique`, `creation-site-vitrine`, chacune enrichie d'un 4e événement) pour donner une vraie utilité à la modale.
- **Bloc "Utilisation future en facturation" volontairement inchangé** : pas d'action "Utiliser dans un devis" ajoutée (décision explicite du chef de projet), pour ne pas laisser penser à une intégration réelle avec la future V0.6 alors qu'aucune destination réelle n'existe encore.
- **Colonne Actions du catalogue inchangée** : reste strictement limitée au bouton "Voir la fiche" par ligne.
- **Aucune donnée modifiée durablement, aucun `localStorage` ajouté, aucun changement réel de statut ou d'archivage, aucune sauvegarde réelle de note, aucun calcul de marge/TVA/TTC réel.**

Vérifications effectuées : test fonctionnel réel dans le navigateur. Les 7 modales testées (statut, modification de l'offre avec pré-remplissage vérifié champ par champ, modalités de paiement avec cases pré-cochées correctes, détail de marge, note interne, historique complet, ajout catalogue), sur un élément Actif (`audit-strategique`) et un élément Archivé (`ancienne-offre-decouverte`) pour couvrir plusieurs statuts. Accessibilité vérifiée : fermeture par clic sur "Valider"/"Enregistrer" (ferme la modale + ouvre le pop-up Work in progress), fermeture par Échap avec focus restitué au bouton déclencheur (testé sur le badge de statut), fermeture par clic sur l'overlay, `title`/`aria-label` présents sur toutes les icônes. Historique : plafond à 3 événements confirmé sur `audit-strategique` (4 événements en base, "+1 événement supplémentaire" affiché, 4 événements visibles dans la modale complète dont le nouveau type "Modification des modalités de paiement"). Non-régression vérifiée : `PRODUCT_DETAILS` toujours à 8 entrées après clic sur "Enregistrer" du formulaire d'ajout, catalogue inchangé (recherche, filtres, une seule action par ligne), `fiche-client.html` (modale de statut V0.4.3 toujours fonctionnelle), `clients.html` (compteur) et `dashboard.html` (aucune erreur console).

Commit créé sur la branche `v0.5.3-products-services-interactions`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

### Correction V0.5.3.1 (avant clôture de la V0.5.3)
Correction ciblée apportée sur la même branche `v0.5.3-products-services-interactions`, avant validation finale, suite à une revue humaine du commit `504235a`.

- **Contraste amélioré**, scopé à `#item-profile-content` (uniquement la fiche produit/service, sans toucher `--color-text-secondary` ni les autres pages) : textes secondaires (`.info-label`, `.modal-hint`, légendes de KPI, date/auteur de l'historique) assombris pour une lecture plus confortable, notamment en faible luminosité.
- **Bloc "Utilisation future en facturation" supprimé**, jugé trop déclaratif et peu utile.
- **Nouveau bloc "Coûts & marge"** à la place : tableau de composantes de coût (nom, quantité par vente/prestation, coût unitaire, total ligne), "Coût estimé total" **calculé localement à l'affichage** (somme des lignes, non persistant), et bouton "Ajouter un coût" ouvrant une vraie modale placeholder (nom, quantité, coût unitaire — aucune ligne réellement ajoutée). `PRODUCT_DETAILS` : le champ `coutEstime` (texte statique) est remplacé par `couts` (tableau de lignes) sur les 8 éléments, avec des totaux cohérents avec les anciennes valeurs affichées.
- **KPI "Coût estimé" et modale "Détail de marge à venir" désormais alimentés par ce même calcul**, plus de duplication incohérente entre le KPI, le bloc et la modale. La modale reprend aussi le détail des composantes de coût et conserve la liste des futures familles de coûts (V0.5.3), sans répéter "Marge estimée" (déjà visible dans sa propre carte KPI).
- **Historique enrichi** : chaque événement porte désormais une heure (HH:MM) et un auteur ("Administrateur principal", identité fictive unique). Le bloc inline reste plafonné aux 3 événements les plus récents ; la modale "Historique complet" affiche l'ensemble avec ces nouvelles informations (retour à l'affichage standard `.history-item`, la variante `.history-item-compact` de la V0.5.3 est supprimée).
- **Badge Type devenu cliquable** (`badge-clickable`), exactement comme le badge Statut : ouvre "Changer le type de l'offre" (Produit ou Service uniquement, aucune autre valeur).
- **Bouton global "Modifier" supprimé** de l'en-tête, remplacé par une icône crayon discrète à côté du nom, ouvrant "Modifier le nom de l'offre" (champ nom uniquement).
- **Modifications réparties bloc par bloc** : "Description commerciale" gagne sa propre icône crayon ("Modifier la description commerciale" : description courte, détaillée, bénéfices, limites/conditions) ; l'icône de "Paramètres de vente" évolue pour couvrir tout le bloc ("Modifier les paramètres de vente" : prix HT, TVA, prix modifiable, conditions particulières, modalités de paiement), remplaçant l'ancienne modale limitée aux seules modalités.
- **Code mort supprimé** : `openEditOfferModal`/`buildOfferFormFields` (V0.5.3, devenus orphelins après la suppression du bouton global "Modifier").
- **Cohérence de l'en-tête vérifiée** : nom, type, statut, prix HT, TVA et description courte proviennent tous de la même source `PRODUCT_DETAILS[slug]`, sans donnée doublonnée incohérente.
- **Pagination des listes non traitée** (hors périmètre de cette correction, comme convenu) : ni sur `produits-services.html`, ni sur `clients.html`.

Vérifications effectuées : test fonctionnel réel dans le navigateur. Bloc Coûts & marge vérifié sur un élément Actif (`audit-strategique` : 2 lignes, total 280 € cohérent avec le KPI et la modale de marge) et un élément Brouillon (`pack-accompagnement-dirigeant` : total 410 €, historique à 1 seul événement, aucune mention "+N" à tort). Modale "Ajouter un coût" vérifiée : ouverture, pré-focus sur le nom, "Ajouter" ferme la modale et déclenche Work in progress sans ajouter de ligne réelle (2 lignes avant/après). Modales "Changer le type", "Modifier le nom", "Modifier la description commerciale" et "Modifier les paramètres de vente" vérifiées champ par champ (pré-remplissage correct, cases de modalités de paiement pré-cochées). Historique complet vérifié : 4 événements affichés avec date, heure et "Administrateur principal", dont le nouveau type "Modification des modalités de paiement". Contraste vérifié via `getComputedStyle` (`rgb(75, 85, 99)` sur la fiche produit/service, `rgb(107, 114, 128)` inchangé sur la fiche client — confirmant l'absence de fuite du correctif). Non-régression confirmée : `fiche-client.html` (modale de statut V0.4.3 toujours fonctionnelle), `produits-services.html` (compteur, modale "Ajouter un produit / service"), `clients.html` (compteur) et `dashboard.html` (aucune erreur console).

Commit de correction créé sur la même branche `v0.5.3-products-services-interactions`. Toujours pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.5.4 — Pagination & listes génériques
Réalisée sur la branche dédiée `v0.5.4-list-pagination` (créée depuis `main`, non fusionnée à ce stade). Ajoute une pagination simple (Précédent/Suivant + sélecteur "Afficher 5/10/25") sur les listes Clients et Produits/Services, avec un compteur enrichi, et pose un pattern réutilisable pour les futures listes (Devis, Factures, Trésorerie).

- **Nouveau helper générique `window.COCKPIT_LIST_PAGINATION.init(config)`** dans `js/app.js` : ne connaît rien des filtres métier d'une page donnée — chaque page lui fournit ses lignes, ses champs de recherche/filtre/taille de page/navigation, et une fonction `matchRow(row, searchValue)` qui referme sur ses propres filtres (1 filtre statut pour Clients, 2 filtres type+statut pour Produits/Services). Gère : filtrage + pagination sur le résultat filtré, recalage automatique de la page courante si elle n'existe plus après un changement de filtre/recherche/taille de page, compteur enrichi ("X–Y sur Z <libellé>" ou message vide dédié), état `disabled` natif des boutons Précédent/Suivant, et réinitialisation complète (recherche, filtres, page 1, taille de page revenue à 5) sur le bouton "Réinitialiser" existant.
- **`pages/clients.html`** : nouvelle barre `.list-pagination-bar` juste au-dessus du tableau (compteur à gauche, sélecteur "Afficher X" + Précédent/"Page X sur Y"/Suivant à droite, repli responsive via `flex-wrap`). L'IIFE "Page Clients" est réécrite pour déléguer toute la logique recherche/filtre/compteur/pagination au helper commun ; la liste `CLIENT_STATUSES` et son exposition globale restent inchangées.
- **`pages/produits-services.html`** : même barre de pagination, même principe. L'IIFE "Page Produits / Services" ne change que sur sa section recherche/filtre/compteur (remplacée par un appel au helper avec les 2 filtres type+statut) ; les données `PRODUCT_TYPES`/`PRODUCT_STATUSES`/`PRODUCT_PAYMENT_MODALITIES`/`PRODUCT_DETAILS` et la modale "Ajouter un produit / service" (V0.5.3) restent inchangées.
- **Compteurs enrichis** : "1–5 sur 6 clients" / "Aucun client trouvé" pour Clients ; "1–5 sur 8 éléments" / "Aucun produit ou service trouvé" pour Produits/Services (accord singulier/pluriel géré automatiquement par le helper).
- **CSS ajouté** : `.list-pagination-bar` / `.pagination-controls` / `.pagination-page-indicator` (nouvelles classes minimales), `.btn-secondary:disabled` (état grisé propre pour les boutons Précédent/Suivant en butée).
- **Aucune donnée modifiée, aucun `localStorage` ajouté, aucune persistance de la taille de page, aucun tri de colonnes, aucun export.**

Vérifications effectuées : test fonctionnel réel dans le navigateur sur les deux listes. Clients : affichage par défaut ("1–5 sur 6 clients", Page 1 sur 2, Précédent désactivé), bouton Suivant (dernière page "6–6 sur 6 clients", Suivant désactivé), changement de taille de page (10 → tout sur une page), recherche seule, filtre statut seul, recherche sans résultat ("Aucun client trouvé"), Réinitialiser (recherche/filtre/page/taille de page tous remis à zéro), liens "Voir la fiche" vérifiés. Produits/Services : mêmes scénarios, avec en plus la vérification du recalage automatique de page (filtre réduisant le résultat à 2 éléments pendant qu'on était en page 2 → retour automatique en page 1 sur 1), filtres type+statut combinés ("1–1 sur 1 élément"), modale "Ajouter un produit / service" toujours fonctionnelle. Non-régression confirmée : `fiche-client.html`, `fiche-produit-service.html` et `dashboard.html` (aucune erreur console dans les trois cas), `window.localStorage.length === 0` sur toutes les pages testées. Redimensionnement à la baisse non testable dans cet environnement de prévisualisation (limitation d'outil déjà rencontrée pour d'autres versions) ; le repli responsive repose sur `flex-wrap: wrap`, la même technique déjà validée sur `.page-toolbar`.

Commit créé sur la branche `v0.5.4-list-pagination`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.6.1 — Paramètres entreprise & Module Devis
Réalisée sur la branche dédiée `v0.6.1-devis-company-settings` (créée depuis `main`, non fusionnée à ce stade). Premier module du chantier Facturation : une page Paramètres entreprise et un module Devis complet (liste + éditeur "document vivant" une page), le tout sans persistance réelle mais entièrement fonctionnel en direct.

- **Paramètres entreprise** (`pages/parametres-entreprise.html`, reliée depuis la carte "Informations de l'entreprise" de `settings.html`, dont le bouton "Modifier" devient un vrai lien) : formulaire complet (nom, logo placeholder, adresse, téléphone, e-mail, site, SIREN, SIRET, TVA intracommunautaire, IBAN, BIC, mentions légales par défaut), pré-rempli depuis `COMPANY_SETTINGS` (nouvelle source centralisée dans `js/app.js`, exposée `window.COCKPIT_COMPANY_SETTINGS`). "Enregistrer" en `btn-wip`, comme partout ailleurs : aucune persistance réelle.
- **`pages/facturation.html` devient la vraie liste des devis** (même principe que `clients.html`/`produits-services.html` passées de placeholder à page réelle) : recherche, filtre par statut, pagination via `COCKPIT_LIST_PAGINATION` (V0.5.4, réutilisé tel quel). Nouveaux onglets `.page-tabs` "Devis" (actif) / "Factures" (`btn-wip`, prépare une architecture à onglets pour la V0.6.2 sans la construire). Colonne Actions limitée à "Voir le devis". Bouton "Créer un devis" réel (lien vers `devis-edition.html`) ; "Créer une facture" reste en Work in progress.
- **`pages/devis-edition.html`** (nouvelle page, la plus complexe du projet à ce jour) : consultation, création et modification sur une seule page, sans assistant en plusieurs étapes.
  - **Client** : `<select>` peuplé depuis `CLIENT_DETAILS` (référence vivante `clientSlug`), mais chaque version de devis fige un `clientSnapshot` au moment de sa création — un devis ancien ne change jamais si la fiche client évolue ensuite. Démontré concrètement sur `DEV-2026-00015` : la v1 affiche l'ancienne adresse de Martin Dupont, les v2/v3 l'adresse actuelle.
  - **Émetteur** : même logique avec `companySnapshot`, toujours affiché en lecture seule (les Paramètres entreprise ne s'éditent pas depuis le devis).
  - **Prestations** : "Ajouter depuis le catalogue" ouvre une modale (`COCKPIT_MODAL`) listant uniquement les produits/services au statut **Actif** (`STATUS_AVAILABILITY.selectionnable`, champ posé en V0.5.2/V0.5.3 et enfin utilisé ici) ; la sélection copie intégralement désignation/description/prix HT/TVA dans une ligne devenue indépendante du catalogue — une évolution ultérieure du catalogue ne modifie jamais un devis existant.
  - **Lignes éditables** : désignation, description, quantité, prix unitaire HT, taux de TVA par ligne, remise en pourcentage, total ligne calculé en direct à chaque frappe (`input`), sans reconstruire toute la ligne (le focus reste stable).
  - **Calculs automatiques et arrondis proprement** : `COCKPIT_DEVIS_CALC` (`roundMoney`/`formatMoney`/`computeLine`/`computeDevisTotals`) — arrondi à 2 décimales à chaque étape (ligne puis somme des lignes déjà arrondies, pas de recalcul global qui désynchroniserait lignes et totaux), récapitulatif Total HT / détail TVA par taux / Total TVA / Total TTC, format français ("1 800,00 €").
  - **Numérotation** : format `DEV-AAAA-00001`, compteur annuel simulé par balayage des numéros existants (`computeNextDevisNumero`) — purement indicatif, non persistant.
  - **Versionnement et verrouillage**, démontrés sur données fictives pré-écrites : `DEV-2026-00015` porte 3 versions (v1 et v2 envoyées, v3 acceptée et active) ; sélecteur de versions (pastilles cliquables) ; toute version non active ou au statut Accepté s'affiche en lecture seule (champs texte au lieu de champs de saisie, actions masquées) ; bannière dédiée si le devis actif est verrouillé (Accepté), message distinct si l'on consulte simplement une ancienne version non verrouillée.
  - **Actions** : "Enregistrer", "Créer une nouvelle version" en `btn-wip` direct ; "Supprimer" ouvre une modale de confirmation avant de déclencher le Work in progress ; "Dupliquer" est une vraie action qui recharge l'éditeur avec un nouveau numéro, version 1, statut Brouillon, client et lignes copiés (nouveau snapshot client pris en direct).
- **Jeu de données fictif** : 5 devis couvrant les 4 statuts (Brouillon, Envoyé, Accepté, Refusé), dont `DEV-2026-00015` avec historique de versions complet.
- **Aucune persistance réelle, aucun `localStorage`, aucune vraie numérotation, aucune vraie sauvegarde** : conforme à l'ensemble des versions précédentes du projet. Hors périmètre strict : factures, conversion devis→facture, paiements, échéances, PDF, impression, statistiques, tableau de bord, avoirs.

Vérifications effectuées : test fonctionnel réel dans le navigateur. Liste des devis (compteur, filtre statut, 5 lignes cohérentes avec les montants calculés). Consultation de `DEV-2026-00015` v3 (verrouillé : bannière affichée, aucun champ de saisie, Enregistrer/Supprimer masqués, Dupliquer/Nouvelle version affichés, récapitulatif 3 120,00 €/624,00 €/624,00 €/3 744,00 € cohérent) et v1 (ancienne version : ancienne adresse client figée, lecture seule avec message dédié, sans bannière de verrouillage). Création d'un nouveau devis vierge (numéro `DEV-2026-00016` calculé correctement) : sélection client avec mise à jour immédiate des informations affichées, filtre catalogue vérifié (5 éléments Actifs sur 8 proposés, les Brouillon/Inactif/Archivé exclus), ajout d'une ligne depuis le catalogue, ajout d'une ligne vide, modification en direct de la quantité et de la remise d'une ligne (calcul vérifié à la valeur près : 2 × 1 800 € avec 10 % de remise = 3 240 € HT, 648 € de TVA, 3 888 € TTC), suppression de ligne. Duplication depuis `DEV-2026-00012` (nouveau numéro, client et lignes repris, statut Brouillon). Suppression avec modale de confirmation (Annuler/Supprimer), "Supprimer" déclenchant bien le Work in progress. Accessibilité : Échap ferme la modale catalogue avec restitution du focus au bouton déclencheur. Cas "devis introuvable" vérifié. Non-régression confirmée sur `clients.html`, `produits-services.html`, `fiche-client.html`, `fiche-produit-service.html`, `dashboard.html` et `settings.html` (lien réel vérifié) — aucune erreur console sur aucune page.

### Correctifs de revue V0.6.1 (avant clôture de la V0.6.1)
Corrections ciblées apportées sur la même branche `v0.6.1-devis-company-settings`, avant validation finale, suite à une revue humaine du commit `657b00e`. Périmètre strictement ergonomique/cohérence du module Devis : aucune facture, aucune persistance, aucun `localStorage` ajouté.

- **Recherche client avancée** dans l'éditeur de devis : le `<select>` est remplacé par un champ de recherche texte ("contient", insensible à la casse) interrogeant nom, société, téléphone, e-mail et adresse de `CLIENT_DETAILS`, avec une liste de résultats cliquables. La sélection alimente toujours `clientSlug` + `clientSnapshot` figé, sans rien changer à cette logique.
- **Recherche avancée dans "Ajouter depuis le catalogue"** : un champ de recherche filtre en direct (nom, description, catégorie, prix, statut) la liste déjà restreinte aux produits/services Actifs ; la copie en ligne indépendante du catalogue est inchangée.
- **Description de ligne agrandissable** : icône dédiée à côté du champ description, ouvrant une modale avec une zone de texte plus confortable ; la validation met à jour la ligne et le champ inline, sans impact sur les calculs.
- **Bloc de totaux restructuré** : `computeDevisTotals` calcule désormais aussi le total brut avant remise et le total des remises (arrondis à chaque étape, comme le reste). Le récapitulatif affiche l'ordre demandé : Total HT avant remise, Remises, Total HT net, détail TVA par taux, Total TVA, Total TTC.
- **Menu déroulant "Enregistrer"** : le bouton unique devient un menu avec deux choix ("Enregistrer comme brouillon" / "Enregistrer comme version définitive"), tous deux en `btn-wip` — aucune sauvegarde réelle, pose seulement l'ergonomie future.
- **"Créer une nouvelle version" devient une vraie action** (n'est plus `btn-wip`) : disponible uniquement sur la version active d'un devis Accepté, elle bascule l'éditeur sur un brouillon éditable en mémoire de page (numéro de version suivant, statut Brouillon, client/lignes/conditions de paiement repris de la version d'origine, nouveau `companySnapshot`), sans jamais modifier les versions existantes ni rien persister. Un message et une pastille dédiée ("vX (brouillon, non enregistré)") signalent clairement qu'il s'agit d'une préparation non enregistrée.
- **Bloc "Conditions de paiement"** ajouté à l'éditeur (délai de paiement, acompte demandé, paiement en plusieurs fois, note libre), visible en lecture seule ou éditable selon l'état du devis, avec des données illustrées sur les 5 devis fictifs (dont une évolution progressive sur les 3 versions de `DEV-2026-00015`, jusqu'au paiement en 3 fois de la maintenance sur la version acceptée).

Vérifications effectuées : test fonctionnel réel dans le navigateur. Recherche client (par société "leroy" et par ville "nantes", sélection confirmée). Recherche catalogue (par mot-clé "maintenance", cas sans résultat vérifié). Modale de description (édition et report vers le champ inline). Totaux vérifiés sur `DEV-2026-00015` v3 (3 300,00 € avant remise, -180,00 € de remises, 3 120,00 € net, 624,00 € de TVA, 3 744,00 € TTC) et sur une ligne de test (2 × 1 000 € HT, remise 10 % → 2 000,00 €/-200,00 €/1 800,00 €/360,00 €/2 160,00 €). Menu Enregistrer (ouverture/fermeture, clic sur une option déclenchant le Work in progress et refermant le menu). Nouvelle version depuis `DEV-2026-00015` v3 acceptée : bascule en v4 brouillon éditable, client/lignes/conditions de paiement repris, aucune modification des versions existantes. Conditions de paiement vérifiées en lecture seule (v1, un seul champ rempli) et en édition (nouveau devis). Non-régression confirmée sur `facturation.html`, `parametres-entreprise.html`, `clients.html`, `produits-services.html` et `dashboard.html` — aucune erreur console sur aucune page.

Commit correctif créé sur la même branche `v0.6.1-devis-company-settings`. Toujours pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

**Clôture de la V0.6.1** : après revue humaine des correctifs, la branche `v0.6.1-devis-company-settings` (commits `657b00e` et `4d5f982`, plus un correctif `.gitignore` `6ca6ba8`) a été fusionnée dans `main` (fast-forward), taguée `v0.6.1`, et poussée sur `origin` avec le tag.

## V0.6.2 — Module Factures & Paiements
Réalisée sur la branche dédiée `v0.6.2-factures-payments` (créée depuis `main` après la clôture de la V0.6.1, non fusionnée à ce stade). Active l'onglet "Factures" préparé en V0.6.1 : liste, éditeur de facture "document vivant" sur le même principe que les devis, conversion depuis un devis Accepté, et suivi des paiements — toujours sans persistance réelle.

- **Statuts de facture en deux couches** : un `statutEmission` explicite et stocké (Brouillon / Émise / Annulée) et un statut de paiement calculé à l'affichage (Non payée / Partiellement payée / Payée / En retard), combinés par `COCKPIT_FACTURE_CALC.computeStatutAffiche` — une facture Brouillon ne peut jamais afficher un statut de paiement incohérent.
- **`pages/facturation.html`** : les deux onglets "Devis"/"Factures" sont désormais tous les deux réels, chacun avec son propre panneau, sa recherche, ses filtres et sa pagination (`COCKPIT_LIST_PAGINATION`, deux instances indépendantes). La liste des factures est générée dynamiquement depuis `FACTURE_DETAILS` (contrairement à la liste des devis, écrite en dur) car le statut affiché dépend d'un calcul.
- **`pages/facture-edition.html`** (nouvelle page) : consultation, création manuelle, conversion depuis un devis Accepté.
  - **Brouillon éditable** : client (recherche avancée dupliquée depuis l'éditeur de devis), lignes indépendantes copiées du catalogue ou ajoutées librement (avec description agrandissable), calculs HT/TVA/TTC/remise, conditions de paiement — sur le même principe que les devis.
  - **"Émettre la facture"** : action réelle en mémoire de page. Attribue le numéro officiel `FAC-AAAA-00001` (réservé à l'émission, un brouillon reste "sans numéro"), une date d'émission, une échéance dérivée du délai de paiement choisi, puis verrouille durablement lignes/client/snapshots/conditions de paiement (aucune modification possible ensuite, y compris dans cette même session).
  - **Paiements** : "Ajouter un paiement" (date, montant, mode, référence, note) est une action réelle, possible uniquement sur une facture Émise. Total payé, reste à payer, pourcentage payé et statut affiché se recalculent immédiatement. Un paiement qui dépasserait le reste à payer est refusé avec un message explicite, sans fermer la modale.
  - **Annulation non proposée** : les factures Émises n'offrent aucune action d'annulation dans cette version (les avoirs sont hors périmètre) — une note dédiée l'explique. Un statut Annulée existe néanmoins dans l'architecture et dans les données fictives, pour démonstration.
- **Conversion devis → facture** : bouton "Convertir en facture" sur `devis-edition.html`, visible uniquement sur un devis au statut Accepté et pas déjà converti. Ouvre un brouillon de facture préempli avec le `clientSnapshot`/`companySnapshot`/lignes/conditions de paiement déjà figés de cette version de devis, et conserve une référence croisée bidirectionnelle (la facture affiche un lien vers son devis source, le devis affiche un lien "Facture associée" une fois converti).
- **Jeu de données fictif** : `FACTURE_DETAILS` (6 factures couvrant les 4 statuts affichés + Annulée + un brouillon sans numéro), dont `FAC-2026-00003` liée à `DEV-2026-00015` (payée intégralement en deux versements). Un second devis Accepté (`DEV-2026-00016`, sans facture associée) a été ajouté à `DEVIS_DETAILS` pour pouvoir démontrer une conversion réelle.
- **Aucune persistance réelle au-delà de la session courante** : conforme au reste du projet — un rechargement de page réinitialise tout (émission, paiements, conversions), seules les actions décrites ci-dessus sont réelles en mémoire.

Vérifications effectuées : test fonctionnel réel dans le navigateur. Liste des factures (6 lignes, statuts et montants cohérents avec les calculs, y compris le reste à payer). Consultation de `FAC-2026-00003` (verrouillée, Payée, lien vers le devis source, paiements et totaux cohérents avec la version acceptée du devis) ; tentative de paiement supplémentaire bloquée ("Ce montant dépasse le reste à payer (0,00 €)"). `FAC-2026-00002` (Partiellement payée) : ajout d'un paiement de 700 € refusé (dépasse le reste de 600 €), ajout de 600 € accepté, statut recalculé en direct vers Payée. Brouillon fictif (`BROUILLON-JULIEN-PETIT`) émis en direct : numéro `FAC-2026-00006` attribué, dates d'émission/échéance calculées, verrouillage immédiat vérifié (recherche client et lignes redeviennent en lecture seule). Facture annulée (`FAC-2026-00005`) : message dédié, paiements bloqués, aucune note Avoirs affichée à tort. Recherche client et recherche catalogue vérifiées dans l'éditeur de facture (mêmes comportements que dans l'éditeur de devis). Conversion réelle depuis `DEV-2026-00016` (Accepté) : brouillon préempli avec client/lignes/conditions de paiement du devis, référence croisée affichée des deux côtés ; tentative de conversion depuis un devis non accepté (`DEV-2026-00012`, Envoyé) correctement bloquée (état "introuvable"). Non-régression confirmée sur `devis-edition.html` (dont une ancienne version non active), `clients.html`, `produits-services.html`, `dashboard.html`, `parametres-entreprise.html` et `fiche-client.html` — aucune erreur console sur aucune page.

Commit créé sur la branche `v0.6.2-factures-payments`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

**Clôture de la V0.6.2** : après revue humaine validée, la branche `v0.6.2-factures-payments` (commit `ec891a6`) a été fusionnée dans `main` (fast-forward), taguée `v0.6.2`, et poussée sur `origin` avec le tag.

## V0.6.3 — Documents PDF / Impression (devis et factures)
Réalisée sur la branche dédiée `v0.6.3-pdf-print` (créée depuis `main` après la clôture de la V0.6.2, non fusionnée à ce stade). Permet de produire un document propre et imprimable à partir d'un devis ou d'une facture existants, sans bibliothèque externe : impression navigateur (`window.print()`) avec un CSS d'impression dédié, l'utilisateur choisissant "Enregistrer en PDF" comme destination s'il le souhaite.

- **Deux nouvelles pages document, sans sidebar ni topbar applicative** : `pages/devis-document.html?devis=NUMERO&version=N` et `pages/facture-document.html?facture=KEY`. Une simple barre d'action ("Retour à l'édition" + "Imprimer / Enregistrer en PDF") et le document lui-même, centré à l'écran, rendu sobre et proche d'un logiciel de comptabilité.
- **`css/print.css`** (nouveau) : mise en page de la "feuille" document à l'écran, et règles `@media print` dédiées — format A4, marges, barre d'action masquée à l'impression, couleurs des badges conservées (`print-color-adjust: exact`), en-tête de tableau répété sur chaque page, saut de page évité au milieu d'une ligne ou du récapitulatif.
- **Contenu du document devis** : émetteur, client, numéro, version, statut, date, lignes, récapitulatif complet (Total HT avant remise/Remises/Total HT net/TVA/Total TTC), conditions de paiement, mentions légales (`COMPANY_SETTINGS.mentionsLegales`, affichées pour la première fois dans l'application), zone de signature "Bon pour accord". Bannière dédiée si la version est un brouillon, bannière distincte si ce n'est pas la version active du devis (version historique).
- **Contenu du document facture** : émetteur, client, numéro (ou "Brouillon sans numéro"), dates d'émission/échéance, statut affiché, référence au devis source si la facture en provient (lien croisé vers `devis-document.html`), lignes, récapitulatif, conditions de paiement, paiements enregistrés et reste à payer (uniquement si la facture n'est plus un brouillon), mentions légales. Bannière dédiée si brouillon, bannière dédiée si annulée.
- **Aucun recalcul indépendant** : les deux pages document réutilisent exactement `COCKPIT_DEVIS_CALC`/`COCKPIT_FACTURE_CALC` déjà validés (mêmes arrondis, même logique de statut affiché) — aucune nouvelle fonction de calcul.
- **Lien "Aperçu / Imprimer"** ajouté dans `devis-edition.html` et `facture-edition.html`, visible uniquement en mode consultation d'un document déjà existant dans les données (y compris un brouillon fictif ou une ancienne version de devis), ouvert dans un nouvel onglet. Masqué avec un message explicite sur un devis/une facture en cours de création non encore enregistré — aucun mécanisme de stockage temporaire (`sessionStorage` ou équivalent) n'a été introduit pour contourner cette limite, conformément à l'absence de persistance du projet.
- **Aucune bibliothèque externe, aucune dépendance CDN** : le bouton "Imprimer / Enregistrer en PDF" ne fait qu'appeler `window.print()`.

Vérifications effectuées : test fonctionnel réel dans le navigateur. Document devis sur `DEV-2026-00015` v3 (accepté, actif : aucune bannière, totaux 3 300,00 €/-180,00 €/3 120,00 €/624,00 €/3 744,00 € cohérents, mentions légales et zones de signature affichées) et v1 (bannière "Version historique" affichée). Devis brouillon fictif (`DEV-2026-00011`) : bannière "Brouillon" affichée. Lien invalide (`DEV-9999-99999`) : état "Document introuvable" correct. Document facture sur `FAC-2026-00003` (Payée, verrouillée, lien vers le devis source, paiements et reste à payer à 0,00 € cohérents), `FAC-2026-00004` (En retard, reste à payer 1 170,60 € cohérent), `FAC-2026-00005` (Annulée : bannière dédiée, section paiements vide) et `BROUILLON-JULIEN-PETIT` (bannière brouillon, section paiements masquée). Bouton "Imprimer / Enregistrer en PDF" vérifié (appel de `window.print()` confirmé). Feuille de style `css/print.css` correctement chargée avec son bloc `@media print`. Lien "Aperçu / Imprimer" vérifié dans les deux éditeurs : visible et correctement pointé en mode consultation, masqué avec message explicite sur un nouveau document non enregistré. Non-régression confirmée sur `facturation.html` (les deux onglets, 6 devis et 6 factures) — aucune erreur console sur aucune page testée.

Commit créé sur la branche `v0.6.3-pdf-print`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

### Correctif de revue V0.6.3 — Rendu impression

Corrections d'ergonomie et de rendu print apportées après la revue humaine, sur la même branche `v0.6.3-pdf-print` :

- **En-têtes/pieds de page navigateur** : ces éléments (date, titre, chemin `file:///`) sont ajoutés par le navigateur lui-même, hors de portée du CSS. Un texte explicatif (`.document-print-hint`, masqué à l'impression) a été ajouté au-dessus du bouton d'impression sur les deux pages document : "Dans la fenêtre d'impression, désactivez les en-têtes et pieds de page du navigateur pour obtenir un document propre." Marges `@page` légèrement resserrées (15mm/15mm/18mm).
- **Tableau des lignes en impression** : colonnes fixes (`table-layout: fixed`) avec largeurs dédiées par colonne (16/26/8/15/9/9/17 %, total 100 %), police réduite (10,5px), colonnes numériques en `white-space: nowrap` pour ne plus jamais casser un montant et son symbole "€" sur deux lignes.
- **Bloc Conditions de paiement** : nouvelle classe `.document-conditions-block` (posée sur le bloc dans les deux pages document) avec `break-inside: avoid` en impression, pour éviter la coupure du bloc entre deux pages.
- **Badges de statut en impression** : contour fin coloré sur fond blanc plutôt que fond plein coloré (aspect "interface web"), texte en majuscules avec un léger espacement — rendu plus sobre, toujours lisible.

Vérifications effectuées : re-test des scénarios déjà validés en V0.6.3 (devis actif sans bannière, ancienne version avec bannière "Version historique", devis brouillon avec bannière dédiée, facture Payée/En retard/Annulée/Brouillon, non-régression sur `facturation.html`) — aucune erreur console, largeurs de colonnes vérifiées par lecture directe des règles CSS (somme exacte à 100 %). Limite assumée : la pagination réelle sur plusieurs pages (ex. facture Payée découpée sur 2 pages) n'a pas pu être observée directement, l'environnement de prévisualisation ne permettant pas d'émuler le rendu d'impression paginé du navigateur ; les règles `break-inside`/`page-break-inside` ont été vérifiées au niveau du CSS mais pas visuellement sur un rendu paginé réel.

Commit correctif créé sur la même branche `v0.6.3-pdf-print`. Toujours pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

**Clôture de la V0.6.3** : après revue humaine validée, la branche `v0.6.3-pdf-print` (commits `2ab3dfc` et `16bdf05`) a été fusionnée dans `main` (fast-forward), taguée `v0.6.3`, et poussée sur `origin` avec le tag.

## V0.6.4 — Intégration commerciale du module Facturation
Réalisée sur la branche dédiée `v0.6.4-facturation-integration` (créée depuis `main` après la clôture de la V0.6.3, non fusionnée à ce stade). Rend visibles et exploitables, dans le reste de l'application, les devis/factures/paiements construits en V0.6.1-6.3 : fiche client, tableau de bord, statistiques commerciales, navigation croisée — toujours en lecture seule, sans nouvelle donnée ni persistance.

- **Réorganisation de `js/app.js`** : `CLIENT_DETAILS` a été extrait de l'IIFE "Fiche client" en un bloc de données autonome, positionné avant les blocs `DEVIS_DETAILS`/`FACTURE_DETAILS` (eux-mêmes déplacés juste après). Nécessaire car la fiche client a désormais besoin de lire `DEVIS_DETAILS`/`FACTURE_DETAILS`, et ces derniers ont eux-mêmes besoin de `CLIENT_DETAILS` déjà exposé (pour figer les `clientSnapshot` au moment de leur définition) — un simple déplacement de code, sans réécriture de données ni changement de signature d'aucun helper existant.
- **`window.COCKPIT_FACTURATION_STATS.computeStats()`** (nouveau) : point de calcul unique pour tous les indicateurs commerciaux (CA facturé/encaissé, reste à encaisser, factures en retard, devis acceptés/refusés/envoyés, taux de transformation, panier moyen facture, montant moyen devis, top clients, prochaines échéances), réutilisé par le dashboard et `facturation.html`. Exclut systématiquement les factures Brouillon et Annulées du CA/encaissement, exclut les devis Brouillon du montant moyen, et ne calcule le taux de transformation que sur les devis décidés (Accepté/Refusé, `accepté / (accepté + refusé)`).
- **Fiche client (`pages/fiche-client.html`)** : la section "Documents & facturation" affiche désormais les devis et factures réellement liés au client (association sur `clientSlug`, version active pour les devis), avec statut, montant TTC, reste à payer (factures), et deux liens ("Voir" vers l'éditeur, "Document" vers la page imprimable). Les anciennes entrées fictives incohérentes de type Devis/Facture (numérotation `DV-2026-XXX`/`FA-2026-XXX` sans rapport avec les vraies données, déjà signalées en V0.6.1) sont retirées ; les entrées "Contrat" restent affichées telles quelles, inchangées, hors périmètre facturation.
- **Tableau de bord (`pages/dashboard.html`)** : nouveau bloc compact "Aperçu Facturation" (liste simple, pas une nouvelle carte KPI) — facturé, encaissé, reste à encaisser, factures en retard, devis en attente, taux de transformation. La carte favorite "Devis envoyés", jusqu'ici statique (valeur "6" sans rapport avec les données), reflète désormais le vrai nombre de devis Envoyés. Les 2 grilles de KPI existantes (réservées aux futurs modules Trésorerie/Finance) restent inchangées.
- **`pages/facturation.html`** : nouveau bloc "Statistiques commerciales" au-dessus des onglets Devis/Factures — CA facturé/encaissé/reste à encaisser, taux de transformation, panier moyen facture, montant moyen devis, top clients (3 premiers), prochaines échéances (3 premières). Texte uniquement, aucun graphique.
- **Navigation croisée** : le nom du client devient un lien vers `fiche-client.html?client=SLUG` (nouvelle classe `.document-party-name-link`) dans les parties "Client" de `devis-edition.html`, `facture-edition.html`, `devis-document.html` et `facture-document.html`, dès que `clientSlug` est connu — y compris pendant la recherche client active (le lien apparaît dans le panneau d'information sous la recherche, sans perturber la recherche elle-même).

Vérifications effectuées : test fonctionnel réel dans le navigateur. Fiche client : Martin Dupont (devis Accepté + facture Payée réels, plus aucune entrée fictive en doublon), Sophie Bernard (devis Accepté + facture Émise réels, plus le Contrat fictif conservé), Julien Petit (devis brouillon + facture brouillon réels). Dashboard : bloc "Aperçu Facturation" et carte "Devis envoyés" cohérents avec `computeStats()` (7 314,60 €/4 644,00 €/2 670,60 €/1/2/67 %). `facturation.html` : bloc statistiques cohérent, top clients et prochaines échéances corrects, onglets Devis/Factures toujours fonctionnels. Navigation croisée vérifiée sur les 4 pages (devis-edition en consultation et en recherche active, facture-edition, devis-document sur une version historique, facture-document). Non-régression confirmée sur `clients.html`, `produits-services.html`, `fiche-produit-service.html`, `parametres-entreprise.html`, `settings.html` — aucune erreur console sur aucune page testée. Attention particulière portée à l'ordre d'exécution des blocs de données après réorganisation : vérifiée sur toutes les pages consommatrices (fiche client, devis-edition, facture-edition, facturation, devis-document, facture-document, dashboard).

Commit créé sur la branche `v0.6.4-facturation-integration`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

**Clôture de la V0.6.4** : après revue humaine validée, la branche `v0.6.4-facturation-integration` (commit `6d42b9e`) a été fusionnée dans `main` (fast-forward), taguée `v0.6.4`, et poussée sur `origin` avec le tag.

## V0.7 — Agenda commercial

Réalisée sur la branche dédiée `v0.7-agenda-commercial` (créée depuis `main` après la clôture de la V0.6.4, non fusionnée à ce stade). Construit un nouveau module Agenda commercial centré sur la préparation et le suivi des rendez-vous commerciaux, avec un parcours complet : rendez-vous → préparation commerciale → communication client → devis brouillon → devis classique → facture. Trésorerie et Finance ne sont pas développées dans cette version.

- **Données (`js/app.js`)** : nouveau bloc `RDV_STATUSES` (prévu/confirmé/reporté/réalisé/annulé/sans suite), `RDV_PRIORITES` (basse/normale/haute), `RDV_OPPORTUNITES` (faible/moyen/fort), `RDV_DETAILS` (6 rendez-vous fictifs cohérents avec les clients/devis/factures déjà existants) et un nouvel helper `window.COCKPIT_AGENDA_CALC` : génération d'identifiant, dates, historique, et surtout les trois actions sur le devis lié — `creerDevisBrouillonPourRdv`, `peutSupprimerDevisBrouillonLie` / `supprimerDevisBrouillonLie` (strictement limité à un devis brouillon, une seule version, jamais envoyé ni converti en facture — aucune suppression générale de devis n'est ouverte), `transformerDevisBrouillonEnClassique`. Positionné après les blocs Devis/Factures dont il dépend. Deux devis pré-écrits (`DEV-2026-00011`, `DEV-2026-00015` v3) reçoivent un champ additif `rdvRef` pour démontrer le lien bidirectionnel sur des données qui survivent à un rechargement de page.
- **Règle centrale "un RDV = un devis brouillon lié"** : à la création réelle d'un rendez-vous (fiche `pages/fiche-rdv.html`), un devis brouillon est automatiquement créé et lié dès qu'un client est renseigné ; sinon, un bouton très visible "Créer le devis brouillon lié" est proposé dans la fiche.
- **`pages/agenda.html`** : réécrite en liste réelle (recherche, filtre Tous/Aujourd'hui/Cette semaine/À confirmer/Reportés/Réalisés/Sans suite, pagination via `COCKPIT_LIST_PAGINATION`), sur le même principe que Clients/Produits/Devis/Factures (lignes écrites en dur, filtrées côté JS). Le paramètre `?client=slug` (venant de la fiche client) pré-remplit la recherche. Pas de vue calendrier visuelle (jugée fragile pour cette version), priorité donnée à une vue liste fiable.
- **`pages/fiche-rdv.html`** (nouvelle) : fiche complète en mode création (aucun `?rdv=`) ou consultation/édition (`?rdv=ID`), sur le même principe que `devis-edition.html`. Contient : en-tête (titre, statuts, client, date/heure/lieu, montant potentiel, notes internes, édition via modale) ; bloc Préparation commerciale (contexte du besoin, notes diverses, proposition envisagée, prix cible, prix minimum, marge de négociation calculée, arguments commerciaux, objections possibles, prochaines étapes) ; bloc Communication client (confirmation, rappels effectués, relances, dernière/prochaine communication, commentaire de suivi) avec actions rapides réelles (marquer confirmé/relancé/réalisé, reporter, sans suite) ; bloc Devis lié (créer/supprimer le brouillon, transformer en devis classique, ou lien vers la facture si déjà convertie) ; bloc Historique (création, statut, report, devis, communication, préparation, modification) — un badge "Reporté X fois" apparaît dès qu'un rendez-vous a été reporté au moins deux fois. Toutes les actions sont réelles en mémoire de page, comme partout ailleurs dans l'application : rien ne survit à un rechargement.
- **`pages/rdv-document.html`** (nouvelle) : export imprimable de la fiche de préparation commerciale, sur le même gabarit que les documents devis/facture (`document-sheet` + `css/print.css`, bouton "Imprimer / Enregistrer en PDF" via `window.print()`). Contient les informations du rendez-vous, du client, le contexte du besoin, la proposition envisagée avec prix cible/minimum/marge, les arguments et objections, le résumé de communication, ainsi que deux zones vierges imprimables "Notes pendant le rendez-vous" et "Suite à donner" (réutilisation de `.document-signature-zone`/`.document-signature-box`).
- **Tableau de bord (`pages/dashboard.html`)** : le bloc "Agenda du jour" devient "Agenda commercial" — liste réelle des rendez-vous du jour, puis compteurs (prochains rendez-vous, non confirmés, reportés, devis brouillon en attente, et "reportés plusieurs fois" si au moins un rendez-vous est concerné). Aucune nouvelle grosse carte KPI, aucun autre bloc du dashboard modifié.
- **Navigation croisée** : la fiche client (`pages/fiche-client.html`) affiche désormais dans son bloc "Rendez-vous liés" (déjà présent, jusqu'ici alimenté par une liste fictive statique `client.rendezVous`) les rendez-vous réellement liés au client (calculés depuis `RDV_DETAILS`) ; le lien d'en-tête "Voir dans l'agenda" pointe vers `agenda.html?client=slug`. Le nom du client reste cliquable dans la fiche rendez-vous vers `fiche-client.html`. Depuis `devis-edition.html`, un devis portant un `rdvRef` affiche un lien "Voir le rendez-vous lié" vers `fiche-rdv.html`.

Vérifications effectuées : test fonctionnel réel dans le navigateur. Création d'un rendez-vous (client, dates, priorité/opportunité, montant potentiel) avec devis brouillon auto-créé et historique correspondant ; modification des informations, de la préparation commerciale et de la communication ; actions rapides (confirmer, relancer, réaliser, reporter, sans suite) avec historique cohérent, y compris le badge "reporté plusieurs fois" ; création puis suppression d'un devis brouillon lié (retour à l'état vide) ; transformation d'un devis brouillon en devis classique (disparition des actions de suppression, apparition du lien facture si convertie) ; suppression du rendez-vous avec redirection vers l'agenda ; export PDF de la fiche de préparation (toutes les sections présentes, y compris les deux zones vierges) ; filtres et recherche de `agenda.html` (Tous/Aujourd'hui/Cette semaine/À confirmer/Reportés/Réalisés/Sans suite) vérifiés un par un ; pré-remplissage `?client=` vérifié depuis la fiche client ; bloc Agenda commercial du dashboard vérifié cohérent avec les données. Non-régression confirmée sur `clients.html`, `produits-services.html`, `facturation.html` (les deux onglets et les statistiques commerciales V0.6.4), `devis-edition.html`, `facture-edition.html`, `devis-document.html`, `facture-document.html`, `parametres-entreprise.html`, `settings.html`, `fiche-client.html` (plusieurs clients) — aucune erreur console sur aucune page testée.

Point de vigilance assumé : un rendez-vous (et son éventuel devis brouillon auto-créé) n'existe qu'en mémoire de la page courante, comme toute création réelle dans cette application (aucune persistance). Ouvrir dans un nouvel onglet ou après un rechargement le devis brouillon lié à un rendez-vous créé pendant la session affichera donc l'état "Document introuvable" déjà géré proprement par `devis-edition.html` — comportement identique à celui de tout document créé en mémoire ailleurs dans l'app, pas une régression propre à cette version. Les rendez-vous de démonstration pré-écrits (notamment `rdv-0001` et `rdv-0004`, liés à des devis pré-écrits) ne sont pas concernés et survivent à un rechargement.

Commit créé sur la branche `v0.7-agenda-commercial`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.7.1 — Correctifs UX/métier Agenda-RDV-Devis

Trois correctifs ciblés successifs sur la branche `v0.7-agenda-commercial`, sans revue globale ni retouche de Trésorerie/Finance :
- Vraies vues calendrier Jour/Semaine/Mois (grille horaire, navigation Précédent/Aujourd'hui/Suivant, sélecteurs semaine+année/mois+année, date picker natif fiabilisé contre la saisie clavier d'année invalide), la vue Liste existante devenant une vue secondaire parmi d'autres ; badges statut/priorité/opportunité de la fiche RDV rendus cliquables (petite modale de sélection) ; bug "Voir le devis" corrigé par un repli propre vers un nouveau brouillon pré-rempli si le devis auto-créé en mémoire n'existe plus après navigation.
- Lisibilité des cartes RDV en vues Semaine/Jour, layout dédié pour la vue Jour (timeline + panneau résumé), formulaire de rendez-vous restructuré en 4 sections (Informations / Client / Proposition commerciale / Notes) avec recherche client "contient", sélection lieu/format en liste, horaires par pas de 15 min, et une **proposition commerciale** réelle (produits/services du catalogue, quantités, remise max, TVA) alimentant un montant estimé.
- Cohérence TVA de la proposition commerciale alignée sur les devis/factures (toujours affichée, sans condition d'assujettissement) ; la proposition commerciale déverse désormais ses lignes dans le devis brouillon lié (protégé dès qu'il a été envoyé/converti) ; styles de listes déroulantes harmonisés (suppression des contours noirs natifs) ; navigation agenda/RDV/devis préservée via l'ouverture en nouvel onglet ; suppression du bouton "Transformer en devis classique" de la fiche RDV (action déplacée côté devis) et renommage de "Document" en "Aperçu / imprimer".

Commits créés sur la branche `v0.7-agenda-commercial`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.8 — Module Trésorerie

Réalisée sur la branche dédiée `v0.8-tresorerie` (créée depuis `v0.7-agenda-commercial`, la V0.7.1 n'étant pas encore fusionnée dans `main` à ce stade). Remplace le placeholder "module en construction" de `pages/tresorerie.html` par un véritable outil de pilotage du cash — volontairement simple, pas une comptabilité complète (pas de bilan, compte de résultat, rapprochement bancaire ni synchronisation bancaire).

- **Données (`js/app.js`)** : nouveau bloc `TRESORERIE_CATEGORIES`, `TRESORERIE_STATUSES`, `TRESORERIE_OPERATIONS` (opérations manuelles fictives : charges prévues cohérentes avec la maquette de référence) et `window.COCKPIT_TRESORERIE_CALC`, positionné juste après `COCKPIT_FACTURATION_STATS` dont il dépend. `computeSnapshot(periodDays)` calcule en un seul point solde estimé, à encaisser/à décaisser, solde prévisionnel, prochains mouvements, charges prévues, factures à encaisser, alertes et une série de points pour le graphique — en réutilisant directement `COCKPIT_FACTURATION_STATS`/`COCKPIT_FACTURE_DETAILS`/`COCKPIT_DEVIS_CALC`/`COCKPIT_FACTURE_CALC` pour tout ce qui vient de la Facturation, sans dupliquer de logique.
- **`pages/tresorerie.html`** (remplace le placeholder) : sélecteur de période (30/60/90 jours) et bouton "Ajouter une opération" réel ; 4 cartes KPI (solde estimé, à encaisser, à décaisser, solde prévisionnel) ; bloc "Prochains mouvements" (timeline chronologique encaissements/décaissements, cliquable vers la facture concernée) ; tableau "Charges prévues" ; tableau "Factures à encaisser" (cohérent avec les paiements déjà enregistrés) ; graphique SVG simple de projection du solde sur la période ; bloc "Alertes & points d'attention" (factures en retard, charge importante sous 7 jours, risque de tension de trésorerie, encaissement important attendu). La modale "Ajouter une opération" (type, libellé, catégorie, montant, date, statut, notes, récurrence) modifie `TRESORERIE_OPERATIONS` en mémoire et recalcule immédiatement l'ensemble de la page.
- **Tableau de bord (`pages/dashboard.html`)** : nouveau bloc compact "Aperçu Trésorerie" (solde estimé, à encaisser, à décaisser, solde prévisionnel à 30 jours, alerte principale), sur le même principe que le bloc "Aperçu Facturation" de la V0.6.4. La carte KPI "Trésorerie disponible", jusqu'ici statique, reflète désormais le vrai solde estimé.
- **CSS** : nouveaux styles pour la timeline de mouvements (icônes encaissement/décaissement colorées) ; réutilisation des composants déjà existants (`data-table`, `badge`, `alert-item`, `dashboard-block`, graphique SVG type tableau de bord) pour rester visuellement cohérent sans redesign global.

Vérifications effectuées : test fonctionnel réel dans le navigateur. `tresorerie.html` sans erreur console, KPI cohérents avec les vraies factures/paiements existants (reste à encaisser 2 670,60 € sur 3 factures, cohérent avec `computeStats()`), charges prévues et factures à encaisser correctement listées et triées, alertes déclenchées correctement (factures en retard, charge sous 7 jours, risque de tension de trésorerie sur la période par défaut), graphique affiché sans erreur. Ajout d'une opération testé de bout en bout : la modale s'ouvre, l'enregistrement met à jour `TRESORERIE_OPERATIONS` et recalcule immédiatement les KPI et le tableau des charges. Bloc "Aperçu Trésorerie" et carte "Trésorerie disponible" du tableau de bord vérifiés cohérents avec `computeSnapshot()`. Non-régression ciblée confirmée sur `facturation.html` (statistiques et liste des devis inchangées), `agenda.html` (vues calendrier inchangées) et `facture-edition.html` (fiche facture et paiements inchangés) — aucune erreur console sur aucune page testée. Pas de non-régression complète de toute l'application.

Point de vigilance assumé : le solde prévisionnel calculé sur les données de démonstration peut apparaître négatif sur la fenêtre par défaut (30 jours), les charges prévues fictives dépassant les factures réellement en attente d'encaissement dans le jeu de données existant — c'est un comportement voulu (démontre l'alerte "Risque de tension de trésorerie"), pas un bug de calcul.

Commit créé sur la branche `v0.8-tresorerie`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.9 — Module Analyses (renommage de Finance)

Réalisée sur la branche dédiée `v0.9-analyses` (créée depuis `v0.8-tresorerie`, non encore fusionnée dans `main` à ce stade). Renomme le module "Finance" en **Analyses** et en fait un véritable cockpit d'analyse transversal — pas une comptabilité complète — qui recoupe les données déjà existantes (Facturation, Clients, Agenda, Trésorerie) sans dupliquer leurs calculs.

- **Renommage** : `pages/finance.html` renommé `pages/analyses.html` (`git mv`) ; lien et libellé de sidebar mis à jour sur les 15 pages (`Finance` → `Analyses`, `finance.html` → `analyses.html`), y compris les liens KPI du tableau de bord qui pointaient déjà vers cette page.
- **Données (`js/app.js`)** : nouveau bloc `window.COCKPIT_ANALYSES_CALC`, positionné après `COCKPIT_TRESORERIE_CALC` dont il dépend. `computeSnapshot(periodPreset)` calcule Vue d'ensemble, Commercial, Clients, Activité et une synthèse Trésorerie, en réutilisant systématiquement `COCKPIT_DEVIS_CALC`/`COCKPIT_FACTURE_CALC`/`COCKPIT_AGENDA_CALC`/`COCKPIT_FACTURATION_STATS`/`COCKPIT_TRESORERIE_CALC` — aucune formule monétaire ou métier n'est réinventée, seule l'agrégation par période/par client est nouvelle. Le sélecteur de période (Toutes périodes par défaut, Mois/Trimestre/Année en cours) ne s'applique qu'aux agrégations simples à recalculer (devis/factures/rendez-vous créés dans la période) ; le solde de trésorerie et les indicateurs clients restent des instantanés courants.
- **`pages/analyses.html`** (remplace le placeholder) : cinq onglets — **Vue d'ensemble** (CA facturé/encaissé, reste à encaisser, taux de transformation, clients actifs, RDV réalisés, solde de trésorerie estimé, bloc "Lecture rapide" à constats automatiques, alertes principales) ; **Commercial** (devis créés/acceptés/refusés/en attente, taux de transformation, montants moyens devis/facture, répartition des devis par statut en mini-barres, top devis) ; **Clients** (clients actifs/inactifs/en retard, top clients par CA facturé en mini-barres, tableau détaillé CA facturé/encaissé/reste à encaisser/devis/RDV/point d'attention) ; **Activité** (indicateurs RDV prévus/réalisés/reportés/sans suite, taux RDV→devis/RDV→facture, tunnel Rendez-vous → Devis → Facture, liste de points d'attention cliquables) ; **Trésorerie** (synthèse reprenant la V0.8 sans la refaire, lien direct vers le module).
- **Tableau de bord (`pages/dashboard.html`)** : nouveau bloc compact "Aperçu Analyses" (taux de transformation, top client, alerte principale), sur le même principe que les blocs "Aperçu Facturation"/"Aperçu Trésorerie" déjà existants — pas de refonte du dashboard.
- **CSS** : nouveaux styles pour les mini-graphiques en barres (`bar-chart-*`), le tunnel (`analyses-funnel-*`) et la liste de constats (`insight-*`), en réutilisant les composants déjà existants (`data-table`, `badge`, `alert-item`, `dashboard-block`, `page-tabs`, `form-grid-2`, `devis-summary-row`).

Vérifications effectuées : test fonctionnel réel dans le navigateur. `analyses.html` sans erreur console, cinq onglets fonctionnels, KPI Vue d'ensemble cohérents avec `computeStats()` existant (7 314,60 €/4 644,00 €/2 670,60 €/67 %), synthèse Trésorerie identique aux valeurs déjà vérifiées en V0.8 (solde estimé 8 044,00 €, solde prévisionnel -3 424,40 €), top clients et détails cohérents avec les factures/paiements réels, tunnel RDV→Devis→Facture et points d'attention agenda corrects, sélecteur de période testé (Mois en cours réduit correctement les compteurs). Bloc dashboard "Aperçu Analyses" vérifié cohérent. Une incohérence a été détectée et corrigée pendant les vérifications : l'alerte "factures en retard" apparaissait en double (une fois construite directement, une fois déjà incluse dans la synthèse Trésorerie) — la construction directe a été retirée au profit de la synthèse Trésorerie déjà correcte. Non-régression ciblée confirmée sur `facturation.html`, `agenda.html` et `tresorerie.html` — aucune erreur console sur aucune page testée. Pas de non-régression complète de toute l'application.

Commit créé sur la branche `v0.9-analyses`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## V0.9.1 — Refonte visuelle du module Analyses

Réalisée sur la même branche `v0.9-analyses`. Amélioration purement visuelle du module Analyses livré en V0.9, sans aucun changement de logique métier ni de calcul.

- Onglets en pilule (fond gris, actif surélevé en blanc/violet) et sélecteur de période stylé, scopés à la page (`#analyses-tabs`, `#analyses-period-select`) pour ne pas affecter les onglets Devis/Factures de `facturation.html`.
- KPI et bloc "Lecture rapide" transformés en cartes colorées (icône + titre + sous-texte) — mêmes faits qu'avant, juste restructurés pour l'affichage (`computePointsCles` retourne désormais des objets structurés plutôt que des phrases toutes faites).
- "Répartition des devis" et nouveau "Charges prévues par catégorie" (onglet Trésorerie) en donuts CSS (`conic-gradient` + légende), à la place de listes de barres.
- Tunnel Rendez-vous → Devis → Facture enrichi de flèches et de taux de conversion entre étapes.
- Vue d'ensemble enrichie d'un aperçu compact "Top clients" et d'un mini-donut "Répartition des devis", réutilisant les données déjà calculées par `COCKPIT_ANALYSES_CALC`.

Vérifications effectuées : rechargements successifs sans erreur console ; contenu vérifié sur les 5 onglets (cartes lecture rapide, donut devis, top clients, funnel avec pourcentages, donut charges par catégorie) ; non-régression rapide sur `dashboard.html`, `facturation.html` et `tresorerie.html` (aucune erreur console, composants partagés `.kpi-value`/`.alert-item`/`.page-tab` intacts).

Commit créé sur la branche `v0.9-analyses`. Pas de merge dans `main`, pas de tag, pas de push — décision laissée à la revue humaine.

## Régularisation Git — V0.7 à V0.9.1 fusionnées dans `main`

`origin/main` s'était arrêté à la V0.6.4 pendant que les versions suivantes (V0.7, V0.7.1, V0.8, V0.8.1, V0.8.2, V0.9, V0.9.1) avaient été développées sur une chaîne de branches locales (`v0.7-agenda-commercial` → `v0.8-tresorerie` → `v0.9-analyses`), jamais fusionnées ni poussées. Un audit Git complet (branches, tags, `merge-base`, `fsck`) a confirmé un historique **linéaire et propre** : chaque branche démarre exactement de la pointe de la précédente, sans divergence ni commit perdu.

Une branche de sauvegarde `backup/pre-regularisation-v0.9` a été créée avant toute opération. `main` a ensuite été avancée par un simple **fast-forward** jusqu'à la pointe de `v0.9-analyses` (`125d533`) — aucun commit recréé, aucun merge artificiel. Les tags suivants ont été posés sur les commits correspondant à chaque étape validée :

- `v0.7` → `bc8e8a3` (module Agenda commercial)
- `v0.7.1` → `86cee37` (vues calendrier et enrichissements Agenda)
- `v0.8` → `5050dc9` (module Trésorerie, incluant les correctifs V0.8.1/V0.8.2)
- `v0.9` → `8aeb13a` (module Analyses)
- `v0.9.1` → `125d533` (refonte visuelle Analyses)

`main` et `origin/main` ont ensuite été resynchronisés (`git push origin main --tags`). Documentation mise à jour en conséquence (`README.md`, `docs/roadmap-v0bis.md`, `docs/backlog.md`, `PROJECT_INDEX.md`, README internes) pour refléter la V0.9.1 comme dernière version présente sur `main`.

## V0.9.2 — Configuration personnalisable de démonstration

Réalisée sur la branche dédiée `v0.9.2-demo-config` (créée depuis `main` après la régularisation). Note de nommage : la demande initiale suggérait "V0.9.1" pour cette fonctionnalité, mais ce nom était déjà utilisé par la refonte visuelle du module Analyses (commit déjà poussé) ; comme aucun commit existant n'est jamais réécrit, cette amélioration est numérotée **V0.9.2**.

- **`js/demo-config.js`** (nouveau) : centralise l'identité fictive utilisée pendant une démonstration — `prenom`, `nom`, `fonction`, `entreprise`, `activite`, `ville`, `email`, `telephone`, `objectifMensuel` — avec valeurs par défaut fictives et fallback propre si un champ est vide. Chargé avant `js/app.js` sur les 19 pages qui en dépendent (ajout d'une seule balise `<script>`, aucun autre changement de structure). Aucune donnée personnelle réelle, aucun secret, aucune persistance, aucun backend.
- **Connexions** (éléments les plus visibles pendant une démo, sans toucher aux données métier fictives ni refondre l'application) : salutation et sous-titre du tableau de bord ; légende "Objectif mensuel" (dashboard et `pages/settings.html`) ; carte "Compte utilisateur" de `pages/settings.html` (jusqu'ici un placeholder sans donnée) ; `window.COCKPIT_COMPANY_SETTINGS.nom/telephone/email`, donc `pages/parametres-entreprise.html` et l'émetteur de tous les devis/factures/documents imprimables. Le reste des données de démonstration (clients, produits/services, devis, factures, rendez-vous, trésorerie) n'est pas concerné.
- **Documentation** : section "Personnaliser la démo pour un prospect" ajoutée à `docs/README.md`, expliquant qu'il suffit de modifier `DEMO_CONFIG` dans `js/demo-config.js`.

Vérifications effectuées : test fonctionnel réel dans le navigateur avec les valeurs par défaut (volontairement identiques aux données fictives déjà en place, pour ne rien changer visuellement tant que la config n'est pas personnalisée) — `dashboard.html` (salutation, sous-titre, objectif), `settings.html` (comptes utilisateur et objectifs mensuels), `parametres-entreprise.html` et `devis-document.html` (nom d'entreprise cohérent partout via `window.COCKPIT_COMPANY_SETTINGS`) ; non-régression rapide sur `agenda.html` et `tresorerie.html`. Aucune erreur console sur aucune page testée.

Commit créé sur la branche `v0.9.2-demo-config`, fusionnée dans `main` et taguée `v0.9.2`.

## V0.10.1 — Cohérence globale UI/UX

Réalisée sur la branche dédiée `v0.10.1-coherence-ui-ux` (créée depuis `main`, à jour de la V0.9.2). Version transversale de cohérence visuelle et ergonomique : aucun nouveau module, aucune règle métier modifiée. Audit complet des 19 pages et de `css/styles.css`, synthèse présentée avant développement, puis corrections ciblées.

**Audit — constats retenus** :
- *Bloquant* : les 4 blocs de synthèse du tableau de bord ("Agenda commercial", "Aperçu Facturation", "Aperçu Trésorerie", "Aperçu Analyses") réutilisaient `.devis-summary` (plafonnée à 320px, alignée à droite) — un composant conçu pour le récapitulatif de devis/facture, pas pour un bloc pleine largeur.
- *Bloquant, découvert en vérification responsive* : aucune des 19 pages ne déclarait de balise `<meta name="viewport">`. Sans elle, les navigateurs mobiles utilisent une largeur de mise en page virtuelle d'environ 980px, ce qui empêche les points de rupture `@media` existants (760/900/1100px) de jamais s'activer sur un téléphone réel — la sidebar ne se repliait jamais, aucun tableau ne pouvait défiler proprement.
- *Incohérence importante* : `pages/fiche-rdv.html` était la seule fiche sans recherche/notification dans la topbar et sans fil d'Ariane ; ordre des icônes KPI de `tresorerie.html` rompu (1-3-2-4 au lieu de 1-2-3-4) ; classe orpheline `analyses-kpis-8` sur `analyses.html` (7 cartes réelles, aucune règle CSS associée) ; ordre des boutons d'action différent entre `devis-edition.html` et `facture-edition.html`.
- *Polish* : style inline `justify-content: flex-start` dupliqué sur 3 pages ; `.kpi-value` sans troncature propre en cas de montant long ; débordement horizontal mineur de la topbar du tableau de bord à largeur intermédiaire (bouton date).
- *Volontairement laissé inchangé* (justifié, pas un bug) : liens agenda en `target="_blank"` (préservation de contexte de filtre, décision V0.7.1) ; liste Devis en HTML statique vs Factures en rendu JS sur `facturation.html` (changement de pipeline de rendu, pas visuel, dette déjà notée dans `docs/backlog.md`) ; onglets pilule et sélecteur de période arrondi d'Analyses (style délibéré validé en V0.9.1) ; mécanisme "Enregistrer" (devis, brouillon/définitif) vs "Émettre" (facture, one-way) — différence de cycle de vie métier légitime.

**Corrections appliquées** :
- **CSS (`css/styles.css`)** : nouvelle classe `.dashboard-stats-summary` (pleine largeur, sans le plafonnement de `.devis-summary`) ; `.kpi-value` complétée avec `overflow: hidden; text-overflow: ellipsis; max-width: 100%` ; nouvelle classe utilitaire `.page-toolbar-start` ; `.topbar-actions` passe en `flex-wrap: wrap` pour absorber le bouton date du tableau de bord aux largeurs intermédiaires ; ajout de `<meta name="viewport" content="width=device-width, initial-scale=1">` sur les 19 pages ; filet de sécurité `.page-content { overflow-x: auto; }` sous 760px pour que tableau ou bloc large défile dans sa zone plutôt que de casser la mise en page.
- **`pages/dashboard.html`** : les 4 blocs de statistiques passent de `.devis-summary` à `.dashboard-stats-summary` (changement de classe uniquement, JS de remplissage inchangé).
- **`pages/fiche-rdv.html`** : ajout de la recherche/notification en topbar et d'un fil d'Ariane "Agenda › Fiche rendez-vous" (`.fiche-top-row` + `.breadcrumb`), alignés sur le pattern des autres fiches ; suppression du style inline devenu redondant.
- **`pages/tresorerie.html`** : ré-ordonnancement des icônes KPI (1-2-3-4).
- **`pages/analyses.html`** : suppression de la classe CSS orpheline `analyses-kpis-8`.
- **`pages/facture-edition.html`** : le bouton principal "Émettre la facture" passe en première position de la barre d'actions, avant "Aperçu / Imprimer", pour s'aligner sur l'ordre de `devis-edition.html` (aucun id ni logique modifié).
- **`pages/devis-edition.html`, `pages/parametres-entreprise.html`, `pages/facture-edition.html`** : remplacement du style inline dupliqué par `.page-toolbar-start`.

**Documentation corrigée** :
- **`README.md`** : section "Limites de la V0" entièrement réécrite pour distinguer interactions réellement fonctionnelles en mémoire, fonctionnalités en Work in progress, absence de persistance durable, absence de backend/base de données, absence d'authentification réelle, absence d'IA — sans minimiser le niveau fonctionnel atteint. Sections "Suggestions d'amélioration" et "Points à prévoir pour la V1" nettoyées des entrées déjà résolues (recherches/filtres, création réelle de devis/factures/RDV). Note de version mise à jour.
- **`docs/backlog.md`** : entrée Agenda corrigée (les vues calendrier Jour/Semaine/Mois sont réelles depuis la V0.7.1, ce n'était plus "volontairement non développé") ; entrée "Dépôt GitHub distant à créer" marquée résolue (régularisation Git de juillet 2026) ; entrée "Recherche et filtres" reformulée pour ne viser que la recherche globale transverse, pas les filtres par page déjà réels ; nouvelles entrées UX/UI référençant les constats V0.10.1 traités, la future V0.10.2 (refonte dashboard) et la limite assumée sur la liste Devis statique.

**Vérifications effectuées** : test fonctionnel réel dans le navigateur (serveur statique local) sur les 19 pages — aucune erreur console. Recherche/filtre Clients testés (filtrage réel), vues Agenda Jour/Semaine/Mois/Liste testées (bascule correcte), onglets Devis/Factures de `facturation.html` testés, bloc Paiements d'une facture Émise vérifié, les 5 onglets d'Analyses testés un par un, fiche rendez-vous (consultation et création) vérifiée, documents imprimables (devis/facture/RDV) vérifiés. Layout mesuré programmatiquement à largeur desktop, tablette (768px) et mobile (375px, après correction du viewport) : plus aucun débordement horizontal détecté sur `dashboard.html` ni `clients.html` (le tableau large défile désormais dans sa propre zone au lieu de faire défiler toute la page). Fallback `DEMO_CONFIG` revérifié sur le code (valeur vide → `DEFAULTS`).

**Limites restantes** : la liste "Devis" de `facturation.html` reste en HTML statique (contrairement aux "Factures" en rendu JS) — non traité, changement de pipeline de rendu hors périmètre visuel. La refonte structurelle du tableau de bord est explicitement reportée à une V0.10.2.

Commit créé sur la branche `v0.10.1-coherence-ui-ux`. Pas de merge dans `main`, pas de tag — décision laissée à la revue humaine.

### Correctif de revue V0.10.1 — exactitude README + réévaluation `.kpi-value`

- **`README.md`** : la formulation "changement de statut et ajout de notes sur les fiches client et produit/service, via de vraies modales" exagérait le niveau réel — vérifié dans `js/app.js` (`makeSaveButton`), le bouton de validation de ces modales (statut client, statut/type produit, notes) porte la classe `btn-wip` et ne modifie ni `CLIENT_DETAILS` ni `PRODUCT_DETAILS`. Reformulé et déplacé vers "Work in progress", en distinguant explicitement ouverture/préremplissage réels vs absence de changement effectif vs absence de persistance.
- **`.kpi-value` (overflow/ellipsis ajoutés en V0.10.1)** : réévalué avec les montants les plus longs réels (Dashboard, Trésorerie, Analyses) à largeur desktop (1280px), tablette (768px) et mobile (375px) — **aucune troncature réelle constatée** dans aucun cas. Test complémentaire avec des montants synthétiques extrêmes (ex. "-12 345 678,90 €") : la carte KPI s'agrandit plutôt que de tronquer (comportement de dimensionnement automatique des grilles CSS, préexistant à la V0.10.1, non introduit par cette version). La règle reste donc un filet de sécurité inerte avec les données actuelles, sans effet de masquage de montant constaté — **conservée telle quelle**, aucune modification nécessaire.

Vérifications : rechargement de `dashboard.html`, `tresorerie.html`, `analyses.html` à desktop/tablette/mobile, aucune erreur console, aucune régression visuelle constatée. Nouveau commit sur `v0.10.1-coherence-ui-ux`. Toujours pas de merge dans `main`, pas de tag.

## V0.10.2 — Refonte du tableau de bord en cockpit quotidien

Réalisée sur la branche dédiée `v0.10.2-dashboard-cockpit` (créée depuis `main`, à jour de la V0.10.1, tag `v0.10.1` sur `495ae7c`). Remplace l'empilement de blocs accumulés depuis la V0.1 (4 KPI statiques, agenda du jour, to-do liste statique, alertes statiques, graphique CA statique, indicateurs favoris, blocs "Aperçu Facturation/Trésorerie/Analyses") par une structure à trois niveaux organisée autour de trois questions : où en est mon entreprise, que dois-je faire aujourd'hui, comment évolue mon activité.

**Niveau 1 — Situation générale** : 4 KPI recalculés selon une période de pilotage choisie (mois civil par défaut, 30 derniers jours, trimestre civil, période personnalisée avec dates explicites) — CA facturé sur la période (`FACTURE_DETAILS` filtrées par `dateEmission`, statut `emise`), Encaissé sur la période (paiements filtrés par leur propre date), Trésorerie disponible (solde estimé + prévision à un horizon configurable — fin de mois, fin du mois suivant, 30/60/90 jours — converti en nombre de jours avant d'appeler `COCKPIT_TRESORERIE_CALC.computeSnapshot`), Objectif (pourcentage de `DEMO_CONFIG.objectifMensuel` proratisé à la durée de la période). Les 4 cartes sont cliquables vers le module pertinent (Facturation, Trésorerie, Analyses).

**Niveau 2 — Ma journée** : mini-agenda vertical quotidien (grille horaire dynamique centrée sur les rendez-vous du jour affiché, fenêtre étendue automatiquement avec défilement interne si l'amplitude est large, navigation Jour précédent/Aujourd'hui/Jour suivant, ligne d'heure actuelle si le jour affiché est aujourd'hui, état vide propre avec liens vers l'Agenda) + to-do list réellement fonctionnelle en mémoire de page (ajout/modification/suppression/complétion/réouverture via le moteur de modale générique `COCKPIT_MODAL`, filtre À faire/Terminées, défilement interne) + alertes calculées depuis les données réelles (alertes de trésorerie reprises telles quelles depuis `computeSnapshot(...).alertes`, rendez-vous non confirmé imminent, rendez-vous reporté plusieurs fois via `estReportePlusieursFois`, devis à relancer, objectif en retard — classées par gravité, 4 maximum, aucune alerte fictive comme l'ancienne "Stock faible").

**Niveau 3 — Performance** : graphique de performance financière configurable (SVG, 6 ou 12 derniers mois, jusqu'à 3 séries simultanées parmi CA facturé / Encaissé / Solde net via un menu à cases à cocher, tooltip custom réutilisant le principe validé en Trésorerie — encadré blanc, ombre douce, coins arrondis) + tunnel commercial compact (Rendez-vous → Devis acceptés → Factures émises → Factures payées, volume/montant/taux de passage par étape, réutilise les classes `.analyses-funnel-*` déjà existantes) + "Lecture commerciale" (jusqu'à 3 constats automatiques calculés depuis le tunnel, explicitement non présentés comme de l'intelligence artificielle).

**`js/pilotage-config.js`** (nouveau) : centralise les valeurs par défaut du Dashboard (période de pilotage, horizon de trésorerie, plage horaire d'agenda, période/indicateur par défaut du graphique). Chargé avant `js/app.js` sur `dashboard.html` et `settings.html`. Aucune propagation réelle entre les deux pages (pas de backend, pas de `localStorage` — voir `docs/decisions.md`) : la carte "Pilotage et activité" de `settings.html` affiche honnêtement les valeurs par défaut réellement utilisées, "Modifier" reste en Work in progress ; les vrais contrôles vivent sur le Dashboard lui-même.

**Limite assumée** : "Montant décaissé" et "Reste à encaisser" ne sont pas proposés comme séries du graphique de performance — `TRESORERIE_OPERATIONS` n'a pas de profondeur historique fiable sur 6-12 mois dans le jeu de données fictif actuel (voir `docs/decisions.md`).

**Fichiers modifiés** : `pages/dashboard.html` (contenu du `<main>` entièrement réécrit, sidebar/topbar inchangées), `pages/settings.html` (+1 carte "Pilotage et activité"), `js/app.js` (suppression des 4 anciennes IIFE liées aux blocs "Aperçu ..." et à l'agenda/statistiques du dashboard, ajout d'une nouvelle IIFE couvrant l'ensemble du cockpit + une IIFE pour la carte Paramètres), `css/styles.css` (nouvelles classes scopées : `.dashboard-cockpit-level`, `.dashboard-journee-grid`, `.mini-agenda-*`, `.dashboard-priorites-*`, `.dashboard-performance-grid`, `.dashboard-chart-*`, `.dashboard-tunnel-extra`), `js/pilotage-config.js` (nouveau).

**Vérifications effectuées** : test fonctionnel réel dans le navigateur — 4 KPI recalculés correctement testés sur les 4 presets de période (mois/30j/trimestre/personnalisée, montants vérifiés manuellement par recoupement avec les données `FACTURE_DETAILS`) ; mini-agenda testé sur un jour à un seul rendez-vous (aujourd'hui), un jour différent, un jour vide (état vide + liens), et un cas synthétique à deux rendez-vous extrêmes (02h/23h) pour vérifier le défilement interne ; to-do testée de bout en bout (ajout, bascule terminée/à faire, filtre, suppression) ; alertes calculées vérifiées cohérentes avec les données réelles (factures en retard, charge à payer, risque de trésorerie, rendez-vous reporté plusieurs fois) ; graphique testé en 6 et 12 mois, sélection d'une/deux/trois séries, tooltip au survol ; tunnel commercial et constats automatiques vérifiés cohérents avec `DEVIS_DETAILS`/`FACTURE_DETAILS`/`RDV_DETAILS`. Layout mesuré programmatiquement à 1280px, 768px et 375px : un débordement horizontal a été détecté et corrigé pendant les vérifications (voir ci-dessous). Non-régression : `agenda.html`, `facturation.html`, `tresorerie.html`, `analyses.html`, `devis-edition.html`, `facture-edition.html`, `fiche-client.html`, `fiche-rdv.html`, `fiche-produit-service.html`, `clients.html`, `produits-services.html`, `parametres-entreprise.html`, `settings.html`, `devis-document.html`, `facture-document.html`, `rdv-document.html`, `database.html`, `login.html` — aucune erreur console sur aucune page.

**Correction pendant la recette** : à 375px, `.dashboard-chart-block` débordait le viewport (jusqu'à 426px) à cause d'un piège classique de CSS Grid — une piste de grille en `1fr` s'agrandit par défaut pour accueillir le contenu non enveloppé de ses descendants (`min-width: auto` implicite sur les items de grille), même quand ce contenu est visuellement plus étroit une fois affiché. Corrigé en ajoutant `min-width: 0` sur les blocs enfants de `.dashboard-journee-grid` et `.dashboard-performance-grid`, plus `flex-wrap: wrap` sur l'en-tête et les contrôles du bloc graphique.

Commit créé sur la branche `v0.10.2-dashboard-cockpit`. Pas de merge dans `main`, pas de tag `v0.10.2` — décision laissée à la revue humaine visuelle.
